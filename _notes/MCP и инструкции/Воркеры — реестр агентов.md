# Реестр воркеров — AI-система маркетинга АйДаКемп

> Каждый агент выбирает ветку, регистрируется здесь, указывает зону ответственности и статус.  
> Обновляй запись при каждом существенном изменении архитектуры.

---

## 🌿 Ветки системы

| Ветка | Зона | Воркер |
|-------|------|--------|
| **ads-analytics** | Реклама + аналитика (Директ, VK, Метрика, ETL, отчёты) | ✅ Claude #1 |
| **content** | Контент, тексты, SEO, посты | свободна |
| **crm** | Клиенты, воронка, CRM, мессенджеры | свободна |
| **site** | Сайт aidacamp.ru, UX, разработка, фиксы | свободна |
| **hypotheses** | Гипотезы, A/B тесты, ai_hypotheses | свободна |

---

## 👷 Claude #1 — Реклама и аналитика

**Ветка:** `ads-analytics`  
**Зарегистрирован:** 23 апреля 2026  
**Статус:** 🟢 активен

### Зона ответственности
- ETL-пайплайн (`/opt/etl/etl-daily.py`) → PostgreSQL
- Таблицы: `direct_campaign_stats`, `metrika_utm`, `metrika_goals`, `metrika_traffic`, `clarity_daily` и все метрика-таблицы
- Отчёты: куда уходят деньги, CPA по кампаниям, CR по UTM
- Кампании Яндекс Директ: стратегии, ставки, минус-слова, площадки
- Кампании VK Ads: плейсменты, креативы, сплит-тесты
- UTM-схема и конверсии по заголовкам

### Сделано в этой сессии (23.04.2026)
- [x] Починил ETL: все таблицы Метрики теперь пишутся (metrika_traffic, goals, utm, demographics, geo, devices, entry_pages, search_phrases)
- [x] Создал `clarity_daily` — Clarity теперь в БД
- [x] Загрузил данные за апрель (18 499 строк)
- [x] Отчёт «где деньги» → `Маркетинг/Аналитика/Отчёт-апрель-2026-где-деньги.md`
- [x] Воронка: 567 «Выбор возраста» → 51 «Выбрать смену» → 6 заявок → 24 звонка
- [x] Найден UX-баг: нижняя плашка автоматически инициирует вызов → браузер блокирует → звонки теряются

### Текущие задачи
- [ ] Починить нижнюю плашку телефона (auto-call → `<a href="tel:">`)
- [ ] Масштабировать `competitors` в РСЯ (CR 51%)
- [ ] Масштабировать `nophone`/`price` на поиске (CR 83–93%)
- [ ] Исключить мусорные площадки: `com.vitastudio.mahjong`, `dsp-minimob-ww.yandex.ru`

### Архитектура ETL (актуально)

```
Яндекс Директ API → direct_campaign_stats (CPA, клики, затраты)
                  → direct_placements (площадки РСЯ)
                  → direct_search_queries (поисковые запросы)

Яндекс Метрика API → metrika_traffic (источники трафика)
                   → metrika_goals (достижения целей по источнику)
                   → metrika_utm (конверсии по UTM-меткам)
                   → metrika_demographics, geo, devices, entry_pages, search_phrases

Microsoft Clarity API → clarity_daily (dead-click, scroll, сессии)
                      → clarity_pages (по страницам)
```

### Ключевые константы
- Основная цель: `GoalId 541048270` «Выбор возраста» (567/мес)
- Заявки: `GoalId 541048197` (6/мес — не использовать для обучения)
- CPA цель Директ: 400–500₽ (реальная ~161₽)
- Лучший канал: РСЯ старая (708698819), CPA 128₽
- Лучший контент: `competitors` CR 51%, `nophone` CR 93%

---

## 📐 Архитектура AI-системы маркетинга (общая)

```
PostgreSQL (56 таблиц)
    ├── Реклама: direct_*, vk_*
    ├── Аналитика: metrika_*, clarity_*
    ├── CRM: crm_contacts, ai_customers
    ├── Контент: ai_dialogs, knowledge_chunks
    └── Гипотезы: ai_hypotheses

Obsidian (база знаний)
    ├── Портрет клиента
    ├── Контекст для агентов
    ├── UTM-схема
    └── Отчёты

MCP-сервер (aidacamp-tools)
    ├── stats — SQL к PostgreSQL
    ├── ssh — сервер
    ├── direct_* — Яндекс Директ API
    └── vk_* — VK Ads API
```

---

*Создано: 23 апреля 2026 | Обновлять при смене ответственности*
