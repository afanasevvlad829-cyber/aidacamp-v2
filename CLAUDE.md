# АйДаКемп — Project Rules

> **⚡ Правило №1 (разработка):** вся работа по сайту — в изолированном Docker-контейнере: `./scripts/agent-docker.sh "<задача>" "<бриф>"`. Агент клонирует репо из GitHub в одноразовый non-root контейнер, прод/секреты недоступны. Локально только хотфиксы владельца. Подробности — [`DEV_PROTOCOL.md`](DEV_PROTOCOL.md) → «Правило №1» + [`docker/agent/README.md`](docker/agent/README.md). ⚠️ Старый `vps-start-agent.sh` (агент под root на проде) — **DEPRECATED**, небезопасен.

## 📂 Карта документов (читай первым)

| Файл | За что отвечает |
|---|---|
| **`CLAUDE.md`** (этот файл) | Позиционирование, бренд, инциденты, правила контента, MCP |
| **`DEV_PROTOCOL.md`** | Стек, git workflow, deploy, тестирование — всё по разработке сайта |
| **`AGENTS.md`** | Git safety rules, деструктивные операции, UI architecture |
| **`DESIGN_SYSTEM.md`** | Единственный источник правды по UI: цвета, типографика, иконки, компоненты |
| **`INFRA.md`** | Карта инфраструктуры: домены → nginx → сервисы → порты |
| **`TOOLS.md`** | MCP-инструменты, Browser Agent, stats.sh — полная документация |
| **`PHOTOS.md`** | Фотоархив 9200 фото: поиск, скачивание, сцены |
| **`_notes/Библиотека знаний/`** | Правила по Директу, VK, SEO, налоговому вычету |
| **`.claude/skills/systematic-debugging/`** | Трабошутинг — корневая причина до фикса, 7 фаз |
| **`.claude/skills/task-completion/`** | Протокол проверки перед сдачей любой задачи |

**Правило:** перед задачей открывай нужный файл, не держи всё в памяти.

---

## ⚙️ Ресурсы: работать максимально на сервере, не на маке

Локальный мак владельца — MacBook Air M1, 8 ГБ RAM (распаяно, не апгрейдится). Несколько параллельных тяжёлых процессов (браузеры, MCP-серверы) забивают своп и тормозят все агентские сессии разом (инцидент 11.07.2026).

**Приоритет размещения тяжёлых/фоновых процессов (headless-браузеры, воркеры, долгие вычисления):**
1. **Удалённый MCP-сервис** (по образцу `aidacamp-tools`) — если задаче не нужен именно локальный контекст пользователя (его залогиненный браузер и т.п.)
2. **systemd-сервис на проде** (`aidacamp-prod`, 159.194.223.55) **с явными cgroup-лимитами** (`MemoryMax=`, `CPUQuota=` в юните) — сервер сам ограничен (5.8 ГБ RAM, диск часто 90%+), поэтому перенос без лимитов недопустим. Перед добавлением — проверить `free -h` и `df -h`.
3. **Локально на маке** — только когда реально нужен пользовательский контекст (открытый Chrome с личными логинами) или для мгновенных разовых команд.

**Доступ с мака к серверным демонам:** SSH-туннель как **launchd-агент** в `~/Library/LaunchAgents/` (автозапуск при логине, автовосстановление при обрыве через `KeepAlive`) — пример: `ru.aidacamp.agent-browser-tunnel.plist` (проброс CDP-порта headless Chrome). Не поднимать туннели вручную `ssh -N -L` в фоне сессии — умирают вместе с сессией/окном терминала.

Карта серверных мощностей и портов — [`INFRA.md`](INFRA.md); MCP-инструменты — [`TOOLS.md`](TOOLS.md).

**⏳ Долгие операции — НИКОГДА одним блокирующим вызовом.** Ни `sleep N` в команде, ни цепочка
блокирующих запросов подряд — у клиента свой тайм-аут независимо от сервера (Claude Desktop
особенно, ~4 мин, не настраивается), и если он сработает раньше сервера — результат может
потеряться безвозвратно (инцидент 13.07.2026: потеряли 4 из 8 видео-генераций Replicate). Только
фон на сервере (`nohup`/tmux/systemd) + лог-файл + короткая проверка (`tail`). Готовый пример и
полный паттерн — [`TOOLS.md`](TOOLS.md) → «Долгое ожидание / поллинг внешнего API»,
`/opt/scripts/replicate-poll.sh`. Правило одинаково для ВСЕХ агентов и во всех 4 проектах.

---

## 🎯 Позиционирование бренда — читай ПЕРЕД любой коммуникацией с клиентом

**Любой текст для сайта, креативов, рассылок, постов, скриптов звонков — ДОЛЖЕН
соответствовать позиционированию.** Источники:

- **`_notes/АйДаКемп/Маркетинг/Позиционирование бренда.md`** — образ Дарьи, tone of voice, красные линии
- **`_notes/АйДаКемп/Клиенты/Портрет клиента.md`** — глубинная мотивация, страхи

**Главный принцип:** Мы продаём маме ощущение "я хорошая", а не пользу для детей.
Продукт = социально-одобряемая вещь, которой можно похвастаться подругам + закрыть чувство вины.

**🚫 Запрещено в коммуникации:**
- Нотации родителям ("вы не уделяете внимание")
- Педагогические термины ("soft-skills", "формирование фундамента")
- Серьёзный тон "эксперта в очках"
- Шок-контент, сравнение с конкурентами в лоб

**✅ Работает:** лёгкий тон подруги, красивые фото с улыбками, социальное
подтверждение (отзывы/Яндекс.Карты), налоговый вычет, образ Дарьи как мамы-идеала.

**Перед любым текстом — 5 вопросов:**
1. Смогла бы мама похвастаться этим в чате с подругами?
2. Не обвиняю ли я её в этой фразе?
3. Понятны ли слова на уровне Instagram?
4. Виден ли лёгкий путь наверх к Дарье?
5. Фото — захотела бы она показать это в своём профиле?

Если хоть один «нет» — переписываем.

---

## 📚 Библиотека знаний (читай перед работой с Директом/VK/SEO)

Живая база правил, инцидентов и best practices. Грабли уже записаны — не наступай повторно.

- **[_notes/Библиотека знаний/](_notes/Библиотека%20знаний/README.md)** — корневой индекс
  - [Яндекс.Директ — основные правила.md](_notes/Библиотека%20знаний/Яндекс.Директ%20—%20основные%20правила.md)
  - [VK Ads — основные правила.md](_notes/Библиотека%20знаний/VK%20Ads%20—%20основные%20правила.md)
  - [SEO — основные правила.md](_notes/Библиотека%20знаний/SEO%20—%20основные%20правила.md)
  - [Налоговый вычет — правила и данные.md](_notes/Библиотека%20знаний/Налоговый%20вычет%20—%20правила%20и%20данные.md)

**Правило:** любой инсайт из сессии → фиксировать в нужный файл. Структура: **Симптом / Причина / Решение / Правило** + дата.

---

## 🎨 UI-правила (краткий чек)

**Полная документация — только в [`DESIGN_SYSTEM.md`](DESIGN_SYSTEM.md).** Здесь только критичные запреты:

- **Иконки** — только Bootstrap Icons из `src/data/icons-manifest.json`. Никаких эмодзи в UI.
- **Иконка не в манифесте** → добавить в JSON → `npm run icons`. Никогда не редактировать `icons.css` вручную.
- **Эталонные компоненты:** `src/components/Shifts.astro`, `src/components/FAQ.astro`.

---

## 📝 Брендовые правила для текстов

- **Дарья Афанасьева** — мама **дочери**-подростка (не сына!). Цитаты: «у дочери», «она сама» — НЕ «у сына».
- Тон Дарьи — подруга, не эксперт. Конкретные наблюдения мамы-к-маме.

### 🚫 BANNED WORDS — глобальный запрет

| Запрещено | Чем заменить |
|---|---|
| `единиц` / `единицы` / `единицами` | `рубл-` (игровая валюта лагеря) |
| `баллов` / `баллы` / `балла` / `балл` | `рубл-` |
| массовое `дети` / `ребёнок` (>20 раз в статье) | `ребята`, `школьники`, `подростки`, `он/она` |

Контроль: `npm run check:banned` — встроен в `npm run build`, при нарушении билд падает.

### 🎯 Единый источник смен, дат и цен — НЕ дублировать

**Один источник ДАННЫХ + одно правило роста.** Всё остальное (компоненты, бот, SEO) **выводится** — никаких хардкодов.

| Что | Источник правды | Как брать |
|---|---|---|
| **База цены, даты (start/end), длительность** | `src/data/shifts.ts` → `SHIFT_META` (+ строки `price:`/`startDate:` каждой смены) | импорт |
| **Какие смены открыты к брони** | `src/data/shifts.ts` → `mainShifts` / `shortShifts` | импорт массива |
| **Текущая цена (с ростом)** | `src/data/dynamicPrices.ts` (правило поверх `SHIFT_META`) | `getCurrentPrice(shiftId)` |
| **Цена трансфера от м. Солнцево** | `src/data/shifts.ts` → `TRANSFER_PRICE` | `TRANSFER_PRICE_FMT` / `TRANSFER_INFO` / `TRANSFER_PRICE_ROUNDTRIP_FMT` |

**📈 ЕДИНОЕ ПРАВИЛО РОСТА ЦЕНЫ (для всех смен одинаковое, в `dynamicPrices.ts`):**
1. До «старт − 30 дней» — цена = базовой.
2. За 30 дней до старта — разовый шаг **+5 %** к базе.
3. Далее каждый день **+500 ₽**.
4. На дату старта рост **останавливается** (цена фиксируется).
- Параметры: `RAMP_DAYS=30`, `STEP_PCT=0.05`, `DAILY_INC=500`. Менять правило → только эти константы.

**Правила:**
- Дату/базовую цену/длительность смены менять ТОЛЬКО в `shifts.ts`. `dynamicPrices.ts` берёт их из `SHIFT_META` и применяет правило роста — дублировать дату/цену в нём НЕЛЬЗЯ.
- Цену смены НИКОГДА не хардкодить в компонентах/боте/SEO — только `getCurrentPrice()`.
- Завершить/открыть смену = добавить/убрать её в `mainShifts`/`shortShifts` — **в одном месте**. Бот (`campData.ts`) и SEO (`SchemaOrg.astro`) фильтруются по этому списку автоматически.
- Бот не ведёт свой список смен: `campData.ts` берёт цены через `getCurrentPrice()` и фильтрует смены по `mainShifts`/`shortShifts`.
- Стражи `npm run check:prices` и `check:dates` роняют билд при литерале цены/даты смены вне `shifts.ts`.
- Цену трансфера (2000₽) НИКОГДА не хардкодить строкой в лендингах/промптах бота — только `TRANSFER_PRICE_FMT`/`TRANSFER_INFO`. Страж `npm run check:transfer` роняет билд при хардкоде «2 000 ₽» вне `shifts.ts` (инцидент: на 5+ лендингах цена трансфера разошлась на «бесплатно» vs «2000₽», т.к. хардкодилась в каждом файле отдельно).

### 🧱 Новые гео/возрастные SEO-лендинги — через `[slug]`, не копипастой

В `src/pages/` уже 180+ отдельных `.astro`-файлов под гео/возрастные ключи
(`lager-klin.astro`, `lager-11-let.astro` и т.п.) — это не забытый рефакторинг,
а результат сознательной SEO-политики: разная формулировка на каждой странице
защищает от того, что Google посчитает комбинаторные лендинги шаблонным
дублирующимся контентом (см. `src/data/seasons.ts`: «Тексты сезонов сознательно
НЕ клонируются — привет, Яндекс»). Массово переносить существующие страницы в
единый `[slug].astro`-шаблон **не нужно** — риск (просадка позиций у
проиндексированных страниц) больше выгоды (меньше файлов кода).

Правило — только для **новых** страниц:
- Если новая страница реально шаблонная (тот же гео/возрастной паттерн, без
  уникального смыслового угла) — заводи через `src/data/landings/` +
  `[slug].astro` (уже 17 записей), не копируй существующий `.astro`-файл.
- Если у страницы есть собственный смысловой угол (не просто «замени город») —
  обычный `.astro`-файл, как раньше.

> ⚠️ Инцидент 2026-06-20: бот предлагал завершённую Смену 2 (свой хардкод-список) и тянул цены из мёртвого `/var/www/aidacamp-data/shifts.json` (cron генерации исчез ~4 мая). Исправлено: бот фильтрует по `mainShifts`, SEO читает `getCurrentPrice()`, мёртвый файл убран, цены/даты единообразны через `SHIFT_META` + единое правило роста.

### 💰 Налоговый вычет — НЕЛЬЗЯ хардкодить одну цифру

Перед любым упоминанием — читай [`_notes/Библиотека знаний/Налоговый вычет — правила и данные.md`](_notes/Библиотека%20знаний/Налоговый%20вычет%20—%20правила%20и%20данные.md).

Суммы по сменам 2026 (ориентир; реальная цена — из `dynamicPrices.ts`, см. секцию выше):
> ⚠️ Суммы вычета ниже зависят от образовательной части путёвки и текущей (растущей) цены — сверять при изменении цен, не хардкодить.

- Смена 2.1 (7 дн., 48 000 ₽) → **~2 800 ₽**
- Смена 4 (10 дн., 74 900 ₽) → **~4 800 ₽**
- Смена 2.2 (8 дн., 75 000 ₽) → **~5 800 ₽**
- Смена 3 (13 дн., 89 400 ₽) → **~5 200 ₽**
- Смена 2 (14 дн., 99 000 ₽) → **~6 000 ₽**
- Смена 1 (10 дн., 85 900 ₽) → **~6 200 ₽** ← максимум

> Формула: 13% × (цена − 3 800 ₽/день). `EDU_RESID_PER_DAY=3800` в `shifts.ts` (НЕ путать с `BYT_PER_DAY=6100` — это для возвратов по ФЗ №2300-1).

❌ ЗАПРЕЩЕНО: писать "до 5 434 ₽" или "до 5 200 ₽" — устаревшие цифры.
✅ Если нужна одна цифра: "до 6 200 ₽ (максимум, Смена 1)".

---

## 📦 МЕДИА И ФАЙЛЫ — ЕДИНОЕ ХРАНИЛИЩЕ (правило с 2026-07-02)

**Все фото, видео и документы живут в ОДНОМ месте на сервере: `/var/www/aidacamp-media/`.**
nginx на prod И dev отдаёт `/images/` и `/videos/` напрямую оттуда (alias) — залил один раз → видно на обоих окружениях сразу. Никакой синхронизации.

| Что | Куда класть | URL |
|---|---|---|
| Картинки сайта | `/var/www/aidacamp-media/images/<подпапка>/` | `https://aidacamp.ru/images/...` (и dev) |
| Видео | `/var/www/aidacamp-media/videos/<подпапка>/` | `https://aidacamp.ru/videos/...` (и dev) |
| Документы (PDF, памятки) | `/var/www/aidacamp-media/docs/` | `https://dev.aidacamp.ru/media/docs/...` |
| Превью/дебаг-файлы | `/var/www/aidacamp-media/` | `https://dev.aidacamp.ru/media/<файл>` |
| Фото галереи смен | `/var/www/aidacamp-gallery/` (отдельно) | `/images/gallery/...` |

**Агентам — жёстко:**
- ❌ НЕ копировать медиа в `/var/www/aidacamp/current/client/` или `/var/www/aidacamp-dev/` — эти копии nginx больше НЕ читает (мёртвый груз).
- ❌ НЕ синкать медиа между dev и prod — хранилище одно.
- ❌ Бэкапы nginx-конфигов НИКОГДА не класть в `sites-enabled/` (include подключает `*.bak` → ломает nginx). Только `/etc/nginx/backups/`.
- ✅ Искать существующие фото/видео — СНАЧАЛА в `/var/www/aidacamp-media/` (и `aidacamp-gallery/` для галереи), а не в client-каталогах.
- ✅ Новые картинки в репо (`public/images/`) доезжают до хранилища сами: `deploy.sh` шаг 2c (`--ignore-existing`, серверная копия авторитетна).

---

## 🚫 ЗАПРЕЩЁННЫЕ ЗАВИСИМОСТИ

### `@astrojs/partytown` — НИКОГДА не возвращать

Не добавлять в `package.json`, не импортировать. Билд упадёт на `scripts/guard-no-partytown.sh`.

**Почему:** Partytown ломает Метрика-цели через Web Worker proxy → `TypeError` → 0 конверсий в Директ.

**Инцидент 16-18 апреля 2026:** ~60К₽ потерь за 2 дня, все кампании встали.

### `permanently=true` в Яндекс.Диск API

Только `permanently=false`. Причина: инцидент — 9000+ фото утеряны навсегда.

---

## СТАРТ КАЖДОЙ СЕССИИ

**Два отдельных репозитория — не путать:**

| Задача | Репо | Ветка |
|---|---|---|
| Сайт (вёрстка, компоненты, деплой) | `~/Aidacamp-cloude` | `dev` |
| MCP-сервер (инструменты, API) | `~/MCP` | `main` |

**Алгоритм:**
1. Понять о чём задача — сайт или MCP?
2. Перейти в нужную папку
3. `git branch` — убедиться что на правильной ветке
4. `git fetch origin && git pull --rebase origin dev`
5. **Агент**: запустить `./scripts/agent-start.sh "<задача>"` — получишь изолированный worktree

---

## Факты vs гипотезы (жёсткое правило)

1. **Факт** — только подтверждённое данными или документацией (ссылка обязательна)
2. **Гипотеза** — явно помечать: «гипотеза» или 🤔
3. **Не найдено** — честный ответ, лучше чем додумать
4. API-вопросы: сначала документация, потом тест. Не гадать.

---

## Используй только aidacamp-tools MCP

**НЕ используй:** Kapture, Desktop Commander, Chrome MCP, Claude in Chrome, удалённый MCP `5967f77d`.

**Все инструменты — через `aidacamp-tools`.** Полная документация: [`TOOLS.md`](TOOLS.md).

Известные кампании Директ: `708664426` (Поиск), `708698819` (РСЯ).

---

## Фоновые агенты (headless)

```bash
claude -p "задача" \
  --no-session-persistence \
  --no-chrome \
  --tools "Bash,Read,Write,Edit,Glob,Grep" \
  --max-turns 10 \
  --permission-mode bypassPermissions
```

**НЕ используй `--bare`** — отключает OAuth.

Полная инструкция: `~/MCP/AGENT_INSTRUCTIONS.md` → раздел 0.

### 🛫 Preflight перед конвейером и контракт фоновых задач (аудит 17.07.2026)

Три правила; механика вшита в хуки (`.claude/settings.json` → `scripts/hooks/`),
проза ниже — только про то, что греп поймать не может:

1. **Preflight.** Перед очередью headless-агентов или конвейером с внешним API —
   одна дешёвая проверка токена/биллинга: `bash scripts/preflight.sh claude [host]`
   или `REPLICATE_API_TOKEN=... bash scripts/preflight.sh replicate`. Хук
   `guard-preflight.py` заблокирует очередь `claude -p` и POST в Replicate без
   свежего маркера (30 мин). Для других API (OpenAI, Gemini, …) общего
   греп-паттерна «это старт конвейера» нет — там правило остаётся прозой: тот же
   дешёвый вызов ДО постройки. Инциденты: Replicate «insufficient credit» после
   ~4 ч работы (16.07), 9 агентов вхолостую на протухшем OAuth (15.07).
2. **Файловый контракт.** Фоновый субагент пишет полный результат в файл (путь —
   в брифе), финальным сообщением возвращает только резюме — зеркало правила
   nohup+лог для Bash. Хук `guard-agent-output.py` не выпустит фоновый Agent-запуск
   без пути в prompt. Инцидент: 5 из 9 ревьюеров умерли при рестартах без следа —
   0 Write (16.07).
3. **Реестр фоновых задач.** Каждый фоновый запуск попадает в
   `.claude/agent-tasks.jsonl` (хук `agent-task-registry.py`), при старте/рестарте
   сессии session-start.sh сверяет реестр с диском: файл результата есть и непуст —
   задача закрыта, нет — задача показывается как незакрытая. Статус выводится из
   артефакта, не из обещания. Инцидент: «Перезапущу» написано — перезапуска не было
   (17.07).

Самотесты стражей: `npm run check:hooks` (встроен в `npm run guard`).

---

## 📊 Отчёты — ТОЛЬКО в Reports Hub

🔴 **Любой отчёт, аудит, дашборд → публиковать через `/opt/reports-hub/publish.sh`.**

Дашборд: **https://dev.aidacamp.ru/reports-hub/**

```bash
scp -i ~/.ssh/aidacamp_prod /tmp/report.html root@159.194.223.55:/tmp/
ssh -i ~/.ssh/aidacamp_prod root@159.194.223.55 \
  "/opt/reports-hub/publish.sh /tmp/report.html 'Заголовок' 'Описание' <category> 'тег1,тег2'"
```

Категории: `seo` | `ux` | `analytics` | `ads` | `crm` | `other`

❌ Не класть в `/tmp/` (финально), корень репо, `dist/`, `public/`, серверные папки напрямую.

---

*v2 · 2026-06-03 · Очищен от дублей, добавлен индекс документов*
