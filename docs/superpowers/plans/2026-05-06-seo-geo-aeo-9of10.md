# SEO/GEO/AEO → 9/10 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Поднять оценки сайта aidacamp.ru с SEO 6/10 · GEO 7/10 · AEO 5/10 до SEO 9/10 · GEO 9/10 · AEO 9/10, устранив все критичные технические проблемы и закрыв пробелы в schema-разметке.

**Architecture:** Шесть последовательных фаз: технические правки (robots/sitemap/redirects) → тайтлы статей → schema completeness → HowTo+Speakable → GEO enrichment → финальная проверка. Каждая фаза независима и деплоится отдельно.

**Tech Stack:** Astro 6.x · @astrojs/sitemap · Tailwind CSS v4 · JSON-LD schema · Node 22 · `npm run build` · `./scripts/deploy.sh dev`

**Ветка:** `agent/seo-block1-technical` (активна). Все коммиты — только в неё.

---

## Текущее состояние (до плана)

| Показатель | Сейчас | Цель |
|---|---|---|
| SEO | 6/10 | 9/10 |
| GEO | 7/10 | 9/10 |
| AEO | 5/10 | 9/10 |
| robots.txt — /demo/ заблокирован | ❌ | ✅ |
| sitemap — /demo/* исключены | ❌ | ✅ |
| {totalCount} в meta /stati/ | ❌ | ✅ |
| /blog/ 403 → /stati/ | ❌ | ✅ |
| Дубль URL /reiting-ej vs -ey | ❌ | ✅ |
| Article title ≤65 симв. | 31/73 | 73/73 |
| Article schema | 72/73 | 73/73 |
| dateModified | 62/73 | 73/73 |
| FAQPage на статьях | 69/73 | 73/73 |
| HowTo schema | 2 статьи | 8+ статей |
| Speakable | ❌ | ✅ ключевые лендинги |
| LocalBusiness GeoCoordinates | ❌ | ✅ |
| 301 для старых Tilda-URL | ❌ | ✅ |

---

## Файловая карта

| Файл | Действие | Фаза |
|---|---|---|
| `public/robots.txt` | Modify | 1 |
| `astro.config.mjs` | Modify (sitemap filter) | 1 |
| `src/pages/stati/index.astro` | Modify (meta desc) | 1 |
| `src/middleware.ts` (или create) | Create (301 redirects) | 1 |
| `src/pages/stati/reiting-detskih-lagerey-podmoskove.astro` | Delete (301 to ej-version) | 1 |
| `src/pages/index.astro` | Modify (H1 дубль) | 1 |
| `src/pages/stati/*.astro` (42 файла) | Modify (title длина) | 2 |
| `src/pages/stati/zavisimost-u-detej.astro` | Modify (Article schema) | 3 |
| `src/pages/stati/*.astro` (11 файлов) | Modify (dateModified) | 3 |
| `src/pages/stati/*.astro` (3 файла) | Modify (FAQPage schema) | 3 |
| `src/pages/kompyuternyy-lager.astro` + др. лендинги | Modify (дубль schema) | 3 |
| `src/pages/stati/nalogovyj-vychet-*.astro` и др. | Modify (HowTo schema) | 4 |
| `src/components/SchemaOrg.astro` | Modify (Speakable, LocalBusiness geo) | 4 |

---

## ФАЗА 1: Технические критичные правки

**Цель фазы:** устранить все 🔴 CRITICAL из аудита. SEO: 6 → 7.5.

---

### Task 1: robots.txt — заблокировать /demo/, /dashboard/, /ask-test/

**Files:**
- Modify: `public/robots.txt`

- [ ] **Step 1: Открыть файл и добавить Disallow-блоки**

Текущий `public/robots.txt` содержит:
```
User-agent: *
Allow: /
Disallow: /admin/
Disallow: /api/
...
```

Добавить после строки `Disallow: /lanit-photos/`:
```
Disallow: /demo/
Disallow: /dashboard/
Disallow: /ask-test/
Disallow: /ai-studio/
```

Итоговый вид блока:
```
User-agent: *
Allow: /
Disallow: /admin/
Disallow: /api/
Disallow: /aidacamp-lanit-2026.html
Disallow: /lanit-v5.html
Disallow: /lanit-economics.html
Disallow: /lanit-photos/
Disallow: /demo/
Disallow: /dashboard/
Disallow: /ask-test/
Disallow: /ai-studio/
Clean-param: utm_source&utm_medium&...
Sitemap: https://aidacamp.ru/sitemap.xml
```

- [ ] **Step 2: Проверить итог**

```bash
grep "Disallow" public/robots.txt
```

Ожидаемый вывод — все 10 Disallow-строк, включая `/demo/`, `/dashboard/`, `/ask-test/`, `/ai-studio/`.

- [ ] **Step 3: Commit**

```bash
MASTER_AGENT=1 git add public/robots.txt
MASTER_AGENT=1 git commit -m "fix(seo): robots.txt — block /demo/ /dashboard/ /ask-test/ /ai-studio/"
```

---

### Task 2: sitemap — исключить /demo/, /dashboard/, /ask-test/, /blog/

**Files:**
- Modify: `astro.config.mjs` (строки с `filter: (page) =>`)

- [ ] **Step 1: Найти текущий filter в sitemap**

```bash
grep -n "filter:" astro.config.mjs
```

Текущий код (примерно строка 15):
```javascript
filter: (page) =>
  !page.includes('/admin/') &&
  !page.includes('/попробовать/') &&
  !page.includes('/%D0%BF%D0%BE%D0%BF%D1%80%D0%BE%D0%B1%D0%BE%D0%B2%D0%B0%D1%82%D1%8C/'),
```

- [ ] **Step 2: Заменить filter на полный список**

```javascript
filter: (page) =>
  !page.includes('/admin/') &&
  !page.includes('/demo/') &&
  !page.includes('/dashboard/') &&
  !page.includes('/ask-test/') &&
  !page.includes('/ask/') &&
  !page.includes('/ai-studio/') &&
  !page.includes('/blog/') &&
  !page.includes('/попробовать/') &&
  !page.includes('/%D0%BF%D0%BE%D0%BF%D1%80%D0%BE%D0%B1%D0%BE%D0%B2%D0%B0%D1%82%D1%8C/'),
```

- [ ] **Step 3: Собрать и проверить sitemap**

```bash
npm run build 2>&1 | tail -5
grep -c "demo" dist/client/sitemap-0.xml 2>/dev/null && echo "FAIL: demo pages still in sitemap" || echo "OK: no demo pages"
grep -c "dashboard" dist/client/sitemap-0.xml 2>/dev/null && echo "FAIL: dashboard in sitemap" || echo "OK"
```

Ожидаемый вывод: `OK: no demo pages` и `OK`.

- [ ] **Step 4: Commit**

```bash
MASTER_AGENT=1 git add astro.config.mjs
MASTER_AGENT=1 git commit -m "fix(seo): sitemap — exclude /demo/ /dashboard/ /ask-test/ /blog/ pages"
```

---

### Task 3: Исправить {totalCount} в meta description /stati/

**Files:**
- Modify: `src/pages/stati/index.astro`

- [ ] **Step 1: Найти баг**

```bash
grep -n "totalCount\|{total" src/pages/stati/index.astro | head -5
```

- [ ] **Step 2: Найти реальное количество статей и заменить шаблон**

```bash
ls src/pages/stati/*.astro | grep -v index | wc -l
```

Вывод будет ~72. Заменить в файле строку с description:

```astro
<!-- БЫЛО: -->
description="... {totalCount} материалов от команды АйДаКемп."

<!-- СТАЛО (число зафиксировать): -->
description="Честные статьи о подростках, гаджетах, выборе лагеря и IT-образовании. Без нотаций — от мамы к маме. 72 материала от команды АйДаКемп."
```

> Если count берётся динамически — заменить `{totalCount}` на `{clusters.flatMap(c => c.articles).length}` или посчитать при сборке через `Object.keys(Astro.glob()).length`. Если проще — захардкодить число.

- [ ] **Step 3: Проверить**

```bash
grep "totalCount\|{total" src/pages/stati/index.astro && echo "FAIL: шаблон остался" || echo "OK: нет шаблона"
```

- [ ] **Step 4: Commit**

```bash
MASTER_AGENT=1 git add src/pages/stati/index.astro
MASTER_AGENT=1 git commit -m "fix(seo): /stati/ meta description — fill totalCount template variable"
```

---

### Task 4: /blog/ — настроить 301 редирект на /stati/

**Files:**
- Modify или Create: `src/middleware.ts`

- [ ] **Step 1: Проверить существующий middleware**

```bash
ls src/middleware.ts 2>/dev/null && echo "exists" || echo "need to create"
cat src/middleware.ts 2>/dev/null | head -20
```

- [ ] **Step 2: Добавить редирект /blog/ → /stati/**

Если middleware уже существует — добавить в начало обработчика. Если нет — создать:

```typescript
import { defineMiddleware } from 'astro:middleware';

export const onRequest = defineMiddleware(async (context, next) => {
  const url = new URL(context.request.url);
  const path = url.pathname;

  // Редирект /blog/* → /stati/*
  if (path === '/blog/' || path === '/blog') {
    return context.redirect('/stati/', 301);
  }
  if (path.startsWith('/blog/')) {
    return context.redirect(path.replace('/blog/', '/stati/'), 301);
  }

  return next();
});
```

- [ ] **Step 3: Проверить локально**

```bash
npm run build 2>&1 | grep -E "error|Error" | head -5
```

- [ ] **Step 4: Деплой dev и ручная проверка**

```bash
./scripts/deploy.sh dev
```

Затем:
```bash
curl -s -o /dev/null -w "%{http_code} %{redirect_url}" "https://dev.aidacamp.ru/blog/"
```

Ожидаемый вывод: `301 https://dev.aidacamp.ru/stati/`

- [ ] **Step 5: Commit**

```bash
MASTER_AGENT=1 git add src/middleware.ts
MASTER_AGENT=1 git commit -m "fix(seo): /blog/ → /stati/ 301 redirect via middleware"
```

---

### Task 5: Устранить дубль URL /reiting-detskih-lagerey-podmoskove/

**Files:**
- Modify: `src/pages/stati/reiting-detskih-lagerey-podmoskove.astro` (перевести в 301)
- Или добавить в middleware редирект

> Канонический URL — **с -ej** (`reiting-detskih-lagerej-podmoskove`), он старше.

- [ ] **Step 1: Преобразовать дублирующий файл в редирект**

Заменить весь контент `src/pages/stati/reiting-detskih-lagerey-podmoskove.astro` на:

```astro
---
return Astro.redirect('/stati/reiting-detskih-lagerej-podmoskove/', 301);
---
```

- [ ] **Step 2: Проверить**

```bash
npm run build 2>&1 | grep -E "error" | head -3
```

- [ ] **Step 3: Деплой dev и проверка редиректа**

```bash
./scripts/deploy.sh dev
curl -s -o /dev/null -w "%{http_code} %{redirect_url}" "https://dev.aidacamp.ru/stati/reiting-detskih-lagerey-podmoskove/"
```

Ожидаемый вывод: `301 https://dev.aidacamp.ru/stati/reiting-detskih-lagerej-podmoskove/`

- [ ] **Step 4: Commit**

```bash
MASTER_AGENT=1 git add src/pages/stati/reiting-detskih-lagerey-podmoskove.astro
MASTER_AGENT=1 git commit -m "fix(seo): 301 redirect /reiting-ey → /reiting-ej (canonical URL)"
```

---

### Task 6: 301-редиректы для старых Tilda-URL (404 из Вебмастера)

**Files:**
- Modify: `src/middleware.ts`

- [ ] **Step 1: Добавить блок со старыми URL в middleware**

В `src/middleware.ts` в функцию `onRequest` добавить после блока `/blog/`:

```typescript
  // Редиректы старых Tilda-URL → актуальные страницы
  const TILDA_REDIRECTS: Record<string, string> = {
    '/about': '/',
    '/catalog': '/ceny/',
    '/subrent': '/',
    '/cookies': '/privacy/',
    '/pine-camp-v2': '/',
    '/svedeniya-ob-obrazovatelnoj-organizacii': '/requisites/',
    '/programmirovanei-dlya-detey-7-let': '/lager-7-let/',
    '/programmirovanei-dlya-detey-8-let': '/lager-8-let/',
    '/programmirovanei-dlya-detey-9-let': '/lager-9-let/',
    '/programmirovanei-dlya-detey-10-let': '/lager-10-let/',
    '/programmirovanei-dlya-detey-11-let': '/lager-11-let/',
    '/programmirovanei-dlya-detey-12-let': '/lager-12-let/',
    '/programmirovanei-dlya-detey-13-let': '/lager-12-let/',
    '/programmirovanei-dlya-detey-14-let': '/lager-14-let/',
    '/programmirovanie-dlya-detei-s-nulya': '/stati/programmirovanie-dlya-detej-s-nulya/',
    '/programmirovanie-dlya-shkolnikov': '/kompyuternyy-lager/',
    '/programmirovanie-dlya-vzroslyh': '/',
  };

  const cleanPath = path.endsWith('/') ? path.slice(0, -1) : path;
  if (TILDA_REDIRECTS[cleanPath]) {
    return context.redirect(TILDA_REDIRECTS[cleanPath], 301);
  }
```

- [ ] **Step 2: Проверить сборку**

```bash
npm run build 2>&1 | grep -E "error|Error" | head -5
echo "Build OK"
```

- [ ] **Step 3: Деплой dev, точечная проверка**

```bash
./scripts/deploy.sh dev
curl -s -o /dev/null -w "%{http_code} %{redirect_url}" "https://dev.aidacamp.ru/about"
curl -s -o /dev/null -w "%{http_code} %{redirect_url}" "https://dev.aidacamp.ru/programmirovanei-dlya-detey-10-let"
```

Ожидаемый вывод обеих строк: `301 https://dev.aidacamp.ru/...`

- [ ] **Step 4: Commit**

```bash
MASTER_AGENT=1 git add src/middleware.ts
MASTER_AGENT=1 git commit -m "fix(seo): 301 redirects for ~15 old Tilda URLs (404 cleanup)"
```

---

### Task 7: Исправить дубль H1 на главной странице

**Files:**
- Modify: `src/pages/index.astro` (или компонент Hero)

- [ ] **Step 1: Найти второй H1**

```bash
grep -n "<h1" src/pages/index.astro src/components/Hero*.astro 2>/dev/null | head -10
```

- [ ] **Step 2: Определить природу дубля**

Второй H1 содержит dev-комментарий: `"— допустимо (John Mueller, 2020): не ранжирующий штраф"`. Это либо закомментированный H1 оставшийся в HTML как скрытый тег, либо SEO-honey H1 с `class="sr-only"`.

Если это `class="sr-only"` H1 для скринридеров — **не трогать**, он допустим. Если это случайный дублирующий тег — удалить.

```bash
grep -A2 -B2 "John Mueller\|не ранжирующий" src/pages/index.astro 2>/dev/null | head -20
```

- [ ] **Step 3: Если нужно удалить — убрать артефакт**

Удалить строки вида:
```html
<!-- допустимо (John Mueller, 2020): не ранжирующий -->
<h1 class="hidden">...</h1>
```

или преобразовать в:
```html
<!-- Второй H1 удалён: дублировал видимый H1. Commit: YYYY-MM-DD -->
```

- [ ] **Step 4: Проверить**

```bash
npm run build 2>&1 | grep -E "error" | head -3
ssh -i ~/.ssh/aidacamp_prod root@159.194.223.55 "cd /opt/browser-agent && node scrape.js 'https://dev.aidacamp.ru/' html 2>/dev/null" | python3 -c "
import sys, re
html = sys.stdin.read()
h1s = re.findall(r'<h1[^>]*>(.*?)</h1>', html, re.DOTALL|re.IGNORECASE)
print(f'H1 count: {len(h1s)}')
for h in h1s:
    print(' -', re.sub(r'<[^>]+>','',h).strip()[:80])
"
```

Ожидаемый вывод: `H1 count: 1`

- [ ] **Step 5: Commit**

```bash
MASTER_AGENT=1 git add src/pages/index.astro
MASTER_AGENT=1 git commit -m "fix(seo): remove duplicate H1 artifact from homepage DOM"
```

---

## ФАЗА 2: Оптимизация тайтлов статей

**Цель фазы:** все 73 статьи с title ≤65 символов. SEO: 7.5 → 8.5.

---

### Task 8: Сократить title у 42 статей с >65 символами

**Files:**
- Modify: 42 файла в `src/pages/stati/`

> **Правило сокращения:** убрать ` | АйДаКемп` в конце (занимает 11 символов) где нужно; сократить формулировку; сохранить главный ключевой запрос.

- [ ] **Step 1: Сгенерировать список файлов и текущих тайтлов**

```bash
python3 -c "
import os, re
stati = 'src/pages/stati'
items = []
for f in sorted(os.listdir(stati)):
    if not f.endswith('.astro') or f == 'index.astro': continue
    content = open(os.path.join(stati, f)).read()
    m = re.search(r'title=[\"\'](.*?)[\"\']\s*\n', content)
    if m:
        t = m.group(1)
        if len(t) > 65:
            items.append((len(t), f, t))
for l,f,t in sorted(items, reverse=True):
    print(f'{l:3d}  {f}')
    print(f'       {t[:80]}')
" 2>/dev/null
```

- [ ] **Step 2: Применить сокращения ко всем 42 файлам**

Для каждого файла из списка выше — открыть и заменить title. Примеры корректных сокращений:

| Было | Стало | Было симв. | Стало симв. |
|------|-------|-----------|-------------|
| `Как выбрать IT-лагерь для ребёнка — 7 вопросов которые стоит задать \| АйДаКемп` | `Как выбрать IT-лагерь — 7 вопросов перед бронированием` | 78 | 55 |
| `Зависимость от компьютерных игр у детей — 4 шага как вернуть ребёнка 2026 \| АйДаКемп` | `Зависимость от компьютерных игр у детей — 4 шага` | 73 | 50 |
| `Детский лагерь без телефонов — как дети сами выбирают реальную жизнь \| АйДаКемп` | `Детский лагерь без телефонов — как дети выбирают реальную жизнь` | 79 | 65 |
| `Подросток ничего не хочет — почему это апатия, а не лень, и 5 шагов...` | `Подросток ничего не хочет — апатия vs лень: 5 шагов` | 79 | 53 |
| `Налоговый вычет за детский лагерь 2026 — пошаговая инструкция \| АйДаКемп` | `Налоговый вычет за детский лагерь 2026 — инструкция` | 72 | 52 |

**Общие правила:**
- Убрать ` | АйДаКемп` — Яндекс и так показывает доменное имя
- Убрать год `2026` если он не ключевой
- Убрать "которые стоит задать", "как вернуть ребёнка", другие придаточные
- Оставить главный запрос и цифру ("4 шага", "7 советов")

- [ ] **Step 3: Массовая проверка после правок**

```bash
python3 -c "
import os, re
stati = 'src/pages/stati'
long = []
for f in os.listdir(stati):
    if not f.endswith('.astro') or f == 'index.astro': continue
    content = open(os.path.join(stati, f)).read()
    m = re.search(r'title=[\"\'](.*?)[\"\']\s*\n', content)
    if m and len(m.group(1)) > 65:
        long.append((len(m.group(1)), f))
if long:
    print('FAIL: Still have long titles:')
    for l,f in sorted(long): print(f'  {l}: {f}')
else:
    print('OK: All titles ≤65 chars')
"
```

Ожидаемый вывод: `OK: All titles ≤65 chars`

- [ ] **Step 4: Сборка**

```bash
npm run build 2>&1 | grep -E "error|Error" | head -5
```

- [ ] **Step 5: Commit**

```bash
MASTER_AGENT=1 git add src/pages/stati/
MASTER_AGENT=1 git commit -m "fix(seo): trim 42 article titles to ≤65 chars"
```

---

## ФАЗА 3: Schema completeness

**Цель фазы:** все 73 статьи с Article + FAQPage schema. Дубли на лендингах исправлены. AEO: 5 → 7, GEO: 7 → 8.

---

### Task 9: Добавить Article schema в zavisimost-u-detej.astro

**Files:**
- Modify: `src/pages/stati/zavisimost-u-detej.astro`

- [ ] **Step 1: Проверить текущее содержимое**

```bash
grep -n "application/ld\|@type\|Article\|schema" src/pages/stati/zavisimost-u-detej.astro | head -10
```

- [ ] **Step 2: Добавить Article + FAQPage schema по образцу других статей**

Открыть `src/pages/stati/kak-vybrat-it-lager.astro` и скопировать паттерн `<script type="application/ld+json">` с заменой:
- `headline` — по тайтлу статьи
- `datePublished` — реальная дата (найти в git log)
- `dateModified` — сегодняшняя дата: `2026-05-06`
- `url` — `https://aidacamp.ru/stati/zavisimost-u-detej/`

Пример для вставки в `<Fragment slot="head">`:

```astro
<script type="application/ld+json" set:html={JSON.stringify([
  {
    "@context": "https://schema.org",
    "@type": "Article",
    "headline": "Зависимость у детей от гаджетов: как распознать и помочь",
    "author": {"@type": "Person", "name": "Дарья Афанасьева", "url": "https://aidacamp.ru/"},
    "publisher": {"@type": "Organization", "name": "АйДаКемп", "url": "https://aidacamp.ru", "@id": "https://aidacamp.ru/#organization"},
    "datePublished": "2026-04-01",
    "dateModified": "2026-05-06",
    "url": "https://aidacamp.ru/stati/zavisimost-u-detej/",
    "image": "https://aidacamp.ru/images/hero/o-lagere.avif",
    "inLanguage": "ru"
  },
  ...(faqItems.length ? [{
    "@context": "https://schema.org",
    "@type": "FAQPage",
    "mainEntity": faqItems.map(item => ({
      "@type": "Question",
      "name": item.question,
      "acceptedAnswer": {"@type": "Answer", "text": item.answer}
    }))
  }] : [])
])} />
```

- [ ] **Step 3: Проверить**

```bash
grep '"@type": "Article"' src/pages/stati/zavisimost-u-detej.astro && echo "OK: Article schema found" || echo "FAIL"
npm run build 2>&1 | grep -E "error" | head -3
```

- [ ] **Step 4: Commit**

```bash
MASTER_AGENT=1 git add src/pages/stati/zavisimost-u-detej.astro
MASTER_AGENT=1 git commit -m "fix(schema): add Article+FAQPage schema to zavisimost-u-detej"
```

---

### Task 10: Добавить dateModified в 11 статей, где его нет

**Files:**
- Modify: следующие файлы в `src/pages/stati/`:
  - `chem-zanyat-rebyonka-letom.astro`
  - `detskiy-lager-bez-telefonov.astro`
  - `kuda-det-rebenka-letom.astro`
  - `kak-vybrat-it-lager.astro`
  - `kak-vybrat-lager.astro`
  - `nalogovyj-vychet-za-lager-poshagovaya-instrukciya.astro`
  - `pervyj-raz-v-lagere.astro`
  - `rebenok-ne-hochet-v-lager.astro`
  - `programmirovaniye-s-kakogo-vozrasta.astro`
  - `zavisimost-ot-telefona-u-podrostkov.astro`
  - `reiting-detskih-lagerey-podmoskove.astro` (уже редирект после Task 5)

- [ ] **Step 1: В каждом из 10 активных файлов найти `"datePublished"` и добавить `"dateModified"` следующей строкой**

В каждом файле найти в JSON схемы:
```javascript
"datePublished": "2026-XX-XX",
```

Добавить после:
```javascript
"dateModified": "2026-05-06",
```

- [ ] **Step 2: Массовая проверка**

```bash
python3 -c "
import os, re
files = [
  'chem-zanyat-rebyonka-letom.astro', 'detskiy-lager-bez-telefonov.astro',
  'kuda-det-rebenka-letom.astro', 'kak-vybrat-it-lager.astro',
  'kak-vybrat-lager.astro', 'nalogovyj-vychet-za-lager-poshagovaya-instrukciya.astro',
  'pervyj-raz-v-lagere.astro', 'rebenok-ne-hochet-v-lager.astro',
  'programmirovaniye-s-kakogo-vozrasta.astro', 'zavisimost-ot-telefona-u-podrostkov.astro'
]
for f in files:
    path = f'src/pages/stati/{f}'
    content = open(path).read()
    ok = 'dateModified' in content
    print(f'{'OK' if ok else 'FAIL'}: {f}')
"
```

Ожидаемый вывод: 10 строк `OK`.

- [ ] **Step 3: Commit**

```bash
MASTER_AGENT=1 git add src/pages/stati/
MASTER_AGENT=1 git commit -m "fix(schema): add dateModified to 10 articles missing it"
```

---

### Task 11: FAQPage schema — добавить в 3 статьи без FAQ

**Files:**
- Modify: 3 статьи, которые не имеют ни `faqItems` ни `FAQPage`

- [ ] **Step 1: Найти эти 3 статьи**

```bash
python3 -c "
import os
stati = 'src/pages/stati'
for f in sorted(os.listdir(stati)):
    if not f.endswith('.astro') or f == 'index.astro': continue
    content = open(os.path.join(stati, f)).read()
    if 'faqItems' not in content and 'FAQPage' not in content:
        print(f)
"
```

- [ ] **Step 2: Для каждой из 3 статей — добавить faqItems с 2-3 вопросами по теме статьи**

Пример для гипотетической статьи `istorii-iz-lagerya.astro` (добавить в начало frontmatter):

```javascript
const faqItems = [
  {
    question: "Что происходит в детском лагере каждый день?",
    answer: "Стандартный день начинается с зарядки, затем идут занятия в мини-группах (2-3 блока по 45 минут), обед, тихий час, спортивные активности и вечернее мероприятие. В АйДаКемп один-два блока — IT-программа, остальное — спорт, бассейн, творчество."
  },
  {
    question: "Чем отличаются истории детей из IT-лагеря от обычного?",
    answer: "Главное отличие — дети возвращаются с конкретным результатом: игрой, сайтом или ботом, которые сделали сами. Это создаёт другой тип историй — не про вожатых и костёр, а про то, как придумали и реализовали свою идею."
  }
];
```

Добавить FAQPage schema в `<Fragment slot="head">` по паттерну из Task 9.

- [ ] **Step 3: Проверить**

```bash
python3 -c "
import os
stati = 'src/pages/stati'
for f in sorted(os.listdir(stati)):
    if not f.endswith('.astro') or f == 'index.astro': continue
    content = open(os.path.join(stati, f)).read()
    if 'faqItems' not in content and 'FAQPage' not in content:
        print(f'FAIL: {f} still missing FAQ')
print('Check complete')
"
```

Ожидаемый вывод: только строка `Check complete`.

- [ ] **Step 4: Commit**

```bash
MASTER_AGENT=1 git add src/pages/stati/
MASTER_AGENT=1 git commit -m "fix(schema): add FAQPage schema to 3 articles missing it"
```

---

### Task 12: Исправить дублирующиеся schemas на лендингах

**Files:**
- Modify: `src/pages/kompyuternyy-lager.astro` (и другие лендинги с 2× BreadcrumbList, 2× FAQPage)

- [ ] **Step 1: Найти все лендинги с дублями**

```bash
for f in src/pages/*.astro; do
  count=$(grep -c '"@type": "FAQPage"' "$f" 2>/dev/null || echo 0)
  [ "$count" -gt 1 ] && echo "$count FAQPage: $f"
  count=$(grep -c '"@type": "BreadcrumbList"' "$f" 2>/dev/null || echo 0)
  [ "$count" -gt 1 ] && echo "$count BreadcrumbList: $f"
done
```

- [ ] **Step 2: В каждом найденном файле оставить только один экземпляр каждого типа**

Найти в файле два блока `FAQPage` — удалить один (оставить тот, у которого больше вопросов). Аналогично для `BreadcrumbList`.

- [ ] **Step 3: Проверить что в каждом файле максимум 1× FAQPage и 1× BreadcrumbList**

```bash
for f in src/pages/*.astro; do
  count=$(grep -c '"@type": "FAQPage"' "$f" 2>/dev/null || echo 0)
  [ "$count" -gt 1 ] && echo "FAIL: $count FAQPage in $f"
done && echo "OK: no duplicate FAQPage"
```

- [ ] **Step 4: Commit**

```bash
MASTER_AGENT=1 git add src/pages/
MASTER_AGENT=1 git commit -m "fix(schema): remove duplicate FAQPage and BreadcrumbList schemas on landing pages"
```

---

## ФАЗА 4: HowTo, Speakable, LocalBusiness geo

**Цель фазы:** AEO: 7 → 9, GEO: 8 → 9.

---

### Task 13: HowTo schema на 6 инструкционных статьях

**Files:**
- Modify: следующие статьи:
  - `src/pages/stati/nalogovyj-vychet-za-lager-poshagovaya-instrukciya.astro`
  - `src/pages/stati/dokumenty-dlya-rebenka-v-lager.astro`
  - `src/pages/stati/kak-podgotovit-rebenka-k-lageru.astro`
  - `src/pages/stati/kak-dobratsya-do-lagera.astro`
  - `src/pages/stati/spravki-dlya-lagera.astro`
  - `src/pages/stati/kak-izbavitsya-ot-zavisimosti-ot-igr.astro`

- [ ] **Step 1: Добавить `howToSteps` массив в каждую статью**

Пример для `nalogovyj-vychet-za-lager-poshagovaya-instrukciya.astro`:

```javascript
const howToSteps = [
  { name: "Убедитесь, что лагерь имеет образовательную лицензию", text: "Попросите у лагеря копию лицензии Минобрнауки или проверьте её на сайте Рособрнадзора." },
  { name: "Получите справку об оплате образовательных услуг", text: "После окончания смены запросите у лагеря справку об оплате. Это главный документ для вычета." },
  { name: "Заполните декларацию 3-НДФЛ", text: "Через личный кабинет nalog.ru → Жизненные ситуации → Подать 3-НДФЛ. Укажите расходы на образование ребёнка." },
  { name: "Приложите документы: справка об оплате + договор с лагерем + свидетельство о рождении ребёнка", text: "Все документы загружаются сканами в личном кабинете ФНС." },
  { name: "Дождитесь возврата на счёт", text: "Срок — до 4 месяцев (1 месяц проверки + 3 месяца на выплату). Деньги придут на карту, указанную в декларации." }
];
```

Добавить HowTo schema в `<Fragment slot="head">` в уже существующий JSON-массив:

```javascript
{
  "@context": "https://schema.org",
  "@type": "HowTo",
  "name": "Как получить налоговый вычет за детский лагерь",
  "description": "Пошаговая инструкция по оформлению социального налогового вычета за расходы на образовательный лагерь.",
  "totalTime": "PT30M",
  "step": howToSteps.map((s, i) => ({
    "@type": "HowToStep",
    "position": i + 1,
    "name": s.name,
    "text": s.text
  }))
}
```

- [ ] **Step 2: Повторить для остальных 5 статей с соответствующими шагами**

- [ ] **Step 3: Проверить количество HowTo**

```bash
grep -rl '"@type": "HowTo"' src/pages/stati/ | wc -l
```

Ожидаемый вывод: `8` (2 уже было + 6 новых).

- [ ] **Step 4: Сборка**

```bash
npm run build 2>&1 | grep -E "error" | head -5
```

- [ ] **Step 5: Commit**

```bash
MASTER_AGENT=1 git add src/pages/stati/
MASTER_AGENT=1 git commit -m "feat(schema): add HowTo schema to 6 instructional articles"
```

---

### Task 14: LocalBusiness с GeoCoordinates в SchemaOrg.astro

**Files:**
- Modify: `src/components/SchemaOrg.astro`

- [ ] **Step 1: Найти, где в SchemaOrg.astro находится Organization или LocalBusiness**

```bash
grep -n "LocalBusiness\|GeoCoordinates\|latitude\|address" src/components/SchemaOrg.astro | head -10
```

- [ ] **Step 2: Добавить или расширить LocalBusiness schema**

Добавить в @graph на главной (или на отдельных лендингах) объект LocalBusiness:

```javascript
{
  "@context": "https://schema.org",
  "@type": "LocalBusiness",
  "@id": "https://aidacamp.ru/#localbusiness",
  "name": "АйДаКемп",
  "url": "https://aidacamp.ru",
  "telephone": "+7 (495) 128-44-29",
  "email": "hello@codims.ru",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "Киевское шоссе А-130, 66 км от МКАД",
    "addressLocality": "Наро-Фоминский район",
    "addressRegion": "Московская область",
    "postalCode": "143350",
    "addressCountry": "RU"
  },
  "geo": {
    "@type": "GeoCoordinates",
    "latitude": 55.3500,
    "longitude": 36.7200
  },
  "openingHours": "Ju-Au",
  "priceRange": "₽₽₽",
  "hasMap": "https://yandex.ru/maps/-/CDrXxx",
  "sameAs": [
    "https://vk.com/aidacamp",
    "https://t.me/aidacamp_ru"
  ]
}
```

> **Уточнить координаты** через Яндекс.Карты перед добавлением — взять реальные.

- [ ] **Step 3: Проверить сборку**

```bash
npm run build 2>&1 | grep -E "error" | head -3
```

- [ ] **Step 4: Верифицировать через dev-сайт**

```bash
./scripts/deploy.sh dev
ssh -i ~/.ssh/aidacamp_prod root@159.194.223.55 "cd /opt/browser-agent && node scrape.js 'https://dev.aidacamp.ru/' html 2>/dev/null" | python3 -c "
import sys, re, json
html = sys.stdin.read()
schemas = re.findall(r'application/ld\+json[^>]*>(.*?)</script>', html, re.DOTALL)
for s in schemas:
    try:
        d = json.loads(s)
        items = d if isinstance(d, list) else d.get('@graph', [d])
        for item in (items if isinstance(items, list) else [items]):
            if item.get('@type') == 'LocalBusiness':
                print('OK: LocalBusiness found')
                print('  geo:', item.get('geo'))
    except: pass
"
```

Ожидаемый вывод: `OK: LocalBusiness found` с geo-координатами.

- [ ] **Step 5: Commit**

```bash
MASTER_AGENT=1 git add src/components/SchemaOrg.astro
MASTER_AGENT=1 git commit -m "feat(schema): add LocalBusiness with GeoCoordinates to SchemaOrg"
```

---

### Task 15: Speakable markup на ключевых лендингах

**Files:**
- Modify: `src/pages/index.astro`, `src/pages/it-camp.astro`, `src/pages/kompyuternyy-lager.astro`

- [ ] **Step 1: Добавить SpeakableSpecification в Article/WebPage schema**

В `<script type="application/ld+json">` на главной добавить в WebPage или отдельным объектом:

```javascript
{
  "@context": "https://schema.org",
  "@type": "WebPage",
  "@id": "https://aidacamp.ru/#webpage",
  "url": "https://aidacamp.ru/",
  "name": "Детский IT-лагерь в Подмосковье 2026",
  "speakable": {
    "@type": "SpeakableSpecification",
    "cssSelector": ["h1", ".faq-answer", "[data-speakable]"]
  }
}
```

- [ ] **Step 2: Добавить `data-speakable` атрибут к ключевым ответам на FAQ на лендингах**

В компоненте FAQ или в HTML лендинга на элементах с ответами добавить:

```astro
<div class="faq-answer" data-speakable>
  {item.answer}
</div>
```

- [ ] **Step 3: Проверить сборку**

```bash
npm run build 2>&1 | grep -E "error" | head -3
```

- [ ] **Step 4: Commit**

```bash
MASTER_AGENT=1 git add src/pages/index.astro src/pages/it-camp.astro src/pages/kompyuternyy-lager.astro
MASTER_AGENT=1 git commit -m "feat(aeo): add Speakable schema to homepage and key landing pages"
```

---

## ФАЗА 5: GEO — обогащение контента

**Цель фазы:** GEO 8 → 9. Сигналы для ИИ-движков: авторство, цитирование, оригинальные данные.

---

### Task 16: Унифицировать и обогатить author entity в Article schemas

**Files:**
- Modify: `src/pages/stati/*.astro` (выборочно — статьи, где author неполный)

- [ ] **Step 1: Проверить текущее состояние author entity**

```bash
grep -h '"author"' src/pages/stati/*.astro | sort | uniq -c | sort -rn | head -10
```

- [ ] **Step 2: Стандартизировать шаблон author во всех статьях**

Найти статьи где `author` содержит только `name` без `@id` и `url`. Добавить:

```javascript
"author": {
  "@type": "Person",
  "@id": "https://aidacamp.ru/#founder",
  "name": "Дарья Афанасьева",
  "url": "https://aidacamp.ru/",
  "jobTitle": "Основатель АйДаКемп",
  "sameAs": ["https://vk.com/aidacamp", "https://t.me/aidacamp_ru"]
},
```

- [ ] **Step 3: Добавить `@id` для Organization publisher**

В тех же статьях найти `"publisher"` и добавить `"@id"`:

```javascript
"publisher": {
  "@type": "Organization",
  "name": "АйДаКемп",
  "url": "https://aidacamp.ru",
  "@id": "https://aidacamp.ru/#organization"
},
```

- [ ] **Step 4: Проверить стандартизацию**

```bash
grep -c '"@id": "https://aidacamp.ru/#founder"' src/pages/stati/*.astro | grep -v ":0" | wc -l
echo "articles with full author @id"
```

- [ ] **Step 5: Commit**

```bash
MASTER_AGENT=1 git add src/pages/stati/
MASTER_AGENT=1 git commit -m "feat(geo): enrich author entity with @id and sameAs in all Article schemas"
```

---

### Task 17: Добавить внешние цитаты в топ-10 статей

**Files:**
- Modify: 10 наиболее трафиковых статей (по Метрике)

> Это КОНТЕНТНАЯ правка. Цель — дать ИИ-движкам основание цитировать страницу как авторитетную. Добавляем 1-2 ссылки на официальные источники (ВОЗ, Минобрнауки, pediatr.ru, rospotrebnadzor.ru).

- [ ] **Step 1: Определить топ-10 статей**

Из отчёта Метрики топ-органика апреля: `zavisimost-ot-kompyuternyh-igr`, `kak-vybrat-it-lager`, `lager-maycraft`, `nalogovyj-vychet-za-lager`, и другие высокотрафиковые.

- [ ] **Step 2: В каждой статье добавить блок "Источники" в конец контента**

Пример для статьи о зависимости от компьютерных игр:

```html
<section class="sources mt-8 pt-6 border-t border-gray-200">
  <h3 class="text-sm font-semibold text-gray-500 mb-3">Источники</h3>
  <ul class="text-sm text-gray-600 space-y-1">
    <li><a href="https://www.who.int/news-room/fact-sheets/detail/gaming-disorder" rel="nofollow noreferrer" target="_blank">ВОЗ: Gaming disorder (ICD-11, 6C51)</a></li>
    <li><a href="https://rospotrebnadzor.ru/about/info/news/..." rel="nofollow noreferrer" target="_blank">Роспотребнадзор: нормы экранного времени</a></li>
  </ul>
</section>
```

- [ ] **Step 3: Добавить `citation` в Article schema соответствующих статей**

```javascript
"citation": [
  {
    "@type": "CreativeWork",
    "name": "WHO: Gaming disorder",
    "url": "https://www.who.int/news-room/fact-sheets/detail/gaming-disorder"
  }
]
```

- [ ] **Step 4: Commit**

```bash
MASTER_AGENT=1 git add src/pages/stati/
MASTER_AGENT=1 git commit -m "feat(geo): add external citations to top-10 articles for E-E-A-T"
```

---

## ФАЗА 6: Analytics cleanup + финальная проверка

**Цель фазы:** чистые данные Clarity. Финальная сборка и деплой. Все оценки ≥ 9/10.

---

### Task 18: Исключить staging из Clarity

**Files:**
- Выяснить где подключается Clarity (скорее всего `src/layouts/Base.astro` или `src/components/Analytics.astro`)

- [ ] **Step 1: Найти код Clarity**

```bash
grep -rn "clarity\|CLARITY\|ms.clarity" src/ --include="*.astro" | grep -v "node_modules" | head -5
```

- [ ] **Step 2: Обернуть Clarity в проверку production-домена**

```astro
{import.meta.env.PROD && Astro.url.hostname === 'aidacamp.ru' && (
  <script>
    // Clarity code here
  </script>
)}
```

Если Clarity подключается через environment variable:

```astro
{import.meta.env.CLARITY_ID && import.meta.env.PROD && (
  <!-- Clarity script -->
)}
```

- [ ] **Step 3: Убедиться что dev.aidacamp.ru не отправляет данные в Clarity**

```bash
./scripts/deploy.sh dev
ssh -i ~/.ssh/aidacamp_prod root@159.194.223.55 "cd /opt/browser-agent && node scrape.js 'https://dev.aidacamp.ru/' html 2>/dev/null" | grep -i "clarity" | head -5
```

Ожидаемый вывод: пустой (Clarity не в HTML dev-сайта).

- [ ] **Step 4: Commit**

```bash
MASTER_AGENT=1 git add src/
MASTER_AGENT=1 git commit -m "fix(analytics): disable Clarity on non-production domains"
```

---

### Task 19: Финальная сборка и проверка всех исправлений

- [ ] **Step 1: Запустить полный билд**

```bash
cd ~/Aidacamp-cloude && npm run build 2>&1 | tail -10
```

Ожидаемый вывод: `✓ Completed` без ошибок.

- [ ] **Step 2: Проверить sitemap — нет /demo/ страниц**

```bash
grep -c "demo\|dashboard\|ask-test" dist/client/sitemap-0.xml 2>/dev/null && echo "FAIL" || echo "OK: clean sitemap"
```

- [ ] **Step 3: Проверить robots.txt**

```bash
cat public/robots.txt | grep "Disallow"
```

Ожидаемый вывод: минимум 10 Disallow-строк, включая `/demo/`.

- [ ] **Step 4: Проверить отсутствие {totalCount} в сборке**

```bash
grep -r "totalCount" dist/client/ 2>/dev/null && echo "FAIL: template not replaced" || echo "OK"
```

- [ ] **Step 5: Проверить все тайтлы ≤65 символов**

```bash
python3 -c "
import os, re
stati = 'src/pages/stati'
long = [f for f in os.listdir(stati) if f.endswith('.astro') and f != 'index.astro'
        and len(re.search(r'title=[\"\'](.*?)[\"\']\s*\n', open(os.path.join(stati,f)).read() or '').group(1) if re.search(r'title=[\"\'](.*?)[\"\']\s*\n', open(os.path.join(stati,f)).read()) else '') > 65]
print('FAIL:', long) if long else print('OK: all titles ≤65')
"
```

- [ ] **Step 6: Деплой на dev и ручная проверка через браузер**

```bash
./scripts/deploy.sh dev
```

Проверить вручную:
- https://dev.aidacamp.ru/stati/ — meta description без `{totalCount}`
- https://dev.aidacamp.ru/blog/ — 301 → /stati/
- https://dev.aidacamp.ru/about → 301 → /

- [ ] **Step 7: Создать PR в dev**

```bash
git push origin agent/seo-block1-technical
gh pr create --base dev \
  --title "feat(seo): SEO/GEO/AEO → 9/10 — фазы 1-6 полностью" \
  --body "## Что сделано

### Фаза 1: Технические правки
- robots.txt: заблокированы /demo/, /dashboard/, /ask-test/
- sitemap: исключены 30+ технических страниц  
- Исправлен баг {totalCount} в meta /stati/
- 301 для /blog/ → /stati/
- 301 для дубля /reiting-ey → /reiting-ej
- 301 для ~15 старых Tilda-URL
- Убран дубль H1 на главной

### Фаза 2: Title оптимизация
- 42 статьи: title сокращены до ≤65 символов

### Фаза 3: Schema completeness
- Article schema добавлена в 1 пропущенную статью
- dateModified добавлен в 10 статей
- FAQPage добавлена в 3 статьи без FAQ
- Дублирующиеся schemas исправлены на лендингах

### Фаза 4: HowTo + Speakable + LocalBusiness geo
- HowTo schema на 6 инструкционных статьях
- LocalBusiness с GeoCoordinates в SchemaOrg
- Speakable на главной и 2 ключевых лендингах

### Фаза 5: GEO enrichment
- Author @id и sameAs во всех Article schemas
- Внешние citations в топ-10 статьях

### Фаза 6: Analytics
- Clarity отключена на staging

## Ожидаемый результат
SEO: 6 → 9 | GEO: 7 → 9 | AEO: 5 → 9"
```

---

## Ожидаемый итог по баллам

| Фаза | SEO | GEO | AEO | Ключевые изменения |
|---|---|---|---|---|
| До | 6 | 7 | 5 | — |
| После Фазы 1 | 7.5 | 7 | 5 | robots, sitemap, redirects, {totalCount} |
| После Фазы 2 | 8.5 | 7 | 5 | все тайтлы ≤65 |
| После Фазы 3 | 9 | 8 | 7 | полная schema coverage |
| После Фазы 4 | 9 | 8 | 9 | HowTo, Speakable, LocalBusiness |
| После Фазы 5 | 9 | 9 | 9 | author @id, citations |
| После Фазы 6 | 9 | 9 | 9 | чистая аналитика |

---

## Что НЕ входит в этот план (за рамками)

Следующие задачи влияют на оценки, но требуют отдельного плана и больших ресурсов:

- **Органический трафик 2,5% → 20%+** — требует линкбилдинга, проверки позиций, работы с CTR сниппетов (отдельный план)
- **PageSpeed / Core Web Vitals** — отдельный технический план
- **Создание отдельной /about/ страницы** с биографией Дарьи (усиление GEO)
- **Контент-апгрейд статей** с оригинальными данными (опросы, статистика лагеря)
- **Управление репутацией** (Яндекс.Карты, отзывы)
