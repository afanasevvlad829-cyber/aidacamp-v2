// Python Academy — образовательная платформа по Python
// Полностью на русском, для возраста 9–18

const TOPICS = [
  {id:1, emoji:"🐍", title:"Введение в Python", level:"easy", group:"Основы",
    intro:"Python — это язык, на котором ты разговариваешь с компьютером! 🐍 Представь, что компьютер — это робот-помощник. Чтобы он понял тебя, нужно говорить на специальном языке. Python — один из самых простых и дружелюбных таких языков. Им пользуются учёные, игровые разработчики, создатели сайтов и даже люди, которые делают умные игрушки! Компьютер читает твою программу сверху вниз, строчка за строчкой — прямо как ты читаешь книгу. Файлы с программами на Python заканчиваются на .py — например, «игра.py». 🎮 Пример из жизни: представь, что ты пишешь рецепт для робота-повара. Каждая строка — новый шаг: «возьми муку», «добавь яйца» и так далее. Python работает точно так же!",
    syntax:`print("Привет, мир!")  
# Это комментарий — Python его игнорирует`,
    syntaxNote:"print() — это команда «скажи вслух». Всё, что ты напишешь внутри кавычек, Python напечатает на экране. Комментарии (строки с #) — это твои заметки для себя, компьютер их не читает."},
  {id:2, emoji:"📦", title:"Переменные", level:"easy", group:"Основы",
    intro:"Переменная — это как коробочка с именем, в которую ты кладёшь что-нибудь на хранение! 📦 Представь, что у тебя есть коробка с надписью «возраст» — ты кладёшь туда число 12. Теперь, когда тебе нужно узнать возраст, ты просто открываешь коробку «возраст»! В Python ты можешь создать такую коробку в любой момент: напишешь имя, поставишь = и положишь туда значение. 🎒 Пример из жизни: твой рюкзак — это переменная! В него ты кладёшь учебники (значения), а имя рюкзака — это имя переменной. Хочешь узнать, что внутри? Просто назови его имя! Python сам угадает тип содержимого: число, текст или что-то ещё — не нужно это указывать специально.",
    syntax:`age = 12
name = "Аня"
is_ok = True
print(age, name, is_ok)`,
    syntaxNote:"Имена переменных пишут строчными буквами через подчёркивание: my_name. Важно: age и Age — это РАЗНЫЕ коробочки! Python различает большие и маленькие буквы."},
  {id:3, emoji:"🔢", title:"Типы данных", level:"easy", group:"Основы",
    intro:"Типы данных — это разные виды вещей, которые может хранить Python! 🎨 Как в жизни: числа, слова и ответы «да/нет» — это разные вещи. В Python тоже есть разные типы: int — целые числа (как количество яблок: 5, 10, 100), float — дробные числа (как цена: 3.99, 0.5), str — текст, строки (как имя: «Маша», «Привет!»), bool — только два варианта: True (да) или False (нет), None — пустота, значит «ничего нет». 🍎 Пример из жизни: представь холодильник. Полка для фруктов, полка для напитков, полка для молочного — это разные типы! Нельзя положить молоко в ящик для фруктов. Вот так же и Python хранит разные типы данных отдельно.",
    syntax:`n = 42        # int
f = 3.14      # float
s = "Hello"   # str
b = True      # bool
x = None      # None
print(type(n), type(f))`,
    syntaxNote:"Команда type(x) покажет тебе, какого типа значение лежит в переменной. Попробуй: print(type(42)) — увидишь <class 'int'>!"},
  {id:4, emoji:"✍️", title:"Строки (str)", level:"easy", group:"Основы",
    intro:"Строка — это любой текст в кавычках! ✍️ Имя, сообщение, эмодзи — всё это строки. Представь, что строка — это ожерелье из бусин, где каждая бусина — один символ. Слово «Привет» — это ожерелье из 6 бусин: П, р, и, в, е, т. Ты можешь считать бусины (len), переворачивать ожерелье ([::-1]), отрезать часть (срез) или заменить одни бусины на другие (.replace). 💬 Пример из жизни: когда ты пишешь SMS другу — это строка! Ты можешь посмотреть сколько символов (len), написать большими буквами (.upper()) или найти слово в тексте (.find()). f-строки — как шаблон открытки: «Привет, {имя}! Тебе {возраст} лет» — Python сам подставит нужные значения!",
    syntax:`s = "Привет"
print(len(s))         # 6
print(s.upper())      # ПРИВЕТ
print(f"Меня зовут {s}")  # f-строка`,
    syntaxNote:"f-строки — самый удобный способ вставить переменную в текст. Пиши f перед кавычкой, а переменную оберни в фигурные скобки {}: f'Привет, {name}!'"},
  {id:5, emoji:"➕", title:"Числа и математика", level:"easy", group:"Основы",
    intro:"Python умеет считать как калькулятор — и даже лучше! 🧮 Обычные действия: + сложение, - вычитание, * умножение, / деление. Но есть и особенные: // — деление без остатка (10 // 3 = 3, как если делить 10 конфет между 3 друзьями — каждому достанется по 3), % — остаток от деления (10 % 3 = 1 — одна конфета осталась лишней!), ** — возведение в степень (2 ** 10 = 1024). 🍕 Пример из жизни: ты режешь пиццу на 8 кусков и хочешь раздать поровну 3 друзьям. 8 // 3 = 2 (каждому по 2 куска), 8 % 3 = 2 (2 куска лишних — можно съесть самому! 😄). Модуль math даёт ещё больше: корни, синусы, округления.",
    syntax:`import math
print(5 + 3)        # 8
print(10 // 3)      # 3
print(10 % 3)       # 1
print(2 ** 10)      # 1024
print(math.sqrt(16))# 4.0`,
    syntaxNote:"/ всегда даёт дробный результат (5/2 = 2.5), а // — только целое (5//2 = 2). Остаток % очень полезен: если число % 2 == 0 — значит оно чётное!"},
  {id:6, emoji:"📚", title:"Списки (list)", level:"easy", group:"Структуры",
    intro:"Список — это как рюкзак, в котором можно хранить много вещей по порядку! 🎒 Ты кладёшь вещи одну за другой, и у каждой есть свой номер — начиная с нуля. Первый предмет — номер 0, второй — номер 1, третий — номер 2. Почему с нуля? Так уж договорились программисты всего мира! 🍎🍊🍋 Пример из жизни: список покупок! ['молоко', 'хлеб', 'яблоки'] — это список. Ты можешь добавить новый продукт в конец (.append), убрать что-то (.remove), узнать сколько всего элементов (len), или пройти по всему списку и проверить каждый продукт (цикл for). В список можно класть что угодно: числа, слова, даже другие списки!",
    syntax:`fruits = ["яблоко","груша","банан"]
fruits.append("вишня")
print(fruits[0])     # яблоко
print(len(fruits))   # 4`,
    syntaxNote:"Индексация начинается с 0! fruits[0] — первый элемент, fruits[-1] — последний. Минусовые индексы считают с конца: очень удобно!"},
  {id:7, emoji:"🧩", title:"Срезы (slicing)", level:"mid", group:"Структуры",
    intro:"Срез — это способ вырезать кусочек из списка или строки! ✂️ Представь, что у тебя лента с фотографиями. Ты хочешь взять только фотографии с 3-й по 7-ю — вот это и называется срез! Пишешь a[начало:конец:шаг]. Начало — с какой позиции начать, конец — где остановиться (не включая его!), шаг — через сколько брать. 📷 Пример из жизни: у тебя список из 10 одноклассников. Ты хочешь пригласить только первых пятерых на день рождения — берёшь срез [0:5]. Или хочешь перевернуть список — используешь шаг -1: [::-1]. Это как перемотать плёнку назад! Срезы работают и с текстом: из слова 'Python' можно вырезать 'Py' — это 'Python'[0:2].",
    syntax:`a = [0,1,2,3,4,5]
print(a[1:4])    # [1,2,3]
print(a[::-1])   # [5,4,3,2,1,0]
print("python"[0:2])  # py`,
    syntaxNote:"Запомни: конечный индекс НЕ включается в срез! a[1:4] берёт элементы 1, 2, 3 — но не 4. Это как говорить 'от 1 до 4, не включая 4'."},
  {id:8, emoji:"📝", title:"Кортежи (tuple)", level:"mid", group:"Структуры",
    intro:"Кортеж — это список, который нельзя изменить! 🔒 Представь, что ты записал координаты своего дома на камне. Адрес постоянный, менять его нельзя — вот так работает кортеж. Он выглядит как список, но в круглых скобках (). Зачем нужна такая «заблокированная» версия списка? Чтобы случайно не испортить важные данные! 📍 Пример из жизни: координаты города на карте (55.7, 37.6) — это широта и долгота. Они не должны меняться! Твой день рождения (день, месяц, год) — тоже отличный кортеж: (15, 3, 2013). Кортеж удобен, когда данные связаны между собой и должны оставаться неизменными — как точка на карте или цвет (красный, зелёный, синий).",
    syntax:`point = (3, 4)
x, y = point
print(x, y)   # 3 4`,
    syntaxNote:"Кортежи нельзя менять — это их главное отличие от списков. Зато их можно 'распаковать': x, y = (3, 4) — переменные получают значения автоматически!"},
  {id:9, emoji:"🗂️", title:"Словари (dict)", level:"easy", group:"Структуры",
    intro:"Словарь — это как настоящий словарь или телефонная книга! 📖 В словаре у каждого слова есть своё объяснение, а в телефонной книге у каждого имени — свой номер. В Python словарь работает точно так же: у каждого ключа есть своё значение. Ты не ищешь по номеру (как в списке), а ищешь по имени — это гораздо удобнее! 📱 Пример из жизни: твой контакт в телефоне — это словарь! {'имя': 'Вася', 'телефон': '+7999...', 'класс': '6Б'}. Хочешь узнать телефон? Спроси user['телефон']. Хочешь добавить день рождения? Просто напиши user['день_рождения'] = '15 марта'. Словарь сам расширится! Это самая используемая структура данных в Python.",
    syntax:`user = {"name":"Маша","age":10}
print(user["name"])
user["age"] = 11
for k,v in user.items():
    print(k, v)`,
    syntaxNote:"Доступ к значению — через квадратные скобки с ключом: user['name']. Используй .get(ключ) вместо [] — это безопаснее: если ключа нет, вернёт None вместо ошибки."},
  {id:10, emoji:"🎯", title:"Множества (set)", level:"mid", group:"Структуры",
    intro:"Множество — это коллекция уникальных вещей, где нет повторений! 🎯 Представь мешок со стикерами: сколько бы одинаковых стикеров ты ни положил, в мешке всё равно будет только один такой. Множество само удаляет дубликаты! 🏷️ Пример из жизни: список гостей на вечеринке. Если Вася приглашён дважды — он всё равно придёт один раз. Множество — как список гостей: каждое имя встречается только один раз. Зачем это нужно? Например, чтобы найти одинаковые элементы двух групп: кто учится и в футбольной команде, и в музыкальном кружке? Это пересечение множеств (&)! Объединение (|) — кто в хотя бы одном кружке. Разность (-) — кто только в одном.",
    syntax:`a = {1,2,3,2,1}
print(a)           # {1,2,3}
print(2 in a)      # True
print(a | {3,4})   # объединение`,
    syntaxNote:"set автоматически убирает все повторяющиеся элементы. Операции: | объединение, & пересечение, - разность. Удобно проверять принадлежность: 'x in my_set' работает очень быстро!"},
  {id:11, emoji:"🔀", title:"Условия if/elif/else", level:"easy", group:"Логика",
    intro:"Условие — это развилка на дороге: направо пойдёшь — одно случится, налево — другое! 🚦 Компьютер каждый раз смотрит на условие и решает, что делать дальше. Это как светофор: если горит зелёный (True) — идёшь, если красный (False) — стоишь! 🌧️ Пример из жизни: утром ты выглядываешь в окно. ЕСЛИ идёт дождь → берёшь зонтик. ИНАЧЕ ЕСЛИ облачно → надеваешь куртку. ИНАЧЕ → идёшь в футболке. В Python это: if / elif / else. Ты можешь проверять сколько угодно условий через elif! Важно: блоки кода в Python выделяются отступами (4 пробела или Tab) — не фигурными скобками, как в других языках.",
    syntax:`age = 14
if age >= 18:
    print("Взрослый")
elif age >= 12:
    print("Подросток")
else:
    print("Ребёнок")`,
    syntaxNote:"Блоки выделяются ОТСТУПАМИ (4 пробела или Tab). После if/elif/else ставится двоеточие :. Сравнения: == (равно), != (не равно), > < >= <=."},
  {id:12, emoji:"🔁", title:"Циклы for", level:"easy", group:"Логика",
    intro:"Цикл for — это способ сделать одно и то же несколько раз, не переписывая код! 🔁 Представь, что тебе нужно поздороваться с каждым из 30 одноклассников. Ты же не будешь 30 раз писать print('Привет')? Цикл for сделает это сам! 🦷 Пример из жизни: чистка зубов — ты повторяешь движение снова и снова. Или раздача тетрадей в классе: ты идёшь ПО КАЖДОМУ ряду и кладёшь тетрадь. Именно так работает for: он идёт по каждому элементу списка или по числам и выполняет действие. range(5) создаёт числа 0, 1, 2, 3, 4. range(1, 11) — числа от 1 до 10. Очень удобно для повторяющихся задач!",
    syntax:`for i in range(5):
    print(i)   # 0..4
for f in ["a","b","c"]:
    print(f)`,
    syntaxNote:"range(n) даёт числа от 0 до n-1. range(a, b) — от a до b-1. range(a, b, шаг) — с шагом. Например range(0, 10, 2) даст: 0, 2, 4, 6, 8."},
  {id:13, emoji:"🔃", title:"Циклы while", level:"easy", group:"Логика",
    intro:"Цикл while повторяет действие до тех пор, пока выполняется условие! ⏳ Это как будильник: он звонит, пока ты его не выключишь. Или игра в теннис: ты бьёшь по мячу, пока он не улетит за линию. 🎮 Пример из жизни: игра 'Угадай число'. Ты продолжаешь угадывать ПОКА не угадаешь правильно. Никто не знает заранее, сколько раз придётся повторить — вот для этого и нужен while! Отличие от for: for используют, когда знаешь количество повторений заранее (пройти по 10 элементам). while — когда не знаешь и повторяешь до выполнения условия. Важно: не забудь изменять переменную внутри цикла, иначе он будет работать вечно!",
    syntax:`n = 5
while n > 0:
    print(n)
    n -= 1`,
    syntaxNote:"Обязательно меняй условие внутри цикла! Если условие никогда не станет False — цикл не остановится. Команда break принудительно выходит из цикла, continue — пропускает текущий шаг."},
  {id:14, emoji:"🌀", title:"Вложенные циклы", level:"mid", group:"Логика",
    intro:"Вложенный цикл — это цикл внутри цикла! 🌀 Представь, что ты убираешь в комнате: сначала идёшь по каждой полке (внешний цикл), а на каждой полке проверяешь каждую книгу (внутренний цикл). 🏫 Пример из жизни: расписание уроков. Для каждого дня недели (5 дней) у тебя есть несколько уроков (6 уроков). Итого: 5 × 6 = 30 ячеек расписания. Вложенный цикл обойдёт все 30! Или таблица умножения: внешний цикл — первое число (1..10), внутренний — второе число (1..10). Получаем все 100 примеров. Важно помнить: внутренний цикл выполняется ПОЛНОСТЬЮ на каждом шаге внешнего. Это как матрёшка: открываешь большую — внутри маленькая.",
    syntax:`for y in range(3):
    for x in range(3):
        print(x, y, end="  ")
    print()`,
    syntaxNote:"Внутренний цикл выполняется полностью на каждом шаге внешнего. Для таблицы 3×3 это 9 шагов. Следи за отступами — внутренний цикл делается с двойным отступом!"},
  {id:15, emoji:"🛠️", title:"Функции (def)", level:"easy", group:"Функции",
    intro:"Функция — это мини-программа внутри программы! 🛠️ Представь, что у тебя есть волшебная кнопка «сделай бутерброд». Нажал — и всё само делается! Функция — это такая кнопка. Ты придумываешь её один раз, даёшь ей имя, а потом вызываешь когда нужно — хоть 100 раз! 🍕 Пример из жизни: рецепт приготовления пиццы — это функция! Ты написал рецепт один раз (def make_pizza), а потом «вызываешь» его каждый раз, когда хочешь пиццу. Можно передавать параметры: make_pizza('маргарита') или make_pizza('пепперони'). return — это когда функция возвращает результат: как повар возвращает тебе готовое блюдо. Функции делают код чище, короче и удобнее!",
    syntax:`def add(a, b):
    return a + b
print(add(2, 3))   # 5`,
    syntaxNote:"def — это слово, с которого начинается создание функции. Параметры в скобках — это входные данные. return отправляет результат обратно. Без return функция вернёт None."},
  {id:16, emoji:"⚡", title:"Lambda", level:"mid", group:"Функции",
    intro:"Lambda — это функция на одну строчку, без имени! ⚡ Это как стикер с быстрой заметкой вместо полноценного рецепта. Иногда тебе нужна совсем маленькая функция — не стоит тратить три строки на def, return. Пишешь lambda прямо там, где нужно! 🏃 Пример из жизни: сортировка списка. У тебя есть список учеников с оценками, и ты хочешь отсортировать по оценке. Вместо отдельной функции — пишешь прямо в sorted(): key=lambda x: x['оценка']. Коротко и понятно! Lambda принимает аргументы и сразу возвращает результат. Используется внутри map(), filter(), sorted() — там, где нужна простая функция 'на месте'.",
    syntax:`sq = lambda x: x*x
print(sq(5))   # 25
nums = [1,2,3,4]
print(list(map(lambda x: x*2, nums)))`,
    syntaxNote:"lambda аргументы: результат. Всё на одной строке! Используй лямбды для простых операций. Для сложной логики лучше написать обычную функцию через def."},
  {id:17, emoji:"🔭", title:"Область видимости", level:"mid", group:"Функции",
    intro:"Область видимости — это где 'живёт' переменная и кто может её увидеть! 🔭 Представь школу: есть общие коридоры (глобальное пространство) и отдельные классы (функции). Доска в твоём классе видна всем внутри класса, но не ученикам из других кабинетов. 🏫 Пример из жизни: у тебя есть личный дневник (локальная переменная внутри функции) — он доступен только тебе. И есть школьная доска объявлений (глобальная переменная) — её все видят. Переменная, созданная внутри функции — 'локальная'. Она исчезает, когда функция заканчивает работу. Переменная снаружи — 'глобальная', её видят все. Если хочешь изменить глобальную переменную из функции — нужно написать global имя.",
    syntax:`x = "глобал"
def f():
    y = "локал"
    print(x, y)
f()`,
    syntaxNote:"Функция ВИДИТ глобальные переменные, но не может их изменить напрямую. Чтобы изменить глобальную переменную из функции, напиши 'global x' в начале функции."},
  {id:18, emoji:"📦", title:"Модули и импорт", level:"mid", group:"Функции",
    intro:"Модуль — это готовый набор инструментов, который кто-то уже написал за тебя! 🧰 Представь магазин ЛЕГО: там есть готовые наборы (модули). Вместо того, чтобы создавать каждую детальку с нуля, ты берёшь нужный набор! В Python тысячи готовых модулей: math — математика, random — случайные числа, datetime — дата и время, os — работа с компьютером. 📦 Пример из жизни: ты пишешь реферат и используешь библиотечные книги — это как import. Не нужно самому изобретать формулы! import math — берёшь всю математическую библиотеку. from random import randint — берёшь только одну функцию из модуля random. Это удобно, если нужна всего одна вещь из большого набора.",
    syntax:`import math
from random import randint
print(math.pi)
print(randint(1,10))`,
    syntaxNote:"import имя_модуля — берёт весь модуль. from модуль import функция — только нужную функцию. import модуль as псевдоним — даёт короткое имя, например: import numpy as np."},
  {id:19, emoji:"🏛️", title:"Классы (class)", level:"hard", group:"ООП",
    intro:"Класс — это чертёж или шаблон для создания объектов! 🏛️ Представь, что у тебя есть шаблон анкеты для собаки: кличка, порода, возраст, умение лаять. По этому шаблону можно создать любое количество собак: Рекс, Шарик, Бобик — у каждого своя кличка, но все умеют лаять! 🐕 Пример из жизни: класс Dog — это чертёж. Объекты rex = Dog('Рекс') и bobik = Dog('Бобик') — это конкретные собаки, созданные по чертежу. Класс описывает ЧТО умеет объект (методы) и ЧТО у него есть (атрибуты). __init__ — это конструктор, он вызывается при создании объекта (как строитель, который строит дом по чертежу). self — это сам объект, который ты создаёшь.",
    syntax:`class Dog:
    def __init__(self, name):
        self.name = name
    def bark(self):
        print(f"{self.name}: гав!")
d = Dog("Рекс")
d.bark()`,
    syntaxNote:"__init__ вызывается автоматически при создании объекта. self — это 'я сам' (ссылка на конкретный объект). self.name = name сохраняет имя внутри объекта."},
  {id:20, emoji:"🧬", title:"Наследование", level:"hard", group:"ООП",
    intro:"Наследование — это когда новый класс берёт всё лучшее от родительского! 🧬 Как ты унаследовал некоторые черты от мамы и папы — цвет глаз, характер. Но ты ещё и сам по себе уникальный! 👨‍👩‍👧 Пример из жизни: у тебя есть класс «Животное» — оно умеет дышать и есть. Класс «Кошка» наследует от «Животного» (тоже дышит и ест), но добавляет своё — умеет мяукать. Класс «Собака» — тоже наследует от «Животного», но умеет лаять. Не нужно писать 'дышать' и 'есть' в каждом классе заново! Пишешь class Cat(Animal): — и кошка уже умеет всё, что умеет животное. Это экономит кучу кода и делает программу логичнее.",
    syntax:`class Animal:
    def eat(self): print("ем")
class Cat(Animal):
    def meow(self): print("мяу")
c = Cat()
c.eat(); c.meow()`,
    syntaxNote:"super() обращается к родительскому классу. Например, super().__init__() вызывает конструктор родителя — очень полезно, чтобы не дублировать код!"},
  {id:21, emoji:"🛡️", title:"Инкапсуляция", level:"hard", group:"ООП",
    intro:"Инкапсуляция — это когда ты прячешь внутренности объекта и защищаешь их от случайных изменений! 🛡️ Как банкомат: ты нажимаешь кнопки (методы), а что происходит внутри — не видишь и не трогаешь. Это защита от ошибок! 🏦 Пример из жизни: твой телефон. Ты не можешь напрямую изменить уровень заряда батареи — только через зарядку. Это инкапсуляция! В Python переменная с _ (одним подчёркиванием) — это подсказка «не трогай без причины». Переменная с __ (двумя подчёркиваниями) — ещё более защищённая. @property — специальный декоратор, который позволяет обращаться к методу как к переменной. Очень удобно для создания 'безопасных' атрибутов!",
    syntax:`class Account:
    def __init__(self):
        self.__balance = 0
    def deposit(self, x):
        self.__balance += x
    @property
    def balance(self): return self.__balance`,
    syntaxNote:"@property превращает метод в атрибут. account.balance — вызывает метод, но выглядит как обращение к переменной. __ (двойное подчёркивание) делает атрибут приватным."},
  {id:22, emoji:"🎭", title:"Полиморфизм", level:"hard", group:"ООП",
    intro:"Полиморфизм — это когда разные объекты реагируют на одну команду по-своему! 🎭 Как слово «скажи» — ты скажешь его кошке и собаке, но кошка мяукнет, а собака залает. Одна команда — разные результаты! 🎪 Пример из жизни: у тебя в классе есть разные инструменты: ножницы, ручка, линейка. Командуешь «работай!» — ножницы режут, ручка пишет, линейка измеряет. Каждый инструмент по-своему выполняет команду. В Python это работает так: у разных классов есть метод с одинаковым именем (например, speak), но каждый делает своё. Ты можешь пройти по списку животных и вызвать speak() у каждого — Python сам разберётся, кто как говорит. Это делает код очень гибким!",
    syntax:`class Cat:
    def speak(self): return "мяу"
class Dog:
    def speak(self): return "гав"
for a in [Cat(), Dog()]:
    print(a.speak())`,
    syntaxNote:"Главное правило полиморфизма: важен не тип объекта, а то, умеет ли он выполнить нужный метод. Это называется 'утиная типизация': если ходит как утка и крякает как утка — значит это утка!"},
  {id:23, emoji:"⚠️", title:"Обработка ошибок", level:"mid", group:"Логика",
    intro:"Обработка ошибок — это способ сказать программе: 'если что-то пошло не так, не падай, а поступи вот так!' ⚠️ Представь, что ты пилот самолёта: если один двигатель ломается — ты не падаешь, а переключаешься на запасной! ✈️ Пример из жизни: ты просишь пользователя ввести число, а он вводит 'привет'. Без обработки ошибок программа сломается и закроется. С try/except — поймаешь ошибку и вежливо попросишь ввести снова! try — это блок кода, где может случиться ошибка. except — что делать, если ошибка произошла. finally — выполняется ВСЕГДА, даже если была ошибка (хорошее место для закрытия файлов). Хорошие программисты всегда думают о возможных ошибках заранее!",
    syntax:`try:
    n = int("abc")
except ValueError as e:
    print("Ошибка:", e)
finally:
    print("Готово")`,
    syntaxNote:"finally выполняется всегда — и когда ошибки нет, и когда есть. Можно ловить разные ошибки: except ValueError, except ZeroDivisionError. Или except Exception — любую ошибку."},
  {id:24, emoji:"📁", title:"Работа с файлами", level:"mid", group:"IO",
    intro:"Работа с файлами позволяет сохранять данные, чтобы они не исчезали после закрытия программы! 💾 Представь, что программа — это твоя голова, а файл — тетрадь. Всё, что у тебя в голове, исчезает когда ты засыпаешь. Но если записать в тетрадь — останется навсегда! 📓 Пример из жизни: игра с сохранением. Когда ты выходишь из игры, твой прогресс записывается в файл. Когда включаешь снова — загружается из файла. Вот так и работает запись/чтение файлов в Python! open() открывает файл, f.write() записывает, f.read() читает. Конструкция with автоматически закрывает файл после использования — как крышка на кастрюле: открыл, сделал что нужно, закрыл.",
    syntax:`with open("data.txt","w",encoding="utf-8") as f:
    f.write("Привет\\n")
with open("data.txt") as f:
    print(f.read())`,
    syntaxNote:"Режимы файла: 'r' — только читать, 'w' — писать (сотрёт старое!), 'a' — дописать в конец, 'rb'/'wb' — бинарный режим. Всегда используй encoding='utf-8' для русского текста!"},
  {id:25, emoji:"📋", title:"JSON", level:"mid", group:"Данные",
    intro:"JSON — это универсальный язык обмена данными между программами! 📋 Представь, что тебе нужно отправить другу не просто текст, а целую анкету: имя, возраст, список хобби. JSON — это стандартный формат для таких 'упакованных' данных, понятный любому языку программирования. 🌐 Пример из жизни: когда приложение погоды загружает данные из интернета, сервер отправляет ответ в формате JSON: {'город': 'Москва', 'температура': 20, 'облачно': true}. Все приложения в мире — игры, соцсети, банки — общаются через JSON! json.dumps() превращает Python-объект в JSON-строку (упаковывает). json.loads() делает обратное (распаковывает). Это как упаковать вещи в чемодан и распаковать их.",
    syntax:`import json
obj = {"name":"Аня","age":10}
s = json.dumps(obj, ensure_ascii=False)
back = json.loads(s)
print(s, back)`,
    syntaxNote:"json.dumps() — Python → текст JSON (для отправки/сохранения). json.loads() — текст JSON → Python (для использования). ensure_ascii=False сохраняет русские буквы нормально."},
  {id:26, emoji:"🎲", title:"Случайные числа", level:"easy", group:"Данные",
    intro:"Модуль random позволяет добавить непредсказуемость в программу — как кубик в настольной игре! 🎲 Без случайных чисел все игры были бы одинаковыми: враги всегда в одном месте, карты всегда в том же порядке. Скучно! 🎮 Пример из жизни: тебе нужно решить, кто первый ходит в игре. random.randint(1, 6) — это как бросить кубик! random.choice(['Вася', 'Петя', 'Маша']) — случайно выбрать игрока из списка. random.shuffle(карты) — перемешать колоду карт. Используется в играх, лотереях, тестах (случайный порядок вопросов), научных экспериментах. random.seed(42) задаёт 'семя' — тогда 'случайные' числа всегда одинаковые. Удобно для тестирования!",
    syntax:`import random
print(random.randint(1,10))
print(random.choice(["red","green","blue"]))
random.shuffle([1,2,3,4])`,
    syntaxNote:"randint(a, b) — случайное целое число от a до b включительно. choice(список) — случайный элемент из списка. shuffle(список) — перемешивает список прямо на месте (изменяет его)."},
  {id:27, emoji:"⏰", title:"Дата и время", level:"mid", group:"Данные",
    intro:"Модуль datetime позволяет работать с датами и временем в программе! ⏰ Программы постоянно используют время: когда было отправлено сообщение, сколько дней до дня рождения, не истёк ли срок действия билета. 🎂 Пример из жизни: сколько дней осталось до твоего дня рождения? datetime умеет считать! Или: в какой день недели было 1 сентября 2015 года? Python ответит за секунду. datetime.now() — текущее время. timedelta — промежуток времени (можно прибавлять и вычитать дни, часы). strftime — форматирует дату как текст: '%d.%m.%Y' даст '23.05.2026'. Время и даты — фундаментальная вещь в реальных программах: расписания, напоминания, аналитика.",
    syntax:`from datetime import datetime, timedelta
now = datetime.now()
print(now)
print(now + timedelta(days=7))`,
    syntaxNote:"strftime форматирует дату: %d — день, %m — месяц, %Y — год, %H:%M — часы:минуты. Пример: now.strftime('%d %B %Y') даст '23 Мая 2026'."},
  {id:28, emoji:"🧪", title:"List Comprehension", level:"mid", group:"Структуры",
    intro:"List comprehension — это суперкороткий способ создать список! 🧪 Вместо того чтобы писать цикл for на несколько строк, ты пишешь всё в одну строчку прямо внутри квадратных скобок. Это как сжать целый абзац в одно предложение! ✏️ Пример из жизни: тебе нужен список квадратов чисел от 1 до 10. Обычно: создаёшь пустой список, пишешь for, добавляешь каждый квадрат. Это 3 строки. С list comprehension: [x*x for x in range(1, 11)] — всё в одну строку! Можно добавить условие: [x for x in range(20) if x % 2 == 0] — только чётные числа. Это читается почти как обычное предложение: 'Х для каждого Х в диапазоне, если Х чётное'.",
    syntax:`sq = [x*x for x in range(10)]
evens = [x for x in range(20) if x%2==0]
print(sq); print(evens)`,
    syntaxNote:"List comprehension читается так: [что_делать for элемент in коллекция if условие]. Условие необязательно. Работает также для словарей: {k:v for ...} и множеств: {x for ...}."},
  {id:29, emoji:"🧰", title:"Map / Filter / Reduce", level:"mid", group:"Функции",
    intro:"Map, filter и reduce — три мощных инструмента для работы со списками! 🧰 Представь конвейер на фабрике: сначала детали красят (map), потом отбирают только хорошие (filter), потом складывают всё вместе (reduce). 🏭 Примеры из жизни: map — применить действие к каждому. Как помыть все яблоки в корзине: map(помыть, корзина_яблок). filter — оставить только подходящие. Как выбрать из класса только тех, кто выше 160 см: filter(высокий, список_учеников). reduce — свернуть список в одно значение. Как посчитать сумму всех оценок в дневнике: reduce(сложить, оценки). В современном Python чаще используют list comprehension — но map/filter тоже встречаются в чужом коде.",
    syntax:`from functools import reduce
nums = [1,2,3,4]
print(list(map(lambda x:x*2, nums)))
print(list(filter(lambda x:x>2, nums)))
print(reduce(lambda a,b:a+b, nums))`,
    syntaxNote:"map и filter возвращают специальные объекты — оберни в list(), чтобы увидеть результат. reduce суммирует список: reduce(lambda a,b: a+b, [1,2,3,4]) = 10."},
  {id:30, emoji:"🌐", title:"HTTP-запросы (requests)", level:"hard", group:"Сеть",
    intro:"Библиотека requests позволяет Python общаться с интернетом — как браузер, но в коде! 🌐 Когда ты открываешь сайт, браузер отправляет запрос на сервер и получает ответ. Python с requests делает то же самое, только ты сам управляешь каждым шагом! 📱 Пример из жизни: ты пишешь бота погоды. Программа отправляет запрос на сайт погоды: 'дай мне температуру в Москве'. Сервер отвечает данными в формате JSON. Ты берёшь нужные числа и показываешь пользователю. Это называется API — интерфейс для общения программ между собой. requests.get(url) — попросить данные. r.status_code — код ответа (200 = успех, 404 = не найдено). r.json() — получить ответ как Python-словарь.",
    syntax:`import requests
r = requests.get("https://api.github.com")
print(r.status_code)
print(r.json()["current_user_url"])`,
    syntaxNote:"Установка: pip install requests. .status_code 200 = всё хорошо. .json() парсит JSON-ответ. .text — ответ как строка. Всегда проверяй статус перед использованием данных!"},
  {id:31, emoji:"📊", title:"NumPy — массивы", level:"hard", group:"Данные",
    intro:"NumPy — это суперскоростная библиотека для работы с числами и массивами! 📊 Обычные списки Python медленные для математики. NumPy — как специальный спортивный автомобиль вместо обычного: делает те же действия, но в 100 раз быстрее! 🚀 Пример из жизни: представь, что тебе нужно умножить все оценки класса на 1.1 (повысить на 10%). С обычным списком — цикл for, строчка за строчкой. С NumPy: grades * 1.1 — и всё сразу! NumPy используют учёные, инженеры, создатели нейросетей. Именно на NumPy построены ChatGPT, распознавание лиц и умные рекомендации в YouTube. np.array создаёт массив. mean() — среднее, sum() — сумма, max()/min() — максимум/минимум.",
    syntax:`import numpy as np
a = np.array([1,2,3,4])
print(a * 2)
print(a.mean(), a.sum())`,
    syntaxNote:"NumPy операции применяются ко ВСЕМ элементам сразу: a * 2 умножает каждый элемент на 2. Это намного быстрее цикла for. Установка: pip install numpy."},
  {id:32, emoji:"🐼", title:"Pandas — таблицы", level:"hard", group:"Данные",
    intro:"Pandas — это Excel в Python! 🐼 Если ты умеешь работать с таблицами в Excel, то Pandas делает то же самое, но гораздо мощнее и всё в коде. 📊 Пример из жизни: у тебя есть таблица результатов олимпиады: имя, класс, предмет, оценка. Pandas поможет: найти среднюю оценку по каждому предмету, отфильтровать только отличников, отсортировать по баллам, построить график. DataFrame — это главная структура Pandas, как лист в Excel. Каждый столбец — это Series (как один список). Pandas читает файлы Excel, CSV, JSON и сохраняет обратно. Используется везде: от анализа продаж в магазине до исследований в науке!",
    syntax:`import pandas as pd
df = pd.DataFrame({"name":["A","B"],"age":[10,12]})
print(df)
print(df["age"].mean())`,
    syntaxNote:"DataFrame — таблица с именованными столбцами. df['столбец'] — выбрать столбец. df[df['age'] > 10] — отфильтровать строки. .mean(), .sum(), .max() — статистика по столбцу. Установка: pip install pandas."},
  {id:33, emoji:"📈", title:"Matplotlib — графики", level:"hard", group:"Данные",
    intro:"Matplotlib рисует красивые графики прямо в Python! 📈 Числа и таблицы сложно понять, но график — сразу видно всё! 👁️ Пример из жизни: ты ведёшь дневник погоды всё лето — каждый день записываешь температуру. Через 90 дней у тебя 90 чисел. Непонятно! Но нарисуй график — сразу видно: в июле было жарко, в августе похолодало. Matplotlib делает именно это: берёт твои числа и рисует линейный график, столбчатую диаграмму, круговую диаграмму или точечный рисунок. plt.plot(x, y) — линейный график. plt.bar(x, y) — столбики. plt.title() — заголовок. plt.xlabel()/plt.ylabel() — названия осей. plt.show() — показать график в окне.",
    syntax:`import matplotlib.pyplot as plt
x = [1,2,3,4]
y = [10,15,8,20]
plt.plot(x, y)
plt.title("Пример")
plt.show()`,
    syntaxNote:"plt.show() открывает окно с графиком. plt.savefig('график.png') — сохраняет в файл. Установка: pip install matplotlib. Используй разные типы: plot, bar, scatter, hist, pie."},
  {id:34, emoji:"📚", title:"Стек (стек на list)", level:"hard", group:"Алгоритмы",
    intro:"Стек — это структура данных по принципу 'последним пришёл — первым ушёл'! 📚 Представь стопку тарелок: ты кладёшь тарелки сверху и берёшь тоже сверху. Нельзя взять тарелку снизу, не убрав все верхние! 🍽️ Примеры из жизни: кнопка 'Отменить' (Ctrl+Z) — это стек! Когда ты отменяешь действия, они отменяются в обратном порядке: последнее действие отменяется первым. Или история браузера: нажал 'Назад' — вернулся на предыдущую страницу. push — положить элемент на вершину стека (append в Python). pop — снять элемент с вершины (pop в Python). peek — посмотреть на верхний элемент, не снимая его. В Python стек удобно делать из обычного списка!",
    syntax:`class Stack:
    def __init__(self): self.items = []
    def push(self, x): self.items.append(x)
    def pop(self): return self.items.pop()
    def peek(self): return self.items[-1]`,
    syntaxNote:"append() добавляет в конец списка (вершина стека), pop() берёт с конца. Это быстро — O(1)! Стек используют: компиляторы Python, проверка правильности скобок, навигация 'назад'."},
  {id:35, emoji:"🚶", title:"Очередь (deque)", level:"hard", group:"Алгоритмы",
    intro:"Очередь — это структура данных по принципу 'первым пришёл — первым ушёл'! 🚶 Как живая очередь в магазине: тот, кто пришёл раньше всех, обслуживается первым. Честно и по порядку! 🏪 Примеры из жизни: очередь в столовой — первый пришёл, первый получил обед. Принтер — документы печатаются в том порядке, в каком были отправлены. Чат-боты обрабатывают сообщения по очереди. enqueue — встать в конец очереди (добавить элемент). dequeue — обслужить первого в очереди (взять с начала). В Python есть специальная структура deque (двусторонняя очередь) — она быстрее обычного списка для операций с началом и концом!",
    syntax:`from collections import deque
q = deque()
q.append(1); q.append(2)
print(q.popleft())   # 1`,
    syntaxNote:"deque (читается 'дек') — двусторонняя очередь. append() добавляет в конец, popleft() берёт с начала. Это быстро — O(1). Обычный list.pop(0) медленный для больших очередей!"},
  {id:36, emoji:"⛓️", title:"Связный список", level:"hard", group:"Алгоритмы",
    intro:"Связный список — это цепочка, где каждое звено знает следующее! ⛓️ Представь поезд: каждый вагон соединён со следующим. Чтобы добраться до 5-го вагона — надо пройти через 1, 2, 3, 4. Зато очень легко добавить новый вагон в начало или вставить между двумя! 🚂 Пример из жизни: музыкальный плейлист! Каждая песня знает, какая идёт следующей. Хочешь вставить новую песню — просто меняешь ссылки у соседних. В обычном списке пришлось бы двигать все остальные элементы. Нoda — это один элемент (вагон). В ней хранится значение (v) и ссылка на следующий элемент (next). head — это первый элемент цепочки. Связный список отлично подходит для частых вставок и удалений в начале.",
    syntax:`class Node:
    def __init__(self, v):
        self.v = v
        self.next = None
class LinkedList:
    def __init__(self): self.head = None
    def add(self, v):
        n = Node(v); n.next = self.head; self.head = n`,
    syntaxNote:"Нет быстрого доступа по индексу (нельзя взять 5-й элемент сразу), зато вставка в начало — O(1). Чтобы найти элемент — нужно обходить с начала. Это компромисс: быстрые вставки vs быстрый доступ."},
  {id:37, emoji:"🌲", title:"Дерево", level:"hard", group:"Алгоритмы",
    intro:"Дерево — это структура данных с корнем, ветками и листьями — прямо как настоящее дерево! 🌲 Только растёт оно сверху вниз: корень наверху, листья внизу. Каждый узел может иметь несколько 'детей', а у каждого ребёнка — своих детей. 🗂️ Примеры из жизни: папки на компьютере — это дерево! Диск C → Папки → Подпапки → Файлы. Или схема организации компании: директор → менеджеры → сотрудники. Или семейное дерево! В браузере веб-страница — это тоже дерево (DOM): html → head/body → теги внутри. Обход дерева: DFS (в глубину — как идти по лабиринту до упора, потом возвращаться) или BFS (в ширину — обходить уровень за уровнем).",
    syntax:`class TreeNode:
    def __init__(self, v):
        self.v = v
        self.children = []
    def add(self, c): self.children.append(c)`,
    syntaxNote:"Корень — самый верхний узел без родителя. Листья — узлы без детей. DFS (поиск в глубину) использует стек/рекурсию. BFS (поиск в ширину) использует очередь."},
  {id:38, emoji:"🔢", title:"Алгоритмы сортировки", level:"hard", group:"Алгоритмы",
    intro:"Алгоритмы сортировки — это разные способы расставить элементы по порядку! 🔢 Представь, что тебе нужно расставить 100 книг на полке по алфавиту. Есть разные стратегии — быстрые и медленные. 📚 Пузырьковая сортировка: сравниваешь пары соседних карточек и меняешь местами, если не в том порядке. Самые тяжёлые 'всплывают' вверх — как пузырьки! Медленная, но простая для понимания. Сортировка выбором: ищешь самый маленький элемент, ставишь на первое место. Потом второй по величине — на второе. И так далее. Быстрая сортировка: выбираешь 'опорный' элемент, делишь список на меньшие и большие, сортируешь каждую часть. Очень быстрая! В реальных программах используй sorted() — Python сам выберет лучший алгоритм (Timsort).",
    syntax:`def bubble(a):
    a = a[:]
    for i in range(len(a)):
        for j in range(len(a)-1-i):
            if a[j] > a[j+1]:
                a[j], a[j+1] = a[j+1], a[j]
    return a
print(bubble([3,1,4,1,5,9,2,6]))`,
    syntaxNote:"В реальности всегда используй sorted(a) или a.sort() — встроенные алгоритмы Python быстрее и надёжнее. Алгоритмы сортировки изучают, чтобы понять принципы эффективности кода."},
  {id:39, emoji:"🔍", title:"Поиск (бинарный)", level:"hard", group:"Алгоритмы",
    intro:"Бинарный поиск — это умный способ искать в отсортированном списке, деля его пополам! 🔍 Представь игру 'Угадай число от 1 до 100'. Умная стратегия: называй середину! Назвал 50 — тебе говорят 'больше'. Теперь ищешь от 51 до 100, называешь 75. 'Меньше' — ищешь от 51 до 74. И так далее. За 7 шагов гарантированно угадаешь! 📖 Пример из жизни: поиск слова в бумажном словаре. Ты открываешь примерно на середине, видишь где находишься и понимаешь — искать в первой или второй половине. Потом снова делишь пополам. Очень быстро! Линейный поиск (проверять каждый элемент по порядку): для 1000 элементов — до 1000 шагов. Бинарный поиск: для 1000 элементов — максимум 10 шагов! Но список должен быть отсортирован.",
    syntax:`def binary(a, t):
    l, r = 0, len(a)-1
    while l <= r:
        m = (l + r) // 2
        if a[m] == t: return m
        if a[m] < t: l = m + 1
        else: r = m - 1
    return -1
print(binary([1,3,5,7,9,11], 7))`,
    syntaxNote:"Бинарный поиск работает ТОЛЬКО на отсортированных данных! Возвращает индекс найденного элемента или -1 если не нашёл. В Python есть bisect — встроенный бинарный поиск."},
  {id:40, emoji:"🎮", title:"Мини-проекты", level:"hard", group:"Проекты",
    intro:"Мини-проекты — это когда ты берёшь всё, что выучил, и создаёшь что-то настоящее! 🎮 Это самая захватывающая часть программирования — твои идеи становятся реальными программами! 🌟 Примеры проектов: Калькулятор — принимает числа и операцию, выводит результат. Используй функции и условия. Игра 'Угадай число' — компьютер загадывает, ты угадываешь. Используй random и while. To-do список — добавляй и удаляй задачи, сохраняй в файл. Используй списки и файлы. Викторина — задаёт вопросы, считает очки. Используй словари и циклы. Чат-бот — отвечает на вопросы по ключевым словам. Используй строки и условия. Начни с маленького проекта и постепенно добавляй функции. Лучший способ учиться — делать реальные вещи!",
    syntax:`import random
secret = random.randint(1, 100)
guess = int(input("Угадай число 1-100: "))
if guess == secret: print("Угадал!")
else: print("Загадано было", secret)`,
    syntaxNote:"Советы для первого проекта: сначала напиши самый простой вариант, потом улучшай. Не бойся ошибок — они нормальны! Каждая ошибка учит тебя чему-то новому. Используй всё, что знаешь: функции, циклы, условия, файлы."},
];

function makeExamples(topic){
  // Если есть кастомные примеры по теме — используем их.
  const custom = (typeof TOPIC_EXAMPLES !== "undefined") && TOPIC_EXAMPLES[topic.id];
  if (custom && custom.length){
    return custom.map((ex, idx) => ({
      title: ex.title || `${topic.title} — пример ${idx+1}`,
      level: idx < 5 ? "Новичок" : idx < 10 ? "Средний" : "Продвинутый",
      code: ex.code,
      output: ex.output,
      explanation: ex.explanation || `Пример темы «${topic.title}».`
    }));
  }
  const out = [];
  for (let i = 1; i <= 15; i++){
    out.push({
      title: `${topic.title} — пример ${i}`,
      level: i <= 5 ? "Новичок" : i <= 10 ? "Средний" : "Продвинутый",
      code: topic.syntax + `\nprint("Пример ${i} темы: ${topic.title}")`,
      output: `Пример ${i} темы: ${topic.title}`,
      explanation: `Это ${i}-й пример темы «${topic.title}».`
    });
  }
  return out;
}


function makeQuiz(topic){
  const c = (typeof TOPIC_CONTENT!=="undefined") && TOPIC_CONTENT[topic.id];
  if (c && c.quiz) return c.quiz;
  return [{q:`Тема: ${topic.title}`, opts:["A","B","C","D"], answer:0}];
}


function makeTasks(topic){
  const c = (typeof TOPIC_CONTENT!=="undefined") && TOPIC_CONTENT[topic.id];
  if (c && c.tasks) return c.tasks;
  return [{q:`Задание по теме «${topic.title}»`, hint:"См. синтаксис темы.", solution:"# решение"}];
}


function makeBugs(){
  const noOop = [
    {
      title:"Сумма и среднее списка чисел",
      broken:
`Numbers = [4, 8, 15, 16, 23, 42]
sum = 0
For i in range(0, len(Numbers) + 1):
    sum =+ numbers[i]
avg = sum / Numbers.lenght
If avg => 10
    Print("Среднее больше или равно 10)
else
   print("Меньше 10")
print("Сумма " sum)
result = {sum: sum  avg: avg}
Return result`,
      fixed:
`numbers = [4, 8, 15, 16, 23, 42]
total = 0
for i in range(len(numbers)):
    total += numbers[i]
avg = total / len(numbers)
if avg >= 10:
    print("Среднее больше или равно 10")
else:
    print("Меньше 10")
print("Сумма", total)
result = {"sum": total, "avg": avg}
print(result)`,
      errors:[
        "Numbers — в Python имена пишут с маленькой (numbers)",
        "sum — встроенная функция, не используй как имя переменной",
        "For должно быть for",
        "len(Numbers)+1 выйдет за границы — нужен просто len(numbers)",
        "регистр имени: numbers vs Numbers",
        "=+ это не сложение, нужно +=",
        "lenght — опечатка, и атрибута .length у списка в Python нет, бери len()",
        "If должно быть if",
        "=> вместо >=",
        "после условия и после else нужно двоеточие :",
        "Print — должно быть print",
        "не закрыта кавычка в строке",
        "между строкой и переменной в print нужна запятая",
        "в словаре ключи должны быть строками и разделены запятой",
        "Return вне функции — ошибка, и пишется return"
      ]
    },
    {
      title:"FizzBuzz от 1 до 15",
      broken:
`for i in Range(1 16)
    if i % 3 = 0 and i % 5 = 0
        print("FizzBuzz)
    elif i % 3 == 0
        Print "Fizz"
    elif i % 5 == 0:
        print(Buzz)
    Else:
        print(i)`,
      fixed:
`for i in range(1, 16):
    if i % 3 == 0 and i % 5 == 0:
        print("FizzBuzz")
    elif i % 3 == 0:
        print("Fizz")
    elif i % 5 == 0:
        print("Buzz")
    else:
        print(i)`,
      errors:[
        "Range должно быть range",
        "в range пропущена запятая между аргументами",
        "после for пропущено двоеточие :",
        "= это присваивание, для сравнения нужно ==",
        "после первого if пропущено двоеточие :",
        "не закрыта кавычка в \"FizzBuzz\"",
        "после elif пропущено двоеточие :",
        "Print должно быть print",
        "print без круглых скобок",
        "Buzz без кавычек — Python подумает что это переменная",
        "Else должно быть else (с маленькой)"
      ]
    },
    {
      title:"Факториал через рекурсию",
      broken:
`def Factorial(n)
    if n = 0
        return 1
    Else
        return n * factorial(n - 1)

print(Factorial(5)
print(factorial(-1))
print "10! =" Factorial(10)`,
      fixed:
`def factorial(n):
    if n == 0:
        return 1
    else:
        return n * factorial(n - 1)

print(factorial(5))
print("10! =", factorial(10))`,
      errors:[
        "имя функции лучше с маленькой: factorial",
        "после def пропущено двоеточие :",
        "= вместо == в сравнении",
        "после if пропущено двоеточие :",
        "Else с большой буквы",
        "регистр: Factorial vs factorial — это разные имена",
        "factorial(-1) уйдёт в бесконечную рекурсию (нет проверки n<0)",
        "у первого print не закрыта скобка",
        "print без скобок в последней строке",
        "между строкой и числом в print нужна запятая, а не пробел",
        "повторное несогласованное использование имени функции"
      ]
    },
    {
      title:"Переворот строки",
      broken:
`def Reverse(s):
    result = ""
    for i in range(len(s) 0 -1)
        result = result + s(i)
    Return Result

text = Hello
print("Перевёрнутая:" reverse(text)
print(Reverse(123))`,
      fixed:
`def reverse(s):
    result = ""
    for i in range(len(s) - 1, -1, -1):
        result = result + s[i]
    return result

text = "Hello"
print("Перевёрнутая:", reverse(text))
print(reverse(str(123)))`,
      errors:[
        "имя функции лучше с маленькой: reverse",
        "в range пропущены запятые между аргументами",
        "стартовый индекс должен быть len(s)-1, а не len(s)",
        "после for пропущено двоеточие :",
        "s(i) — у строки индексация через s[i], а не вызов",
        "Return с большой буквы",
        "Result не определён (была result)",
        "Hello без кавычек — Python ищет переменную",
        "регистр: Reverse vs reverse",
        "у print не закрыта скобка",
        "между строкой и значением в print нужна запятая",
        "reverse(123) — число нельзя итерировать, нужно str(123)"
      ]
    },
    {
      title:"Проверка простого числа",
      broken:
`def IsPrime(n)
    if n < 2
        Return False
    for i in range(2 n)
        if n % i = 0:
            return false
    return True

print(isprime(7))
Print(IsPrime(10)
print(IsPrime("11"))`,
      fixed:
`def is_prime(n):
    if n < 2:
        return False
    for i in range(2, n):
        if n % i == 0:
            return False
    return True

print(is_prime(7))
print(is_prime(10))
print(is_prime(11))`,
      errors:[
        "имя функции лучше is_prime (snake_case)",
        "после def пропущено двоеточие :",
        "после первого if пропущено двоеточие :",
        "Return с большой буквы",
        "в range пропущена запятая",
        "= вместо == в сравнении",
        "false должно быть False (с большой)",
        "isprime ≠ IsPrime — Python чувствителен к регистру",
        "Print — должно быть print",
        "у Print не закрыта скобка",
        "IsPrime(\"11\") — строку нельзя сравнивать с числом, нужно int()"
      ]
    },
    {
      title:"Подсчёт гласных в строке",
      broken:
`text = Привет мир
vowels = аеёиоуыэюя
count = 0
For ch in Text:
    if ch.lower in vowels
        count = count + 1
    Else
        pass
print("Гласных:" Count)
print("Длина:", Text.lenght)`,
      fixed:
`text = "Привет мир"
vowels = "аеёиоуыэюя"
count = 0
for ch in text:
    if ch.lower() in vowels:
        count = count + 1
    else:
        pass
print("Гласных:", count)
print("Длина:", len(text))`,
      errors:[
        "строка без кавычек у text",
        "строка без кавычек у vowels",
        "For должно быть for",
        "Text vs text — разные имена (регистр)",
        "ch.lower без () — это ссылка на метод, а не вызов",
        "после if пропущено двоеточие :",
        "Else с большой",
        "Count vs count — регистр",
        "между строкой и переменной в print нужна запятая",
        "Text.lenght — опечатка, и у строки нет .length, нужно len()",
        "несогласованные отступы внутри блока"
      ]
    },
    {
      title:"Фибоначчи (первые n чисел)",
      broken:
`def fib(N)
    a, b = 0 1
    For i in range(N):
        Print(a end=" ")
        a, b = b a + b
    print

fib(10)
fib("five")
fib(-3)`,
      fixed:
`def fib(n):
    a, b = 0, 1
    for i in range(n):
        print(a, end=" ")
        a, b = b, a + b
    print()

fib(10)
fib(5)`,
      errors:[
        "после def пропущено двоеточие :",
        "в распаковке 0 1 пропущена запятая",
        "For должно быть for",
        "Print должно быть print",
        "в print пропущена запятая между a и end=...",
        "в распаковке b a+b пропущена запятая",
        "print без скобок — должно быть print()",
        "fib(\"five\") — строку нельзя передать в range()",
        "fib(-3) — отрицательное число даст пустой результат, нужна проверка",
        "регистр: N в параметре vs n в теле",
        "несогласованные отступы внутри функции",
        "лучше использовать snake_case для имён"
      ]
    },
    {
      title:"Максимум в списке без max()",
      broken:
`nums = (10 20 5 99 7)
maximum = 0
for i in range(1 len(nums) + 1):
    if nums[i] > maximum
        maximum == nums[i]
Print("Максимум:" maximum)
print(maximum + " — это максимум")
return maximum`,
      fixed:
`nums = [10, 20, 5, 99, 7]
maximum = nums[0]
for i in range(1, len(nums)):
    if nums[i] > maximum:
        maximum = nums[i]
print("Максимум:", maximum)
print(str(maximum) + " — это максимум")`,
      errors:[
        "список объявлен круглыми скобками — нужен [..]",
        "в литерале списка пропущены запятые",
        "maximum=0 не подходит для отрицательных чисел — бери nums[0]",
        "в range пропущена запятая",
        "len(nums)+1 выйдет за границы — нужно len(nums)",
        "после if пропущено двоеточие :",
        "== это сравнение, для присваивания нужно =",
        "Print должно быть print",
        "между строкой и переменной в print нужна запятая",
        "число нельзя складывать со строкой через + без str()",
        "return вне функции — ошибка"
      ]
    },
    {
      title:"Частота слов в предложении",
      broken:
`sentence = "кот и пёс и кот и попугай
words = sentence.Split()
freq = []
For w in Words:
    if w in freq
        freq[w] = freq[w] + 1
    else:
        freq[w] == 1
print(Freq)
print("Уникальных:" len(freq)`,
      fixed:
`sentence = "кот и пёс и кот и попугай"
words = sentence.split()
freq = {}
for w in words:
    if w in freq:
        freq[w] = freq[w] + 1
    else:
        freq[w] = 1
print(freq)
print("Уникальных:", len(freq))`,
      errors:[
        "у строки sentence не закрыта кавычка",
        "Split должно быть split",
        "freq должен быть словарём {}, а не списком []",
        "For должно быть for",
        "Words vs words — регистр",
        "после if пропущено двоеточие :",
        "== это сравнение, для присваивания нужно =",
        "к словарю нельзя обращаться по ключу как к списку, если он list",
        "Freq vs freq — регистр",
        "между строкой и значением в print нужна запятая",
        "у последнего print не закрыта скобка"
      ]
    },
    {
      title:"Конвертер Цельсий → Фаренгейт",
      broken:
`def C_to_F(c)
    F = c * 9/5 + 32
    Return f

Temps = [0 20 36.6 100]
for t in temps
    Print("C=" t " F=" C_to_F(T))
print(C_to_F("36"))`,
      fixed:
`def c_to_f(c):
    f = c * 9 / 5 + 32
    return f

temps = [0, 20, 36.6, 100]
for t in temps:
    print("C=", t, "F=", c_to_f(t))
print(c_to_f(36))`,
      errors:[
        "после def пропущено двоеточие :",
        "F и f — разные имена (регистр)",
        "Return с большой",
        "Temps vs temps — регистр",
        "в списке пропущены запятые",
        "после for пропущено двоеточие :",
        "Print должно быть print",
        "T vs t — регистр",
        "между аргументами print нужны запятые",
        "C_to_F(\"36\") — строку нельзя умножать на число корректно, нужно int()",
        "по PEP8 имена функций лучше с маленькой"
      ]
    },
    {
      title:"Простой калькулятор",
      broken:
`a = input("Первое: ")
b = input("Второе: ")
op = input("Операция: ")
if op = "+"
    print(a + b)
elif op == "-":
    Print(a - b)
elif op == "*"
    print(a * b)
elif op == "/":
    if b == 0
        print("Деление на 0)
    else:
        print(a / b)
Else
    print("Неизвестная операция")`,
      fixed:
`a = float(input("Первое: "))
b = float(input("Второе: "))
op = input("Операция: ")
if op == "+":
    print(a + b)
elif op == "-":
    print(a - b)
elif op == "*":
    print(a * b)
elif op == "/":
    if b == 0:
        print("Деление на 0")
    else:
        print(a / b)
else:
    print("Неизвестная операция")`,
      errors:[
        "input() возвращает строку — нужно обернуть в float() или int()",
        "= вместо == в сравнении",
        "после первого if пропущено двоеточие :",
        "Print должно быть print",
        "после elif \"*\" пропущено двоеточие :",
        "a - b со строками вызовет ошибку без преобразования",
        "после if b==0 пропущено двоеточие :",
        "в строке \"Деление на 0\" не закрыта кавычка",
        "Else с большой буквы",
        "после Else пропущено двоеточие :",
        "a/b со строками тоже не сработает",
        "сравнение b==0 со строкой всегда False — нужны числа"
      ]
    },
    {
      title:"Палиндром",
      broken:
`def Is_Palindrome(s)
    s = s.Lower()
    Return s = s[::-1]

print(IsPalindrome("Радар"))
print(is_palindrome(12321))
print(is_palindrome("шалаш")
if is_palindrome("Анна")
    print("Да")`,
      fixed:
`def is_palindrome(s):
    s = s.lower()
    return s == s[::-1]

print(is_palindrome("Радар"))
print(is_palindrome(str(12321)))
print(is_palindrome("шалаш"))
if is_palindrome("Анна"):
    print("Да")`,
      errors:[
        "имя функции лучше snake_case: is_palindrome",
        "после def пропущено двоеточие :",
        "Lower должно быть lower",
        "Return с большой",
        "= вместо == в сравнении",
        "IsPalindrome ≠ Is_Palindrome — регистр",
        "is_palindrome(12321) — у числа нет .lower(), нужно str()",
        "у print(is_palindrome(\"шалаш\") не закрыта внешняя скобка",
        "после if пропущено двоеточие :",
        "несогласованность стиля имён в коде",
        "у строкового slice нужно s[::-1], а не s[:-1]"
      ]
    },
    {
      title:"Сортировка пузырьком",
      broken:
`arr = [5 3 8 1 4]
n = len(arr)
For i in range(n)
    for j in range(0 n - i - 1)
        if arr[j] > arr[j + 1]
            arr[j] arr[j+1] = arr[j+1] arr[j]
Print("Отсортировано:" arr)
print(arr[10])
Return arr`,
      fixed:
`arr = [5, 3, 8, 1, 4]
n = len(arr)
for i in range(n):
    for j in range(0, n - i - 1):
        if arr[j] > arr[j + 1]:
            arr[j], arr[j + 1] = arr[j + 1], arr[j]
print("Отсортировано:", arr)
print(arr)`,
      errors:[
        "в списке пропущены запятые",
        "For должно быть for",
        "после внешнего for пропущено двоеточие :",
        "в range внутреннего for пропущена запятая",
        "после внутреннего for пропущено двоеточие :",
        "после if пропущено двоеточие :",
        "в обмене пропущены запятые слева и справа от =",
        "Print должно быть print",
        "между строкой и списком в print нужна запятая",
        "arr[10] выйдет за границы (IndexError)",
        "Return вне функции — ошибка"
      ]
    },
    {
      title:"НОД (алгоритм Евклида)",
      broken:
`def GCD(a b)
    while b ≠ 0:
        a, b = b a % b
    Return A

print(gcd(48 18))
print(GCD(0, 0))
print(GCD("12", 8))
if GCD(10,5) = 5
    print("OK")`,
      fixed:
`def gcd(a, b):
    while b != 0:
        a, b = b, a % b
    return a

print(gcd(48, 18))
print(gcd(12, 8))
if gcd(10, 5) == 5:
    print("OK")`,
      errors:[
        "имя функции лучше с маленькой: gcd",
        "в параметрах функции пропущена запятая",
        "после def пропущено двоеточие :",
        "≠ не оператор Python — нужно !=",
        "в a,b = b a%b пропущена запятая",
        "Return с большой",
        "A vs a — регистр",
        "gcd ≠ GCD — регистр",
        "в вызове gcd(48 18) пропущена запятая",
        "GCD(0,0) даст некорректный результат — нужна проверка",
        "GCD(\"12\", 8) — строка вместо числа",
        "после if пропущено двоеточие :",
        "= вместо == в условии"
      ]
    },
    {
      title:"Подсчёт строк (имитация файла)",
      broken:
`lines = ["раз" "два", "три, "четыре"]
count = 0
for L in Lines
    if len(l) > 0:
        count =+ 1
    Else:
        continue
print("Непустых строк:" Count)
print("Первая:" lines(0))
print(lines[10])
Return count`,
      fixed:
`lines = ["раз", "два", "три", "четыре"]
count = 0
for line in lines:
    if len(line) > 0:
        count += 1
    else:
        continue
print("Непустых строк:", count)
print("Первая:", lines[0])`,
      errors:[
        "в списке пропущена запятая после \"раз\"",
        "не закрыта кавычка у \"три\"",
        "Lines vs lines — регистр",
        "после for пропущено двоеточие :",
        "L и l — разные имена (регистр)",
        "=+ не сложение, нужно +=",
        "Else с большой",
        "Count vs count — регистр",
        "между строкой и переменной в print нужна запятая",
        "lines(0) — у списка индексация через lines[0]",
        "lines[10] выйдет за границы",
        "Return вне функции"
      ]
    }
  ];

  const withOop = [
    {
      title:"Класс Animal и наследник Dog",
      broken:
`Class Animal
    def __Init__(name, age):
        Self.name = name
        self.Age == age
    def Speak()
        print(self.Name + " говорит")

class Dog(animal):
    def __init__(self, name, age, breed)
        super.__init__(name)
        self.breed = breed
    def bark:
        Print("Гав!")

d = New Dog("Рекс" 3 "лабрадор")
d.Speak
d.bark()
print(d.__secret)`,
      fixed:
`class Animal:
    def __init__(self, name, age):
        self.name = name
        self.age = age
    def speak(self):
        print(self.name + " говорит")

class Dog(Animal):
    def __init__(self, name, age, breed):
        super().__init__(name, age)
        self.breed = breed
    def bark(self):
        print("Гав!")

d = Dog("Рекс", 3, "лабрадор")
d.speak()
d.bark()`,
      errors:[
        "Class должно быть class",
        "после имени класса нужно двоеточие :",
        "__Init__ → __init__",
        "первый параметр метода должен быть self",
        "Self с большой — нужно self",
        "== это сравнение, для присваивания нужно =",
        "Name vs name — регистр",
        "у Speak() пропущено двоеточие :",
        "у Speak нет параметра self",
        "animal с маленькой — должен быть Animal",
        "у конструктора Dog после ) пропущено двоеточие :",
        "super.__init__ → super().__init__()",
        "в super().__init__ не передан age",
        "у def bark пропущены (self) и :",
        "Print → print",
        "New Dog — в Python нет new, просто Dog(...)",
        "в вызове Dog пропущены запятые",
        "d.Speak без () — вызов метода требует ()",
        "поле __secret не объявлено"
      ]
    },
    {
      title:"Банковский счёт",
      broken:
`class BankAccount
    def __init__(self owner balance=0):
        self.Owner = owner
        self.balance == balance
    def Deposit(amount)
        self.balance = self.balance + Amount
    def withdraw(self, amount):
        if amount > self.Balance
            print("Недостаточно)
        else:
            self.balance -= amount

a = bankaccount("Иван" 100)
a.Deposit(50)
a.withdraw("30")
Print(a.balance)`,
      fixed:
`class BankAccount:
    def __init__(self, owner, balance=0):
        self.owner = owner
        self.balance = balance
    def deposit(self, amount):
        self.balance = self.balance + amount
    def withdraw(self, amount):
        if amount > self.balance:
            print("Недостаточно")
        else:
            self.balance -= amount

a = BankAccount("Иван", 100)
a.deposit(50)
a.withdraw(30)
print(a.balance)`,
      errors:[
        "после class пропущено двоеточие :",
        "в параметрах __init__ пропущены запятые",
        "Owner vs owner — регистр поля",
        "== это сравнение, для присваивания нужно =",
        "у Deposit нет self в параметрах",
        "после Deposit пропущено двоеточие :",
        "Amount vs amount — регистр",
        "имя метода Deposit — по PEP8 лучше deposit",
        "Balance vs balance — регистр",
        "после if пропущено двоеточие :",
        "не закрыта кавычка в \"Недостаточно\"",
        "bankaccount с маленькой — должен быть BankAccount",
        "в вызове конструктора пропущена запятая",
        "withdraw(\"30\") — строка вместо числа",
        "Print → print"
      ]
    },
    {
      title:"Прямоугольник",
      broken:
`class Rectangle
    def __init__(self, W, H)
        self.w = w
        self.H = h
    def area():
        Return self.W * self.h
    Def perimeter(self):
        return 2*(self.w + self.H)

r = Rectangle(3, "4")
print(r.Area())
print(r.perimeter)
print(r.area() + r.w)`,
      fixed:
`class Rectangle:
    def __init__(self, w, h):
        self.w = w
        self.h = h
    def area(self):
        return self.w * self.h
    def perimeter(self):
        return 2 * (self.w + self.h)

r = Rectangle(3, 4)
print(r.area())
print(r.perimeter())
print(r.area() + r.w)`,
      errors:[
        "после class пропущено двоеточие :",
        "после __init__ пропущено двоеточие :",
        "W и w — разные имена (регистр)",
        "H и h — разные имена (регистр)",
        "у area нет self в параметрах",
        "Return с большой",
        "self.W не существует (есть self.w)",
        "Def с большой — должно быть def",
        "Rectangle(3, \"4\") — строка вместо числа",
        "r.Area() — метод area, регистр важен",
        "r.perimeter без () — это ссылка на метод",
        "несогласованный регистр полей внутри класса"
      ]
    },
    {
      title:"Студент с оценками",
      broken:
`class Student:
    def __init__(self name)
        self.name = Name
        self.grades = ()
    def add_grade(grade):
        self.Grades.append(grade)
    def average(self):
        Return sum(self.grades) / len(self.Grades)

s = Student("Аня")
s.add_grade(5)
s.add_grade("4")
Print(s.average())
print(s.Name)`,
      fixed:
`class Student:
    def __init__(self, name):
        self.name = name
        self.grades = []
    def add_grade(self, grade):
        self.grades.append(grade)
    def average(self):
        return sum(self.grades) / len(self.grades)

s = Student("Аня")
s.add_grade(5)
s.add_grade(4)
print(s.average())
print(s.name)`,
      errors:[
        "в параметрах __init__ пропущена запятая",
        "после __init__ пропущено двоеточие :",
        "Name vs name — регистр",
        "self.grades = () — это кортеж, у него нет .append, нужен список []",
        "у add_grade нет self в параметрах",
        "Grades vs grades — регистр",
        "Return с большой",
        "повторный регистр Grades — нужен grades",
        "add_grade(\"4\") — строка попадёт в sum() и сломает деление",
        "Print должно быть print",
        "s.Name — поле называется name, регистр важен",
        "несогласованный регистр полей по всему классу"
      ]
    },
    {
      title:"Машина",
      broken:
`class Car
    def __init__(self, model):
        self.Model = model
        self.running == False
    def start()
        self.Running = True
        Print(self.model + " завелась")
    def stop(self):
        self.running = false

c = car("BMW")
c.Start()
c.stop()
print(C.running)`,
      fixed:
`class Car:
    def __init__(self, model):
        self.model = model
        self.running = False
    def start(self):
        self.running = True
        print(self.model + " завелась")
    def stop(self):
        self.running = False

c = Car("BMW")
c.start()
c.stop()
print(c.running)`,
      errors:[
        "после class Car пропущено двоеточие :",
        "Model vs model — регистр поля",
        "== это сравнение, нужно =",
        "у start нет self в параметрах",
        "после start пропущено двоеточие :",
        "Running vs running — регистр",
        "Print → print",
        "false должно быть False (с большой)",
        "car с маленькой — должен быть Car",
        "Start с большой — метод называется start",
        "C vs c — регистр имени экземпляра",
        "несогласованный стиль регистра в полях"
      ]
    },
    {
      title:"Книга и Библиотека",
      broken:
`class Book:
    def __init__(self, Title, Author):
        self.title = title
        self.author = Author
class library:
    def __init__(self)
        self.books = ()
    def Add(book):
        self.books.append(book)
    def show(self):
        For b in self.books:
            Print(b.title " — " b.author)

lib = Library()
lib.Add(Book("Война и мир" "Толстой"))
lib.show()`,
      fixed:
`class Book:
    def __init__(self, title, author):
        self.title = title
        self.author = author

class Library:
    def __init__(self):
        self.books = []
    def add(self, book):
        self.books.append(book)
    def show(self):
        for b in self.books:
            print(b.title, "—", b.author)

lib = Library()
lib.add(Book("Война и мир", "Толстой"))
lib.show()`,
      errors:[
        "title (параметр) с большой — несогласовано с self.title = title",
        "Author vs author — регистр",
        "library с маленькой — класс Library",
        "после __init__ пропущено двоеточие :",
        "self.books = () — кортеж не поддерживает .append, нужен []",
        "у Add нет self в параметрах",
        "Add с большой — лучше add (PEP8)",
        "For должно быть for",
        "Print → print",
        "в print пропущены запятые между аргументами",
        "Library с большой — а в коде объявлен library",
        "в Book(...) пропущена запятая между аргументами"
      ]
    },
    {
      title:"Работник и Менеджер (наследование)",
      broken:
`class Employee
    def __init__(self, name, salary):
        self.name = name
        self.salary == salary
    def Info():
        return self.name + " получает " + self.salary

class Manager(employee):
    def __init__(self, name, salary, team)
        super.__init__(name, salary)
        self.team = Team
    def info(self):
        return super.info() + ", команда: " + self.team

m = manager("Игорь" 1000 "Backend")
Print(m.Info())`,
      fixed:
`class Employee:
    def __init__(self, name, salary):
        self.name = name
        self.salary = salary
    def info(self):
        return self.name + " получает " + str(self.salary)

class Manager(Employee):
    def __init__(self, name, salary, team):
        super().__init__(name, salary)
        self.team = team
    def info(self):
        return super().info() + ", команда: " + self.team

m = Manager("Игорь", 1000, "Backend")
print(m.info())`,
      errors:[
        "после class Employee пропущено двоеточие :",
        "== это сравнение, нужно =",
        "у Info нет self в параметрах",
        "Info с большой — лучше info",
        "self.salary — число, нельзя складывать со строкой без str()",
        "employee с маленькой — нужен Employee",
        "после __init__ пропущено двоеточие :",
        "Team vs team — регистр",
        "super.__init__ → super().__init__()",
        "super.info() → super().info()",
        "manager с маленькой — должен быть Manager",
        "в вызове Manager пропущены запятые",
        "Print → print"
      ]
    },
    {
      title:"Фигура и Круг",
      broken:
`class Shape:
    def Area():
        Return 0

class Circle(Shape)
    def __init__(self, R):
        self.r == r
    def area():
        return 3.14 * self.R * self.R

c = circle(5)
Print(c.Area())
print(c.area + 1)`,
      fixed:
`class Shape:
    def area(self):
        return 0

class Circle(Shape):
    def __init__(self, r):
        self.r = r
    def area(self):
        return 3.14 * self.r * self.r

c = Circle(5)
print(c.area())
print(c.area() + 1)`,
      errors:[
        "Area с большой — лучше area (PEP8)",
        "у Area нет self в параметрах",
        "Return с большой",
        "после class Circle пропущено двоеточие :",
        "R vs r — регистр параметра",
        "== это сравнение, нужно =",
        "у area нет self в параметрах",
        "self.R vs self.r — регистр поля",
        "circle с маленькой — нужен Circle",
        "Print → print",
        "c.area без () — это ссылка на метод, нужен вызов",
        "несогласованный стиль имён в классе"
      ]
    },
    {
      title:"Класс Stack (стек)",
      broken:
`class Stack
    def __init__(self):
        self.items == []
    def Push(item)
        self.Items.append(item)
    def pop(self):
        Return self.items.pop(0)
    def top():
        return self.items[len(self.items)]

s = Stack()
s.Push(1)
s.push(2)
Print(s.top())
print(s.pop)`,
      fixed:
`class Stack:
    def __init__(self):
        self.items = []
    def push(self, item):
        self.items.append(item)
    def pop(self):
        return self.items.pop()
    def top(self):
        return self.items[len(self.items) - 1]

s = Stack()
s.push(1)
s.push(2)
print(s.top())
print(s.pop())`,
      errors:[
        "после class Stack пропущено двоеточие :",
        "== это сравнение, нужно =",
        "Push с большой — лучше push (PEP8)",
        "у Push нет self в параметрах",
        "после Push пропущено двоеточие :",
        "Items vs items — регистр поля",
        "Return с большой",
        "pop(0) удаляет из начала — стек удаляет с конца, нужно pop()",
        "у top нет self в параметрах",
        "self.items[len(self.items)] выйдет за границы — нужно -1",
        "s.Push — метод называется push, регистр важен",
        "s.pop без () — это ссылка на метод, нужно вызвать s.pop()"
      ]
    },
    {
      title:"Класс Queue (очередь)",
      broken:
`class Queue:
    def __init__(self):
        self.items = ()
    def Enqueue(item):
        self.items.append(item)
    def dequeue(self)
        if len(self.items) = 0:
            return None
        Return self.items.pop(0)
    def size(self):
        return self.items.lenght

q = queue()
q.Enqueue("a")
q.enqueue("b")
Print(q.size())
print(q.dequeue)`,
      fixed:
`class Queue:
    def __init__(self):
        self.items = []
    def enqueue(self, item):
        self.items.append(item)
    def dequeue(self):
        if len(self.items) == 0:
            return None
        return self.items.pop(0)
    def size(self):
        return len(self.items)

q = Queue()
q.enqueue("a")
q.enqueue("b")
print(q.size())
print(q.dequeue())`,
      errors:[
        "self.items = () — кортеж не поддерживает .append, нужен []",
        "Enqueue с большой — лучше enqueue",
        "у Enqueue нет self в параметрах",
        "после dequeue пропущено двоеточие :",
        "= вместо == в сравнении",
        "Return с большой",
        ".lenght — опечатка, и у списка нет .length, нужно len()",
        "queue с маленькой — должен быть Queue",
        "q.Enqueue — метод называется enqueue, регистр важен",
        "q.dequeue без () — это ссылка на метод",
        "несогласованный регистр имён методов"
      ]
    },
    {
      title:"Класс Vector2D",
      broken:
`class Vector2D
    def __init__(self, X, Y):
        self.x = x
        self.y == y
    def __add__(other):
        return Vector2D(self.X + other.x, self.y + other.Y)
    def __str__(self):
        Return "(" + self.x + ", " + self.y + ")"

a = vector2d(1, 2)
b = Vector2D(3 4)
c = a + b
Print(c)`,
      fixed:
`class Vector2D:
    def __init__(self, x, y):
        self.x = x
        self.y = y
    def __add__(self, other):
        return Vector2D(self.x + other.x, self.y + other.y)
    def __str__(self):
        return "(" + str(self.x) + ", " + str(self.y) + ")"

a = Vector2D(1, 2)
b = Vector2D(3, 4)
c = a + b
print(c)`,
      errors:[
        "после class Vector2D пропущено двоеточие :",
        "X vs x — регистр параметра",
        "Y vs y — регистр параметра",
        "== это сравнение, нужно =",
        "у __add__ нет self в параметрах",
        "self.X не существует (есть self.x)",
        "self.Y не существует (есть self.y)",
        "Return с большой",
        "числа нельзя складывать со строкой через + без str()",
        "vector2d с маленькой — должен быть Vector2D",
        "в Vector2D(3 4) пропущена запятая",
        "Print → print"
      ]
    },
    {
      title:"Класс Person с __str__",
      broken:
`Class Person:
    def __init__(self, name, age)
        self.Name = name
        self.age == age
    def __Str__():
        return self.name + " " + self.age

p = person("Аня" 25)
print(p)
Print(str(p))
print(p.Name)`,
      fixed:
`class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age
    def __str__(self):
        return self.name + " " + str(self.age)

p = Person("Аня", 25)
print(p)
print(str(p))
print(p.name)`,
      errors:[
        "Class с большой — должно быть class",
        "после __init__ пропущено двоеточие :",
        "Name vs name — регистр поля",
        "== это сравнение, нужно =",
        "__Str__ → __str__ (с маленькой)",
        "у __str__ нет self в параметрах",
        "число нельзя складывать со строкой без str()",
        "person с маленькой — должен быть Person",
        "в вызове Person пропущена запятая",
        "Print → print",
        "p.Name — поле name, регистр важен"
      ]
    },
    {
      title:"Класс Counter (счётчик)",
      broken:
`class Counter
    def __init__(self):
        self.count == 0
    def Increment()
        self.Count = self.count + 1
    def reset(self):
        self.count = 0
    def value():
        Return self.count

c = counter()
c.Increment()
c.increment()
Print(c.value())
print(c.Count)`,
      fixed:
`class Counter:
    def __init__(self):
        self.count = 0
    def increment(self):
        self.count = self.count + 1
    def reset(self):
        self.count = 0
    def value(self):
        return self.count

c = Counter()
c.increment()
c.increment()
print(c.value())
print(c.count)`,
      errors:[
        "после class Counter пропущено двоеточие :",
        "== это сравнение, нужно =",
        "Increment с большой — лучше increment",
        "у Increment нет self в параметрах",
        "после Increment пропущено двоеточие :",
        "Count vs count — регистр поля",
        "у value нет self в параметрах",
        "Return с большой",
        "counter с маленькой — должен быть Counter",
        "c.Increment — метод increment, регистр важен",
        "c.Count — поле count, регистр важен",
        "несогласованный регистр по всему классу"
      ]
    },
    {
      title:"Корзина покупок",
      broken:
`class Cart:
    def __init__(self):
        self.items = ()
    def Add(item, price)
        self.items.append((item, Price))
    def total(self):
        s = 0
        For it in self.items:
            s =+ it[1]
        Return s

c = cart()
c.Add("Хлеб" 50)
c.Add("Молоко" 80)
Print(c.total())`,
      fixed:
`class Cart:
    def __init__(self):
        self.items = []
    def add(self, item, price):
        self.items.append((item, price))
    def total(self):
        s = 0
        for it in self.items:
            s += it[1]
        return s

c = Cart()
c.add("Хлеб", 50)
c.add("Молоко", 80)
print(c.total())`,
      errors:[
        "self.items = () — кортеж не поддерживает .append, нужен []",
        "Add с большой — лучше add",
        "у Add нет self в параметрах",
        "после Add пропущено двоеточие :",
        "Price vs price — регистр параметра",
        "For должно быть for",
        "=+ не сложение, нужно +=",
        "Return с большой",
        "cart с маленькой — должен быть Cart",
        "в c.Add(\"Хлеб\" 50) пропущена запятая",
        "в c.Add(\"Молоко\" 80) пропущена запятая",
        "Print → print"
      ]
    },
    {
      title:"Класс TodoList",
      broken:
`class TodoList
    def __init__(self):
        self.tasks == []
    def Add(task)
        self.Tasks.append(task)
    def remove(self, task):
        if task in self.tasks
            self.tasks.remove(task)
    def show(self):
        For t in self.tasks:
            Print("- " + t)

t = todolist()
t.Add("Купить хлеб")
t.add("Сделать ДЗ")
t.remove("Купить хлеб")
t.show()`,
      fixed:
`class TodoList:
    def __init__(self):
        self.tasks = []
    def add(self, task):
        self.tasks.append(task)
    def remove(self, task):
        if task in self.tasks:
            self.tasks.remove(task)
    def show(self):
        for t in self.tasks:
            print("- " + t)

t = TodoList()
t.add("Купить хлеб")
t.add("Сделать ДЗ")
t.remove("Купить хлеб")
t.show()`,
      errors:[
        "после class TodoList пропущено двоеточие :",
        "== это сравнение, нужно =",
        "Add с большой — лучше add",
        "у Add нет self в параметрах",
        "после Add пропущено двоеточие :",
        "Tasks vs tasks — регистр поля",
        "после if пропущено двоеточие :",
        "For должно быть for",
        "Print → print",
        "todolist с маленькой — должен быть TodoList",
        "t.Add — метод add, регистр важен",
        "несогласованный регистр имён методов и полей"
      ]
    }
  ];

  const result = [];
  noOop.forEach((b, i) => {
    const idx = i + 1;
    result.push({
      id:`b${idx}`,
      title:`Программа №${idx} — ${b.title}`,
      level: idx<=5?"Новичок":idx<=10?"Средний":"Продвинутый",
      oop:false,
      broken:b.broken, fixed:b.fixed, errors:b.errors
    });
  });
  withOop.forEach((b, i) => {
    const idx = i + 1;
    result.push({
      id:`o${idx}`,
      title:`Программа №${idx} — ${b.title}`,
      level: idx<=5?"Новичок":idx<=10?"Средний":"Продвинутый",
      oop:true,
      broken:b.broken, fixed:b.fixed, errors:b.errors
    });
  });
  return result;
}

const AI_TASKS = [
  // === НОВИЧОК: Основы, переменные, ввод-вывод, типы данных ===
  {id:"ai1", title:"#1. 🧮 Переменные и типы данных", desc:"Попроси AI объяснить разницу между int, float, str и bool на простых примерах. Затем напиши программу, которая создаёт по одной переменной каждого типа и печатает её значение и тип через type().", hint:"Промт: «Объясни школьнику типы данных в Python: int, float, str, bool. Дай по 2 примера каждого и покажи, как узнать тип через type()».", level:"easy"},
  {id:"ai2", title:"#2. ⌨️ Ввод и вывод", desc:"С помощью AI напиши программу, которая спрашивает имя и возраст пользователя через input(), а затем выводит: «Привет, <имя>! Через 10 лет тебе будет <возраст+10>».", hint:"Промт: «Напиши Python-программу с input() и print(): спросить имя и возраст, вывести приветствие и возраст через 10 лет. Не забудь про int()».", level:"easy"},
  {id:"ai3", title:"#3. ❓ Условия if / elif / else", desc:"Попроси AI написать программу-калькулятор оценок: вводится число от 0 до 100, программа печатает «отлично», «хорошо», «удовлетворительно» или «плохо». Разбери с AI каждую ветку elif.", hint:"Промт: «Напиши на Python программу с if/elif/else, которая по баллу 0–100 выводит словесную оценку. Объясни каждую ветку».", level:"easy"},
  {id:"ai4", title:"#4. 🔁 Цикл for и range", desc:"Попроси AI показать 3 примера цикла for: вывести числа от 1 до 10, сумму чисел от 1 до 100, таблицу умножения на 7. Сравни решения и объясни AI, что именно делает range().", hint:"Промт: «Дай 3 примера цикла for в Python с range(). Объясни каждый шаг».", level:"easy"},
  {id:"ai5", title:"#5. 🔂 Цикл while", desc:"Сделай с помощью AI игру «угадай число от 1 до 50»: программа загадывает число, пользователь угадывает в цикле while, программа подсказывает «больше/меньше».", hint:"Промт: «Напиши на Python игру угадай число с циклом while и подсказками. Используй random.randint».", level:"easy"},

  // === НОВИЧОК → СРЕДНИЙ: строки, функции, отладка ===
  {id:"ai6", title:"#6. 🔤 Работа со строками", desc:"Попроси AI объяснить методы строк: upper(), lower(), strip(), replace(), split(), len(). Напиши программу, которая считает количество слов в предложении пользователя.", hint:"Промт: «Объясни 6 главных методов строк в Python с примерами. Затем напиши программу, считающую слова через split()».", level:"easy"},
  {id:"ai7", title:"#7. 🧩 Своя функция", desc:"Попроси AI помочь написать функцию is_even(n), которая возвращает True/False. Затем расширь: функция greet(name, age=10) с параметром по умолчанию.", hint:"Промт: «Покажи, как объявить функцию в Python с параметрами и значением по умолчанию. Напиши is_even(n) и greet(name, age=10)».", level:"easy"},
  {id:"ai8", title:"#8. 🐞 Найди ошибку в коде", desc:"Возьми любую программу из раздела «Debug Challenge». Попроси AI найти все ошибки и объяснить каждую. Сравни список AI с эталонными подсказками на странице.", hint:"Промт: «Найди все ошибки в этом Python-коде, пронумеруй и объясни, как исправить: <вставь код>».", level:"easy"},
  {id:"ai9", title:"#9. 🧠 Объясни код как ученику", desc:"Возьми любую функцию из своего кода или из урока. Попроси AI объяснить её построчно так, как будто рассказывает ученику 6 класса.", hint:"Промт: «Объясни этот Python-код построчно школьнику, простым языком: <код>».", level:"easy"},
  {id:"ai10", title:"#10. 📋 Шпаргалка по условиям и циклам", desc:"Попроси AI собрать одностраничную шпаргалку с синтаксисом if/elif/else, for, while и break/continue. Сохрани её и используй при решении задач.", hint:"Промт: «Сделай шпаргалку по if/elif/else, for, while, break, continue в Python с короткими примерами».", level:"easy"},

  // === СРЕДНИЙ: списки, словари, кортежи, множества ===
  {id:"ai11", title:"#11. 📚 Списки: задачи на AI", desc:"Попроси AI дать 5 задач на списки (append, pop, sort, max, sum). Реши их сам, потом попроси AI проверить твой код.", hint:"Промт: «Дай 5 задач на список (list) в Python для новичка. Уровень — школа. Затем проверь моё решение».", level:"mid"},
  {id:"ai12", title:"#12. 🗂️ Словари (dict)", desc:"Сделай с AI программу-телефонную книгу: добавить контакт, найти по имени, удалить, показать все. Используй dict и цикл while с меню.", hint:"Промт: «Напиши на Python телефонную книгу через dict с меню: 1-добавить, 2-найти, 3-удалить, 4-показать, 5-выйти».", level:"mid"},
  {id:"ai13", title:"#13. 🎯 Кортежи и множества", desc:"Попроси AI объяснить, чем tuple отличается от list и зачем нужен set. Напиши программу: из списка слов получить уникальные слова через set.", hint:"Промт: «Сравни list, tuple, set в Python. Покажи пример: получить уникальные слова из текста через set».", level:"mid"},
  {id:"ai14", title:"#14. 🔁 Преобразование данных", desc:"Дай AI список оценок учеников: [4,5,3,5,2,4,5]. Попроси посчитать среднее, найти максимум и количество пятёрок. Затем оформи это в функцию analyze(grades).", hint:"Промт: «Напиши функцию analyze(grades) на Python: возвращает среднее, максимум, количество пятёрок».", level:"mid"},
  {id:"ai15", title:"#15. 🔠 Манипуляции со строкой", desc:"Сделай с AI программу, которая проверяет, является ли слово палиндромом (читается одинаково в обе стороны). Подсказка: s == s[::-1].", hint:"Промт: «Напиши Python-функцию is_palindrome(s), которая возвращает True/False. Объясни срез s[::-1]».", level:"mid"},
  {id:"ai16", title:"#16. 🧮 Функции с *args и return", desc:"Попроси AI написать функцию stats(*nums), которая принимает любое количество чисел и возвращает словарь {min, max, avg, sum}.", hint:"Промт: «Покажи функцию stats(*nums) в Python: возвращает dict с min, max, avg, sum. Объясни *args».", level:"mid"},
  {id:"ai17", title:"#17. 🐞 Исправь и объясни (debug + AI)", desc:"Напиши код с ошибкой специально (например, забудь двоеточие, перепутай = и ==). Покажи AI и попроси не просто исправить, а объяснить, ПОЧЕМУ это ошибка.", hint:"Промт: «Этот код не работает. Не исправляй сразу — сначала объясни ошибку, потом покажи правильный вариант».", level:"mid"},
  {id:"ai18", title:"#18. 📂 Файлы: чтение и запись", desc:"С помощью AI напиши программу: сохранить список покупок в файл shopping.txt, потом прочитать и вывести построчно. Используй open() с 'w' и 'r'.", hint:"Промт: «Напиши на Python запись и чтение файла shopping.txt: список покупок построчно. Используй with open()».", level:"mid"},
  {id:"ai19", title:"#19. 🧪 Викторина по Python от AI", desc:"Попроси AI сгенерировать 10 вопросов с 4 вариантами ответов по темам: переменные, циклы, списки, функции. Пройди тест сам и проверь себя.", hint:"Промт: «Дай 10 вопросов с 4 вариантами ответов по Python (циклы, списки, функции). Правильные ответы укажи в конце».", level:"mid"},
  {id:"ai20", title:"#20. ⏱️ Оптимизация кода", desc:"Напиши «медленный» способ посчитать сумму квадратов чисел от 1 до 1000 через цикл. Попроси AI переписать через sum() и генератор. Сравни оба варианта.", hint:"Промт: «Перепиши этот цикл через sum() и генератор. Объясни, почему короче и быстрее: <код>».", level:"mid"},

  // === ПРОДВИНУТЫЙ: ООП, исключения, проекты ===
  {id:"ai21", title:"#21. 🏛️ Введение в ООП", desc:"Попроси AI создать класс Student с полями name, grade и методом introduce(). Создай 3 объекта-ученика и вызови у каждого introduce().", hint:"Промт: «Создай класс Student в Python с __init__ (name, grade) и методом introduce(). Покажи пример с 3 объектами».", level:"hard"},
  {id:"ai22", title:"#22. 🐾 ООП: наследование", desc:"С AI сделай базовый класс Animal с методом speak(), и наследников Dog и Cat, которые переопределяют speak(). Создай список животных и вызови speak() в цикле.", hint:"Промт: «Покажи наследование в Python: Animal → Dog, Cat. Каждый по-своему говорит. Пройдись циклом по списку».", level:"hard"},
  {id:"ai23", title:"#23. ⚠️ Обработка исключений", desc:"Сделай с AI калькулятор деления, который защищён от ZeroDivisionError и ValueError при неверном вводе. Используй try / except / else / finally.", hint:"Промт: «Напиши калькулятор деления в Python с try/except для ZeroDivisionError и ValueError. Покажи finally».", level:"hard"},
  {id:"ai24", title:"#24. 🐛 Сложный debug с AI", desc:"Возьми «продвинутый» пример из Debug Challenge (с ООП). Сначала попробуй найти ошибки сам, выпиши их. Потом сравни с тем, что найдёт AI. Что ты пропустил?", hint:"Промт: «Найди ВСЕ ошибки в этом ООП-коде Python, в том числе логические, а не только синтаксические: <код>».", level:"hard"},
  {id:"ai25", title:"#25. 🏆 Финальный мини-проект", desc:"Объедини изученное: создай программу «Дневник ученика». Класс Student, методы add_grade(), average(), сохранение/загрузка из файла, обработка ошибок. Попроси AI помочь спроектировать, но код пиши сам.", hint:"Промт: «Спроектируй класс Student: оценки (list), методы add_grade, average, save_to_file, load_from_file. Только структура, без кода — код я напишу сам».", level:"hard"}
];


const ACHIEVEMENTS = [
  {id:"first", name:"🎉 Первые шаги", desc:"Открой первую тему"},
  {id:"five", name:"🔥 На разогреве", desc:"Изучи 5 тем"},
  {id:"half", name:"🏅 Половина пути", desc:"Изучи 20 тем"},
  {id:"all", name:"👑 Мастер Python", desc:"Изучи все 40 тем"},
  {id:"quiz", name:"🧠 Эрудит", desc:"Пройди первый квиз"},
  {id:"quiz5", name:"🎓 Квиз-мастер", desc:"Пройди 5 викторин"},
  {id:"debug1", name:"🐛 Охотник за багами", desc:"Реши первый debug"},
  {id:"debug10", name:"🛠️ Инженер", desc:"Реши 10 debug-программ"},
  {id:"xp100", name:"⭐ 100 XP", desc:"Заработай 100 XP"},
  {id:"xp500", name:"🌟 500 XP", desc:"Заработай 500 XP"},
  {id:"xp1000", name:"💎 1000 XP", desc:"Заработай 1000 XP"}
];

const STATE_KEY = "pyacademy_state_v1";
const state = loadState();
function loadState(){
  try { return JSON.parse(localStorage.getItem(STATE_KEY)) || initState(); }
  catch { return initState(); }
}
function initState(){ return {xp:0, done:{}, quizDone:{}, debugDone:{}, aiDone:{}, achievements:{}, theme:"light", profile:null}; }
function saveState(){ localStorage.setItem(STATE_KEY, JSON.stringify(state)); }

// Real-time profile refresh bus — other modules register via window.PA_onStateChange
function notifyStateChange(){
  try{ if (typeof window.PA_refreshProfile === 'function') window.PA_refreshProfile(); }catch(e){}
}

function addXP(n){
  state.xp += n;
  if (state.xp >= 100) unlock("xp100");
  if (state.xp >= 500) unlock("xp500");
  if (state.xp >= 1000) unlock("xp1000");
  saveState(); renderTop(); notifyStateChange();
}
function markDone(id){ state.done[id] = true; saveState(); renderTop(); renderSidebar(); notifyStateChange(); }
function unlock(id){
  if (state.achievements[id]) return;
  state.achievements[id] = true; saveState();
  toast(`🏆 Достижение: ${ACHIEVEMENTS.find(a=>a.id===id)?.name||id}`);
  notifyStateChange();
}

const KEYWORDS = /\b(False|None|True|and|as|assert|async|await|break|class|continue|def|del|elif|else|except|finally|for|from|global|if|import|in|is|lambda|nonlocal|not|or|pass|raise|return|try|while|with|yield|self)\b/g;
const BUILTINS = /\b(print|len|range|int|str|float|bool|list|dict|set|tuple|input|open|map|filter|reduce|sorted|sum|min|max|abs|round|type|isinstance|enumerate|zip|any|all)\b/g;
function highlight(code){
  return code
    .replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;")
    .replace(KEYWORDS,'<span class="tok-key">$1</span>')
    .replace(BUILTINS,'<span class="tok-fn">$1</span>')
    .replace(/\b(\d+\.?\d*)\b/g,'<span class="tok-num">$1</span>');
}

let currentView = {type:"home"};
let searchQuery = "";
let levelFilter = "all";

function $(sel){ return document.querySelector(sel); }
function el(tag, props={}, ...children){
  const e = document.createElement(tag);
  Object.entries(props).forEach(([k,v])=>{
    if (k==="class") e.className = v;
    else if (k==="html") e.innerHTML = v;
    else if (k.startsWith("on")) e.addEventListener(k.slice(2).toLowerCase(), v);
    else e.setAttribute(k, v);
  });
  children.flat().forEach(c => e.appendChild(typeof c==="string" ? document.createTextNode(c) : c));
  return e;
}

function toast(msg){
  const t = el("div", {class:"toast"}, msg);
  document.body.appendChild(t);
  setTimeout(()=>t.remove(), 2800);
}

function renderTop(){
  const total = TOPICS.length;
  const doneCount = Object.keys(state.done).filter(k=>k.startsWith("t")).length;
  const pct = Math.round((doneCount/total)*100);
  const bar = $("#progress-bar"); if (bar) bar.style.width = pct + "%";
  const xp = $("#xp-val"); if (xp) xp.textContent = state.xp;
  const pctEl = $("#pct"); if (pctEl) pctEl.textContent = pct + "%";
}

function renderSidebar(){
  const groups = {};
  TOPICS.filter(t=>{
    if (levelFilter!=="all" && t.level!==levelFilter) return false;
    if (searchQuery && !(t.title.toLowerCase().includes(searchQuery)||t.intro.toLowerCase().includes(searchQuery))) return false;
    return true;
  }).forEach(t=>{
    groups[t.group] = groups[t.group] || [];
    groups[t.group].push(t);
  });
  const container = $("#nav-list");
  container.innerHTML = "";
  const home = el("div", {class:"nav-item"+(currentView.type==="home"?" active":""), onclick:()=>navigate({type:"home"})},
    el("div",{class:"nav-icon"},"🏠"), el("span",{},"Главная"));
  const debugItem = el("div", {class:"nav-item"+(currentView.type==="debug"?" active":""), onclick:()=>navigate({type:"debug"})},
    el("div",{class:"nav-icon"},"🐛"), el("span",{},"Debug Challenge"));
  const aiItem = el("div", {class:"nav-item"+(currentView.type==="ai"?" active":""), onclick:()=>navigate({type:"ai"})},
    el("div",{class:"nav-icon"},"🤖"), el("span",{},"AI Challenge"));
  const achItem = el("div", {class:"nav-item"+(currentView.type==="ach"?" active":""), onclick:()=>navigate({type:"ach"})},
    el("div",{class:"nav-icon"},"🏆"), el("span",{},"Достижения"));
  container.append(home, debugItem, aiItem, achItem);
  Object.entries(groups).forEach(([g, items])=>{
    container.appendChild(el("div",{class:"nav-section"}, g));
    items.forEach(t=>{
      const item = el("div", {
        class:"nav-item"+(currentView.type==="topic"&&currentView.id===t.id?" active":"")+(state.done["t"+t.id]?" done":""),
        onclick:()=>navigate({type:"topic", id:t.id})
      }, el("div",{class:"nav-icon"}, t.emoji), el("span",{}, t.title));
      container.appendChild(item);
    });
  });
}

function navigate(v){ currentView = v; render(); }

function render(){
  renderSidebar(); renderTop();
  const main = $("#main");
  main.innerHTML = "";
  main.classList.remove("fade-in"); void main.offsetWidth; main.classList.add("fade-in");
  if (currentView.type==="home") renderHome(main);
  else if (currentView.type==="topic") renderTopic(main, TOPICS.find(t=>t.id===currentView.id));
  else if (currentView.type==="debug") renderDebug(main);
  else if (currentView.type==="ai") renderAI(main);
  else if (currentView.type==="ach") renderAch(main);
}

function renderHome(main){
  main.append(
    el("div",{class:"hero"},
      el("div",{class:"hero-emoji"},"🐍"),
      el("h1",{},"Добро пожаловать в Python Academy!"),
      el("p",{},"Интерактивная платформа для изучения Python. 40 тем, 600+ примеров, 30 debug-задач, AI-челлендж, викторины и достижения. Учись, играй, набирай XP!")
    )
  );
  const cards = el("div",{class:"cards"});
  TOPICS.forEach(t=>{
    const c = el("div",{class:"card"+(state.done["t"+t.id]?" done":""), onclick:()=>navigate({type:"topic",id:t.id})},
      el("div",{class:"emoji"}, t.emoji),
      el("h3",{}, t.title),
      el("div",{class:"meta"}, `${t.group} · `, el("span",{class:"level-"+(t.level==="easy"?"easy":t.level==="mid"?"mid":"hard")}, t.level==="easy"?"новичок":t.level==="mid"?"средний":"продвинутый"))
    );
    cards.appendChild(c);
  });
  if (state.profile){
    main.append(el("div",{class:"panel",style:"margin:18px 40px"},
      el("h3",{},"👤 Профиль ученика"),
      el("p",{}, "Имя: " + state.profile.fullName),
      el("p",{}, "Класс: " + state.profile.className),
      el("p",{class:"muted"}, "XP: " + state.xp + " · Изучено тем: " + Object.keys(state.done).filter(k=>k.startsWith("t")).length + "/" + TOPICS.length)
    ));
  }
  main.append(el("h2",{},"📚 Темы курса"), cards);
}

function renderTopic(main, topic){
  if (!topic) return;
  unlock("first");
  const doneCount = Object.keys(state.done).filter(k=>k.startsWith("t")).length;
  if (doneCount >= 5) unlock("five");
  if (doneCount >= 20) unlock("half");
  if (doneCount >= 40) unlock("all");

  main.append(
    el("div",{class:"hero"},
      el("div",{class:"hero-emoji"}, topic.emoji),
      el("h1",{}, topic.title),
      el("p",{}, `Группа: ${topic.group} · Уровень: ${topic.level==="easy"?"новичок":topic.level==="mid"?"средний":"продвинутый"}`)
    )
  );
  const tabs = el("div",{class:"tabs"});
  const tabNames = [["intro","📖 Объяснение"],["examples","💻 Примеры (15)"],["tasks","✏️ Задания"],["quiz","🧠 Викторина"]];
  const contents = {};
  tabNames.forEach(([key,label])=>{
    const t = el("div",{class:"tab"+(key==="intro"?" active":""), onclick:()=>{
      tabs.querySelectorAll(".tab").forEach(x=>x.classList.remove("active"));
      t.classList.add("active");
      Object.values(contents).forEach(c=>c.style.display="none");
      contents[key].style.display="block";
    }}, label);
    tabs.appendChild(t);
  });
  main.appendChild(tabs);
  const wrap = el("div",{}); main.appendChild(wrap);

  contents.intro = el("div",{},
    el("div",{class:"panel"},
      el("h3",{},"Простое объяснение"),
      el("p",{}, topic.intro),
      el("h3",{},"Синтаксис"),
      codeBlock(topic.syntax),
      el("p",{class:"muted"}, "💡 " + topic.syntaxNote),
      el("button",{class:"btn-primary", onclick:()=>{
        if (!state.done["t"+topic.id]) addXP(20);
        markDone("t"+topic.id);
        toast("✅ Тема отмечена как изученная (+20 XP)");
      }},"Отметить как изученную")
    )
  );

  const examples = makeExamples(topic);
  contents.examples = el("div",{});
  examples.forEach((ex,i)=>{
    const out = el("div",{class:"run-out"}, "(нажмите «Показать вывод»)");
    const codeArea = el("textarea",{class:"editor"});
    codeArea.value = ex.code;
    const ex_el = el("div",{class:"example"},
      el("h3",{}, `#${i+1}. ${ex.title} `, el("span",{class:"badge level-"+(ex.level==="Новичок"?"easy":ex.level==="Средний"?"mid":"hard")}, ex.level)),
      el("p",{class:"muted"}, ex.explanation),
      codeArea,
      el("div",{class:"code-actions"},
        el("button",{class:"btn-primary",onclick:()=>{out.textContent = "▶ Ожидаемый вывод:\n" + ex.output;}},"▶ Показать вывод"),
        el("button",{class:"btn-ghost",onclick:()=>{navigator.clipboard.writeText(codeArea.value);toast("📋 Скопировано!")}},"📋 Копировать"),
        el("button",{class:"btn-ghost",onclick:()=>{codeArea.value=ex.code;out.textContent="(нажмите «Показать вывод»)"}},"🔄 Сброс"),
        el("button",{class:"btn-accent",onclick:()=>{out.textContent="✅ Эталонный вывод:\n"+ex.output}},"👁 Решение")
      ),
      out,
      el("p",{class:"muted",style:"margin-top:8px;font-size:12px"}, "💡 Python запускается интерпретатором — сохрани файл .py и запусти: python file.py")
    );
    contents.examples.appendChild(ex_el);
  });

  contents.tasks = el("div",{});
  makeTasks(topic).forEach((tk,i)=>{
    const sol = el("div",{class:"codebox",style:"display:none;margin-top:8px"}, el("pre",{html:highlight(tk.solution)}));
    const hint = el("p",{class:"muted",style:"display:none"}, "💡 " + tk.hint);
    contents.tasks.appendChild(
      el("div",{class:"panel"},
        el("h3",{}, `Задание ${i+1}`),
        el("p",{}, tk.q),
        el("div",{class:"code-actions"},
          el("button",{class:"btn-ghost",onclick:()=>hint.style.display=hint.style.display==="none"?"block":"none"},"💡 Подсказка"),
          el("button",{class:"btn-accent",onclick:()=>sol.style.display=sol.style.display==="none"?"block":"none"},"✅ Решение")
        ),
        hint, sol
      )
    );
  });

  const quiz = makeQuiz(topic);
  contents.quiz = el("div",{});
  let score = 0, answered = 0;
  const scoreEl = el("h3",{}, `Счёт: 0 / ${quiz.length}`);
  contents.quiz.appendChild(scoreEl);
  quiz.forEach((q,qi)=>{
    const opts = el("div",{class:"opts"});
    q.opts.forEach((opt,oi)=>{
      const o = el("div",{class:"opt", onclick:()=>{
        if (o.dataset.locked) return;
        opts.querySelectorAll(".opt").forEach(x=>x.dataset.locked="1");
        if (oi===q.answer){ o.classList.add("correct"); score++; }
        else { o.classList.add("wrong"); opts.children[q.answer].classList.add("correct"); }
        answered++;
        scoreEl.textContent = `Счёт: ${score} / ${quiz.length}`;
        if (answered===quiz.length){
          const isFirstCompletion = !state.quizDone["q"+topic.id];
          // Mark quizDone BEFORE calling addXP/notifyStateChange so the profile
          // card reads the updated quizDone count when it refreshes
          if (isFirstCompletion) {
            state.quizDone["q"+topic.id] = true;
            saveState();
            addXP(30); // internally calls notifyStateChange() -> profile updates with correct quizDone
            unlock("quiz");
            const quizCount = Object.keys(state.quizDone).length;
            if (quizCount >= 5) unlock("quiz5");
          } else {
            // Quiz already completed before — still refresh profile to stay in sync
            notifyStateChange();
          }
          toast(`Квиз пройден! ${score}/${quiz.length}`);
        }
      }}, opt);
      opts.appendChild(o);
    });
    contents.quiz.appendChild(el("div",{class:"quiz-q"}, el("strong",{},`${qi+1}. ${q.q}`), opts));
  });
  Object.entries(contents).forEach(([k,v])=>{ if (k!=="intro") v.style.display="none"; wrap.appendChild(v); });
}

function renderDebug(main){
  const bugs = window.__BUGS__ || (window.__BUGS__ = makeBugs());
  const doneN = Object.keys(state.debugDone).length;
  main.append(
    el("div",{class:"hero"},
      el("div",{class:"hero-emoji"},"🐛"),
      el("h1",{},"Debug Challenge — Охота на ошибки"),
      el("p",{},`30 сломанных Python-программ (15 без ООП + 15 с ООП). В каждой минимум 10 ошибок. Найди и исправь их! Решено: ${doneN}/30`)
    )
  );
  bugs.forEach(b=>{
    const editor = el("textarea",{class:"editor",style:"min-height:260px"});
    editor.value = b.broken;
    const errList = el("ol",{style:"display:none"});
    b.errors.forEach(e=>errList.appendChild(el("li",{},e)));
    const fixedBox = el("div",{class:"codebox",style:"display:none;margin-top:8px"}, el("pre",{html:highlight(b.fixed)}));
    main.appendChild(el("div",{class:"panel"},
      el("h3",{}, `${b.title} `, el("span",{class:"badge level-"+(b.level==="Новичок"?"easy":b.level==="Средний"?"mid":"hard")}, b.level), " ", el("span",{class:"badge"}, b.oop?"🏛️ ООП":"📝 без ООП"), " ", el("span",{class:"badge"}, `найди ${b.errors.length}+ ошибок`)),
      editor,
      el("div",{class:"code-actions"},
        el("button",{class:"btn-ghost",onclick:()=>{
          errList.style.display = errList.style.display==="none"?"block":"none";
        }},"💡 Подсказки (список ошибок)"),
        el("button",{class:"btn-accent",onclick:()=>{
          fixedBox.style.display = fixedBox.style.display==="none"?"block":"none";
        }},"✅ Показать решение"),
        el("button",{class:"btn-primary",onclick:()=>{
          if (!state.debugDone[b.id]) { addXP(50); state.debugDone[b.id]=true; saveState();
            const n = Object.keys(state.debugDone).length;
            unlock("debug1"); if (n>=10) unlock("debug10");
            toast(`🐛 Программа решена! +50 XP (${n}/30)`);
            render();
          }
        }},"✔ Отметить решённой"+(state.debugDone[b.id]?" ✓":""))
      ),
      el("p",{class:"muted",style:"margin-top:8px"}, "Подсказки (нажми кнопку выше):"),
      errList,
      fixedBox
    ));
  });
}

function renderAI(main){
  const doneN = Object.keys(state.aiDone||{}).length;
  main.append(
    el("div",{class:"hero"},
      el("div",{class:"hero-emoji"},"🤖"),
      el("h1",{},"AI Challenge — Творческие задачи с искусственным интеллектом"),
      el("p",{},`${AI_TASKS.length} креативных задач: используй любой AI-чат (ChatGPT, Claude, Lovable AI и т.д.), чтобы их выполнить. Решено: ${doneN}/${AI_TASKS.length}`)
    )
  );
  AI_TASKS.forEach(t=>{
    const hint = el("p",{class:"muted",style:"display:none;margin-top:8px"}, "💡 " + t.hint);
    main.appendChild(el("div",{class:"panel"},
      el("h3",{}, t.title, " ",
        el("span",{class:"badge level-"+(t.level==="easy"?"easy":t.level==="mid"?"mid":"hard")}, t.level==="easy"?"новичок":t.level==="mid"?"средний":"продвинутый")
      ),
      el("p",{}, t.desc),
      el("div",{class:"code-actions"},
        el("button",{class:"btn-ghost",onclick:()=>hint.style.display=hint.style.display==="none"?"block":"none"},"💡 Подсказка / промт"),
        el("button",{class:"btn-primary",onclick:()=>{
          state.aiDone = state.aiDone || {};
          if (!state.aiDone[t.id]) { state.aiDone[t.id]=true; addXP(40); saveState();
            toast(`🤖 Задача выполнена! +40 XP`);
            render();
          }
        }},"✔ Отметить выполненной"+((state.aiDone||{})[t.id]?" ✓":""))
      ),
      hint
    ));
  });
}

function renderAch(main){
  main.append(
    el("div",{class:"hero"},
      el("div",{class:"hero-emoji"},"🏆"),
      el("h1",{},"Достижения"),
      el("p",{},`Открыто: ${Object.keys(state.achievements).length} / ${ACHIEVEMENTS.length}`)
    )
  );
  const wrap = el("div",{class:"achievements"});
  ACHIEVEMENTS.forEach(a=>{
    wrap.appendChild(el("div",{class:"ach"+(state.achievements[a.id]?" unlocked":"")},
      el("div",{}, el("strong",{},a.name)),
      el("div",{class:"muted"}, a.desc)
    ));
  });
  main.appendChild(wrap);
}

function codeBlock(code){
  const box = el("div",{class:"codebox"},
    el("pre",{html: highlight(code)})
  );
  const btn = el("button",{class:"btn-ghost",style:"margin-top:8px",onclick:()=>{navigator.clipboard.writeText(code);toast("📋 Скопировано!");}},"📋 Копировать");
  return el("div",{}, box, btn);
}

window.addEventListener("DOMContentLoaded", ()=>{
  const _initTheme = state.theme || "light";
  document.documentElement.className = _initTheme === "light" ? "light" : "";
  document.documentElement.setAttribute("data-theme", _initTheme);
  $("#search").addEventListener("input", e=>{ searchQuery = e.target.value.toLowerCase(); renderSidebar(); });
  document.querySelectorAll(".chip").forEach(c=>{
    c.addEventListener("click", ()=>{
      document.querySelectorAll(".chip").forEach(x=>x.classList.remove("active"));
      c.classList.add("active"); levelFilter = c.dataset.level; renderSidebar();
    });
  });
  $("#theme-toggle").addEventListener("click", ()=>{
    state.theme = state.theme==="light" ? "dark":"light"; saveState();
    document.documentElement.className = state.theme==="light"?"light":"";
    document.documentElement.setAttribute("data-theme", state.theme);
  });
  $("#reset").addEventListener("click", ()=>{
    if (confirm("Сбросить весь прогресс?")) {
      localStorage.removeItem(STATE_KEY); location.reload();
    }
  });
  const _eb = document.getElementById("edit-profile");
  if (_eb) _eb.addEventListener("click", ()=>showProfileModal(true));
  if (!state.profile) showProfileModal(false); else updateProfileBadge();
  render();
});

function updateProfileBadge(){
  const b = document.getElementById("profile-badge");
  if (!b) return;
  if (state.profile) b.textContent = "👤 " + state.profile.fullName + " · " + state.profile.className;
  else b.textContent = "👤 Гость";
}
function showProfileModal(canCancel){
  const back = el("div",{class:"modal-back"});
  const nameI = el("input",{class:"search",style:"margin:8px 0",placeholder:"Имя и фамилия"});
  const classI = el("input",{class:"search",style:"margin:8px 0",placeholder:"Класс (например, 6А)"});
  if (state.profile){ nameI.value = state.profile.fullName||""; classI.value = state.profile.className||""; }
  const err = el("div",{class:"muted",style:"color:#e25;font-size:12px;min-height:16px"});
  const save = el("button",{class:"btn-primary",style:"margin-right:8px",onclick:()=>{
    const n = nameI.value.trim(), c = classI.value.trim();
    if (n.length<2 || c.length<1){ err.textContent="Заполни оба поля"; return; }
    state.profile = {fullName:n, className:c}; saveState();
    updateProfileBadge(); back.remove(); render();
  }},"Сохранить");
  const cancel = canCancel ? el("button",{class:"btn-ghost",onclick:()=>back.remove()},"Отмена") : document.createTextNode("");
  const box = el("div",{class:"modal"},
    el("h3",{},"👤 Профиль ученика"),
    el("p",{class:"muted"},"Введи имя и класс — прогресс сохранится вместе с профилем."),
    nameI, classI, err,
    el("div",{style:"margin-top:8px"}, save, cancel)
  );
  back.appendChild(box);
  document.body.appendChild(back);
  setTimeout(()=>nameI.focus(),50);
}
