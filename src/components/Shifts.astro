---
import { mainShifts, shortShifts, allShifts } from '../data/shifts';
import ShiftCard from './shifts/ShiftCard.astro';
import ShortShiftCard from './shifts/ShortShiftCard.astro';
import ShiftBookModal from './shifts/ShiftBookModal.astro';
import ShiftCalendarModal from './shifts/ShiftCalendarModal.astro';
import ShiftInfoModal from './shifts/ShiftInfoModal.astro';
---

<section id="shifts" class="mx-auto max-w-[1312px] px-[2px] pt-10 md:px-4">
  <div class="p-[18px] rounded-[24px] bg-[linear-gradient(145deg,#dbeafe_0%,#eff6ff_45%,#dbeafe_100%)]">
    <div class="flex items-center gap-2 mb-1"><div class="w-1 h-5 rounded-full bg-orange-500 shrink-0"></div><h2 class="text-[22px] font-bold leading-[1.1] tracking-[-0.02em] text-slate-900 md:text-[28px]">Смены</h2></div>

    <!-- Мерч за друга — компактная строка, разворачивается по клику -->
    <div class="mt-3 rounded-xl border border-emerald-100 bg-emerald-50 overflow-hidden" data-toggle-root>
      <button type="button" data-toggle aria-expanded="false" class="flex w-full cursor-pointer items-center gap-2.5 px-4 py-2.5 select-none">
        <i class="bi bi-people-fill text-[14px] text-emerald-600 shrink-0" aria-hidden="true"></i>
        <span class="flex-1 text-[13px] font-semibold text-slate-700">Едете с другом? Здесь скидки и мерч</span>
        <i class="bi bi-chevron-down text-[12px] text-slate-400 transition-transform duration-200" aria-hidden="true" id="shifts-friend-chevron"></i>
      </button>
      <div data-toggle-panel class="hidden flex items-stretch gap-0 border-t border-emerald-100">
        <button
          type="button"
          data-gallery-open
          data-gallery-src="/images/merch-hoodie.avif"
          data-gallery-alt="Фирменная толстовка АйДаКемп"
          class="shrink-0 cursor-zoom-in self-stretch"
          aria-label="Посмотреть мерч"
        >
          <img
            src="/images/merch-hoodie.avif"
            alt="Мерч АйДаКемп"
            class="h-full w-[96px] object-cover object-top transition hover:scale-105"
            loading="lazy"
          />
        </button>
        <div class="flex-1 px-4 py-3">
          <p class="text-[14px] font-semibold text-slate-800">Те, кто едет в компании, адаптируются вдвое быстрее</p>
          <p class="text-[13px] text-slate-500 mt-0.5">Позовите друга — подарим фирменный мерч каждому</p>
          <div class="mt-1.5 flex flex-wrap gap-1.5">
            <span class="inline-block rounded-full bg-emerald-100 px-2.5 py-0.5 text-[12px] font-semibold text-emerald-700">Каждая 5-я — скидка 50%</span>
            <span class="inline-block rounded-full bg-orange-100 px-2.5 py-0.5 text-[12px] font-semibold text-orange-700">Каждая 10-я — за 100 ₽</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Mobile: tab nav + horizontal carousel -->
    <div class="mt-4 md:hidden">
      <div class="flex gap-2 pb-2" data-shifts-tabs>
        {allShifts.map((shift, i) => {
          const num = shift.name.replace('Смена ', '');
          return (
            <button
              type="button"
              data-shifts-tab={i}
              class={`flex h-11 w-11 shrink-0 items-center justify-center rounded-full text-[14px] font-semibold transition-colors ${i === 0 ? 'bg-primary-light text-white' : 'bg-slate-100 text-slate-700'}`}
            >
              {num}
            </button>
          );
        })}
      </div>

      <div class="mt-3 flex snap-x snap-mandatory gap-3 overflow-x-auto pb-2 [scrollbar-width:none] [-ms-overflow-style:none] [&::-webkit-scrollbar]:hidden" data-shifts-carousel>
        {allShifts.map((shift) => (
          <ShiftCard shift={shift} variant="mobile" />
        ))}
      </div>
      <!-- Dots indicator (P1 2026-04-20: 6px → 8px + active pill) -->
      <div class="mt-4 flex justify-center items-center gap-2 md:hidden" data-shifts-dots role="group" aria-label="Навигация по сменам">
        {allShifts.map((_, i) => (
          <span
            class={`h-2 rounded-full transition-all duration-200 ${i === 0 ? 'w-6 bg-[#c45f00]' : 'w-2 bg-slate-300'}`}
            data-shifts-dot={i}
            aria-current={i === 0 ? 'true' : 'false'}
          ></span>
        ))}
      </div>
      <!-- Подсказка свайпа — только для первого визита -->
      <p class="mt-2 text-center text-[12px] text-slate-500 md:hidden transition-opacity duration-300" data-shifts-swipe-hint>
        ← свайп, чтобы увидеть все {allShifts.length} смен →
      </p>
      <!-- July sold-out banner (mobile) -->
      <div class="mt-4 flex items-center gap-2.5 rounded-xl border border-red-100 bg-red-50 px-4 py-2.5">
        <i class="bi bi-lock-fill text-[15px] text-red-500 shrink-0"></i>
        <p class="text-[13px] font-semibold leading-[1.4] text-red-700">Июль — мест нет. Свободные места — в августе (Смена 3 и 4).</p>
      </div>
    </div>

    <!-- Desktop: 2-col grid + short shifts sidebar -->
    <div class="mt-6 hidden md:flex md:gap-6">
      <!-- Left: Main shifts (2 col grid) -->
      <div class="grid flex-1 grid-cols-2 gap-4">
        {mainShifts.slice(0, 2).map((shift) => (
          <ShiftCard shift={shift} variant="desktop" />
        ))}
        <!-- July sold-out banner -->
        <div class="col-span-2 flex items-center gap-3 rounded-xl border border-red-100 bg-red-50 px-4 py-2.5">
          <i class="bi bi-lock-fill text-[16px] text-red-500 shrink-0"></i>
          <p class="text-[14px] font-semibold text-red-700">Июль — мест нет. Все июльские смены укомплектованы.</p>
          <span class="ml-auto text-[13px] text-red-400 whitespace-nowrap">Записаться в август →</span>
        </div>
        {mainShifts.slice(2).map((shift) => (
          <ShiftCard shift={shift} variant="desktop" />
        ))}
      </div>

      <!-- Right: Short shifts -->
      <div class="w-[320px] shrink-0">
        <div class="rounded-[20px] border border-slate-200 bg-slate-50 p-5">
          <p class="text-[14px] font-semibold uppercase tracking-[0.08em] text-[#2fa768]">Короткие смены</p>
          <p class="text-[14px] text-slate-600 mt-2 mb-1">Для тех, кто сомневается ехать ли надолго</p>
          <div class="mt-3 grid gap-3">
            {shortShifts.map((shift) => (
              <ShortShiftCard shift={shift} />
            ))}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<ShiftBookModal />
<ShiftCalendarModal />
<ShiftInfoModal />

<script>
  import { STORAGE_KEYS } from '../lib/storage';
  import { shiftInfo } from '../data/shifts';
  import { renderCalendar } from '../scripts/shift-calendar';

  function initShifts() {
    // === FRIEND DISCOUNT TOGGLE ===
    document.querySelectorAll<HTMLElement>('[data-toggle-root]').forEach((root) => {
      const btn = root.querySelector<HTMLButtonElement>('[data-toggle]');
      const panel = root.querySelector<HTMLElement>('[data-toggle-panel]');
      if (!btn || !panel) return;
      btn.addEventListener('click', () => {
        const expanded = btn.getAttribute('aria-expanded') === 'true';
        btn.setAttribute('aria-expanded', expanded ? 'false' : 'true');
        panel.classList.toggle('hidden', expanded);
        const chevron = btn.querySelector<HTMLElement>('i[id="shifts-friend-chevron"]') || document.getElementById('shifts-friend-chevron');
        if (chevron) chevron.style.transform = expanded ? '' : 'rotate(180deg)';
      });
    });

    // === MOBILE CAROUSEL TABS ===
    const carousel = document.querySelector<HTMLElement>('[data-shifts-carousel]');
    const tabs = document.querySelectorAll<HTMLButtonElement>('[data-shifts-tab]');
    const dots = document.querySelectorAll<HTMLElement>('[data-shifts-dot]');

    if (carousel && tabs.length) {
      const cards = Array.from(carousel.children) as HTMLElement[];

      tabs.forEach((tab) => {
        tab.addEventListener('click', () => {
          const idx = parseInt(tab.dataset.shiftsTab || '0', 10);
          cards[idx]?.scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'start' });
        });
      });

      // Swipe hint: скрыть если уже свайпали раньше
      const swipeHint = document.querySelector<HTMLElement>('[data-shifts-swipe-hint]');
      if (swipeHint && localStorage.getItem(STORAGE_KEYS.shiftsSwiped)) {
        swipeHint.style.display = 'none';
      }

      const observer = new IntersectionObserver((entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            const idx = cards.indexOf(entry.target as HTMLElement);
            tabs.forEach((t, ti) => {
              t.classList.toggle('bg-primary-light', ti === idx);
              t.classList.toggle('text-white', ti === idx);
              t.classList.toggle('bg-slate-100', ti !== idx);
              t.classList.toggle('text-slate-700', ti !== idx);
            });
            dots.forEach((d, di) => {
              const isActive = di === idx;
              d.classList.toggle('bg-[#c45f00]', isActive);
              d.classList.toggle('w-6', isActive);
              d.classList.toggle('bg-slate-300', !isActive);
              d.classList.toggle('w-2', !isActive);
              d.setAttribute('aria-current', isActive ? 'true' : 'false');
            });
            if (idx > 0 && swipeHint) {
              swipeHint.classList.add('opacity-0');
              localStorage.setItem(STORAGE_KEYS.shiftsSwiped, '1');
            }
          }
        });
      }, { root: carousel, threshold: 0.6 });

      cards.forEach((card) => observer.observe(card));
    }

    // === BOOKING MODAL ===
    const bookModal = document.getElementById('shiftBookModal') as HTMLDialogElement | null;
    if (!bookModal) return;

    const bookShiftBlock = bookModal.querySelector<HTMLElement>('[data-book-shift-block]');
    const bookName = bookModal.querySelector('[data-book-shift-name]');
    const bookDates = bookModal.querySelector('[data-book-shift-dates]');
    const bookDuration = bookModal.querySelector('[data-book-shift-duration]');
    const bookPrice = bookModal.querySelector('[data-book-shift-price]');
    const bookPhone = bookModal.querySelector<HTMLInputElement>('[data-book-phone]');
    const bookConsent = bookModal.querySelector<HTMLInputElement>('[data-book-consent]');
    const bookSubmit = bookModal.querySelector<HTMLButtonElement>('[data-book-submit]');
    const bookError = bookModal.querySelector<HTMLElement>('[data-book-error]');
    const ageBtns = bookModal.querySelectorAll<HTMLButtonElement>('[data-book-age]');

    let selectedAge = '10-12';

    ageBtns.forEach((btn) => {
      btn.addEventListener('click', () => {
        ageBtns.forEach((b) => {
          b.classList.remove('bg-primary', 'text-[#1e2430]', 'font-semibold');
          b.classList.add('border', 'border-slate-200', 'text-slate-700', 'font-medium');
        });
        btn.classList.remove('border', 'border-slate-200', 'text-slate-700', 'font-medium');
        btn.classList.add('bg-primary', 'text-[#1e2430]', 'font-semibold');
        selectedAge = btn.dataset.bookAge || '10-12';
      });
    });

    if (bookPhone) {
      bookPhone.addEventListener('input', () => {
        let digits = bookPhone.value.replace(/\D/g, '');
        if (digits.startsWith('8')) digits = '7' + digits.slice(1);
        if (!digits.startsWith('7')) digits = '7' + digits;
        digits = digits.slice(0, 11);

        let formatted = '+7';
        if (digits.length > 1) formatted += ' (' + digits.slice(1, 4);
        if (digits.length >= 4) formatted += ') ' + digits.slice(4, 7);
        if (digits.length >= 7) formatted += '-' + digits.slice(7, 9);
        if (digits.length >= 9) formatted += '-' + digits.slice(9, 11);

        bookPhone.value = formatted;
        validateBookForm();
      });
    }

    function isPhoneValid() {
      const digits = (bookPhone?.value || '').replace(/\D/g, '');
      return digits.length === 11;
    }

    function validateBookForm() {
      const valid = isPhoneValid() && (bookConsent?.checked ?? false);
      if (bookSubmit) {
        bookSubmit.disabled = !valid;
        bookSubmit.classList.toggle('opacity-50', !valid);
      }
    }

    bookConsent?.addEventListener('change', validateBookForm);

    document.querySelectorAll('[data-shift-book]').forEach((btn) => {
      btn.addEventListener('click', () => {
        const el = btn as HTMLElement;
        const name = el.dataset.shiftBookName || '';
        if (bookName) bookName.textContent = name;
        if (bookDates) bookDates.textContent = el.dataset.shiftBookDates || '';
        if (bookDuration) bookDuration.textContent = el.dataset.shiftBookDuration || '';
        if (bookPrice) bookPrice.textContent = el.dataset.shiftBookPrice || '';
        if (bookError) bookError.classList.add('hidden');
        // Показываем блок с данными о смене только если есть название
        if (bookShiftBlock) bookShiftBlock.classList.toggle('hidden', !name);
        bookModal.showModal();
        (window as any).trackGoal?.('shift_book_click', { shift: name }, 500);
      });
    });

    bookModal.querySelector('[data-shift-book-close]')?.addEventListener('click', () => bookModal.close());
    bookModal.addEventListener('click', (e) => { if (e.target === bookModal) bookModal.close(); });

    bookSubmit?.addEventListener('click', async () => {
      if (!isPhoneValid()) {
        if (bookError) {
          bookError.textContent = 'Введите корректный номер телефона';
          bookError.classList.remove('hidden');
        }
        return;
      }
      if (!bookConsent?.checked) {
        if (bookError) {
          bookError.textContent = 'Необходимо согласие на обработку данных';
          bookError.classList.remove('hidden');
        }
        return;
      }

      try {
        const res = await fetch('/api/lead', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            phone: bookPhone?.value || '',
            age: selectedAge,
            shift: bookName?.textContent || '',
            source: new URLSearchParams(window.location.search).get('utm_source') || '',
          }),
        });
        await res.json();
        (window as any).trackGoal?.('lead', {}, 6375);
      } catch {}

      bookModal.close();
      const referralModal = document.getElementById('referralModal') as HTMLDialogElement | null;
      referralModal?.showModal();
    });

    // === CALENDAR MODAL ===
    const calendarModal = document.getElementById('shiftCalendarModal') as HTMLDialogElement | null;
    if (calendarModal) {
      const calendarGrid = calendarModal.querySelector('[data-calendar-grid]');
      const calendarTitle = calendarModal.querySelector('[data-calendar-title]');

      document.querySelectorAll<HTMLElement>('[data-shift-calendar]').forEach((btn) => {
        btn.addEventListener('click', () => {
          const startStr = btn.dataset.start || '';
          const endStr = btn.dataset.end || '';
          const shiftName = btn.dataset.shiftName || '';
          if (calendarGrid && calendarTitle) {
            renderCalendar(startStr, endStr, shiftName, calendarGrid, calendarTitle);
          }
          calendarModal.showModal();
        });
      });

      calendarModal.querySelector('[data-calendar-close]')?.addEventListener('click', () => calendarModal.close());
      calendarModal.addEventListener('click', (e) => { if (e.target === calendarModal) calendarModal.close(); });
    }

    // === INFO MODAL ===
    const infoModal = document.getElementById('shiftInfoModal') as HTMLDialogElement | null;
    if (infoModal) {
      const infoTitle = infoModal.querySelector('[data-info-title]');
      const infoDates = infoModal.querySelector('[data-info-dates]');
      const infoPrice = infoModal.querySelector('[data-info-price]');
      const infoBody = infoModal.querySelector('[data-info-body]');

      document.querySelectorAll<HTMLElement>('[data-shift-info]').forEach((btn) => {
        btn.addEventListener('click', () => {
          const shiftId = btn.dataset.shiftInfo || '';
          const info = shiftInfo[shiftId];
          if (info) {
            if (infoTitle) infoTitle.textContent = btn.dataset.shiftTitle || '';
            if (infoDates) infoDates.textContent = info.dates + ' · ' + info.duration;
            if (infoPrice) infoPrice.textContent = info.price;
            if (infoBody) infoBody.innerHTML = info.html;
          }
          infoModal.showModal();
        });
      });

      infoModal.querySelector('[data-info-close]')?.addEventListener('click', () => infoModal.close());
      infoModal.addEventListener('click', (e) => { if (e.target === infoModal) infoModal.close(); });
    }
  }

  document.addEventListener('astro:page-load', initShifts);
  initShifts();

  // === AGE PERSONALIZATION: показываем счётчик ровесников ===
  function showPeerCounts(age: string) {
    document.querySelectorAll<HTMLElement>('.peer-count-badge').forEach((badge) => {
      const counts = JSON.parse(badge.dataset.peerCounts || '{}');
      const n = counts[age];
      if (!n) return;
      const textEl = badge.querySelector<HTMLElement>('.peer-count-text');
      if (textEl) textEl.textContent = `${n} ровесников уже едут в эту смену`;
      badge.classList.remove('hidden');
      badge.classList.add('flex');
    });
  }

  function initShiftAgePersonalization() {
    // Fix 2026-04-20: показываем peer-count ТОЛЬКО после явного выбора в сессии
    // (не читаем из localStorage — иначе на первом заходе показывается "ровесники"
    // без реального выбора возраста, логика бьётся).
    const sessionAge = sessionStorage.getItem(STORAGE_KEYS.selectedAge);
    if (sessionAge) showPeerCounts(sessionAge);
    document.addEventListener('age-personalize', (e: Event) => {
      showPeerCounts((e as CustomEvent<{ age: string }>).detail.age);
    });
  }

  document.addEventListener('astro:page-load', initShiftAgePersonalization);
  initShiftAgePersonalization();
</script>
