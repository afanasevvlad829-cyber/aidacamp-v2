---
/**
 * GalleryPreview — лёгкий preview Gallery на главной
 * Показывает 3 фото с CTA «Все фото →» на /foto/
 * Снижает initial load главной (вместо полной Gallery с 30+ фото)
 */
const previews = [
  { src: '/images/gallery/study-coding.avif', alt: 'IT-занятия в АйДаКемп' },
  { src: '/images/gallery/pool-interior.avif', alt: 'Бассейн в лагере' },
  { src: '/images/gallery/hackathon-present.avif', alt: 'Хакатон, защита проектов' },
];
---

<section id="gallery-preview" class="mx-auto max-w-[1312px] px-[2px] pt-6 md:px-4">
  <div class="p-[18px]">
    <div class="flex items-center justify-between gap-3 mb-4">
      <div class="flex items-center gap-2">
        <div class="w-1 h-5 rounded-full bg-primary shrink-0"></div>
        <h2 class="text-[24px] font-bold leading-[1.1] tracking-[-0.02em] text-body md:text-[30px]">
          Фото из лагеря
        </h2>
      </div>
      <a
        href="/foto/"
        class="group inline-flex items-center gap-1.5 text-[15px] font-semibold text-primary hover:text-primary-hover"
      >
        Как выглядит лагерь?
        <i class="bi bi-arrow-right text-[14px] transition-transform group-hover:translate-x-1" aria-hidden="true"></i>
      </a>
    </div>

    <div class="grid grid-cols-2 md:grid-cols-3 gap-2 md:gap-3">
      {previews.map((p, i) => (
        <a href="/foto/" class="group relative block overflow-hidden rounded-[12px] bg-slate-100 aspect-square active:scale-[0.97] transition-transform duration-150">
          <img
            src={p.src}
            alt={p.alt}
            class="h-full w-full object-cover transition-transform duration-300 group-hover:scale-105"
            loading="lazy"
            width="400"
            height="400"
          />
          {i === 2 && (
            <div class="absolute inset-0 bg-black/40 flex items-center justify-center">
              <span class="text-white text-[14px] font-semibold flex items-center gap-1">
                <i class="bi bi-images text-[18px]" aria-hidden="true"></i>
                Ещё 30+
              </span>
            </div>
          )}
        </a>
      ))}
    </div>
  </div>
</section>
