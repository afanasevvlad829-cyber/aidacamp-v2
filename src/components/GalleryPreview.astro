---
/**
 * GalleryPreview — лёгкий preview Gallery на главной
 * Показывает 3 фото с CTA «Все фото →» на /foto/
 * Снижает initial load главной (вместо полной Gallery с 30+ фото)
 */
const previews = [
  { src: '/images/gallery/best/cover.webp', alt: 'IT-занятия в АйДаКемп' },
  { src: '/images/gallery/grid/sm/IMG_5089-sm.webp', alt: 'Бассейн в лагере' },
  { src: '/images/gallery/grid/sm/IMG_4789-sm.webp', alt: 'Хакатон, защита проектов' },
];
---

<section id="gallery-preview" class="mx-auto max-w-[1312px] px-[2px] pt-6 md:px-4">
  <div class="rounded-[18px] bg-card p-[18px]">
    <div class="flex items-center justify-between gap-3 mb-4">
      <div class="flex items-center gap-2">
        <div class="w-1 h-5 rounded-full bg-primary shrink-0"></div>
        <h2 class="text-[24px] font-bold leading-[1.1] tracking-[-0.02em] text-body md:text-[30px]">
          Фото из лагеря
        </h2>
      </div>
      <a
        href="/foto/"
        class="text-[14px] font-medium text-primary hover:text-[#d68830] inline-flex items-center gap-1"
      >
        Все фото
        <i class="bi bi-arrow-right text-[12px]" aria-hidden="true"></i>
      </a>
    </div>

    <div class="grid grid-cols-2 md:grid-cols-3 gap-2 md:gap-3">
      {previews.map((p, i) => (
        <a href="/foto/" class="group relative block overflow-hidden rounded-[12px] bg-slate-100 aspect-square">
          <img
            src={p.src}
            alt={p.alt}
            class="h-full w-full object-cover transition-transform duration-300 group-hover:scale-105"
            loading="lazy"
            width="400"
            height="400"
          />
          {i === 2 && (
            <div class="absolute inset-0 bg-black/40 flex items-center justify-center">
              <span class="text-white text-[14px] font-semibold flex items-center gap-1">
                <i class="bi bi-images text-[18px]" aria-hidden="true"></i>
                Ещё 30+
              </span>
            </div>
          )}
        </a>
      ))}
    </div>
  </div>
</section>
