---
export const prerender = false;
import '../../styles/global.css';
import { signLid, verifyLid } from '../../lib/leadLink';
import { getCustomerGroupIds } from '../../lib/alfaCrm';
import { pickPamyatkaShift, type PamyatkaShift } from '../../data/pamyatkaShifts';

const { lid: lidParam } = Astro.params;
const t = Astro.url.searchParams.get('t') || '';

const isPreview = lidParam === 'test' || lidParam === 'preview';
const lidNum = isPreview ? 0 : Number(lidParam);
const valid = isPreview || (Number.isInteger(lidNum) && lidNum > 0 && lidNum < 9999999 && verifyLid(lidNum, t));

if (!valid) {
  return new Response('Ссылка недействительна', { status: 404 });
}

// SSR: подтянуть смену клиента из CRM. В preview всегда показываем пример смены 1.
let shift: PamyatkaShift | null = null;
if (isPreview) {
  shift = pickPamyatkaShift([660]);
} else {
  try {
    const groupIds = await getCustomerGroupIds(lidNum);
    shift = pickPamyatkaShift(groupIds);
  } catch {
    shift = null;
  }
}

const sectionTone: Record<string, string> = {
  doc: '#dc2626',
  hyg: '#0ea5e9',
  cloth: '#16a34a',
  swim: '#06b6d4',
  cozy: '#a855f7',
  med: '#e2493a',
  no: '#64748b',
  before: '#ec9b44',
};

const sections = [
  {
    id: 'doc',
    icon: 'shield-fill-check',
    title: 'Документы — без них не заселят',
    note: 'Кладём первыми. Заверни в файлик — не помнётся.',
    items: [
      {
        id: 'doc-1', text: 'Медсправка форма 079/у', hint: 'Срок — 6 месяцев. Берётся в поликлинике.',
        info: {
          title: 'Медицинская справка форма 079/у',
          image: '/images/pamyatka/doc-079u.png',
          body: 'Берётся в детской поликлинике у участкового педиатра. Оформляется бесплатно по полису ОМС, занимает 1–3 дня. Срок действия — 6 месяцев со дня выдачи. Без неё ребёнка не примут в лагерь — это требование Роспотребнадзора, не наше.<br/><br/><a href="https://docs.cntd.ru/document/420295344" target="_blank" rel="noopener">Посмотреть приказ Минздрава №834н</a>',
        },
      },
      {
        id: 'doc-2', text: 'Полис ОМС', hint: 'Оригинал, копия или QR из Госуслуг',
        info: {
          title: 'Полис обязательного медицинского страхования',
          image: '/images/pamyatka/doc-oms.png',
          body: 'В 2026 году полис ОМС существует в&nbsp;трёх вариантах: <b>цифровой</b> (QR-код в&nbsp;Госуслугах), <b>пластиковая карта с&nbsp;чипом</b> или <b>бумажный бланк А5</b>. Все имеют единый 16-значный номер, без серий, действуют бессрочно на&nbsp;всей территории РФ. Любой из&nbsp;вариантов подойдёт — но&nbsp;QR-код из&nbsp;Госуслуг лучше распечатать заранее или показать на&nbsp;телефоне при заселении. Если полиса нет — оформить бесплатно в&nbsp;страховой компании или МФЦ.',
        },
      },
      {
        id: 'doc-3', text: 'Свидетельство о рождении / паспорт', hint: 'У кого что есть',
        info: {
          title: 'Документ удостоверяющий личность',
          body: 'Дети до 14 лет — свидетельство о рождении (оригинал или копия). От 14 лет — паспорт. Без этого документа Роспотребнадзор не разрешает заселять.',
        },
      },
      {
        id: 'doc-4', text: 'Согласие на медицинское вмешательство', hint: 'Скачайте бланк, заполните и привезите',
        info: {
          title: 'Информированное согласие',
          image: '/images/pamyatka/doc-soglasie.png',
          body: '<a href="/docs/pamyatka/soglasie-med.docx" download class="pm-info-download"><i class="bi bi-download"></i> Скачать бланк (DOCX, 16 КБ)</a><br/><br/>Стандартный бланк. Подписывают оба родителя (или один — с пометкой «второй родитель отсутствует/в командировке»). Распечатываете дома, заполняете, привозите подписанным. Без него медсестра лагеря не имеет права оказывать помощь даже от насморка.',
        },
      },
      {
        id: 'doc-5', text: 'Справка об отсутствии контактов (095/у)', hint: 'Берётся за 3 дня до заезда',
        info: {
          title: 'Справка об отсутствии контактов с инфекционными больными',
          image: '/images/pamyatka/doc-095u.jpg',
          body: 'Форма 095/у — действительна <b>3 дня</b>. Берётся в районной поликлинике у педиатра в день перед выездом или за день. Делается за 15–30 минут, бесплатно по полису ОМС.<br/><br/>Это пережиток ковидных требований Роспотребнадзора, который до сих пор не отменили. Без неё нас не имеют права допустить к заезду — приходится возиться. Извините за бюрократию.',
        },
      },
    ],
  },
  {
    id: 'hyg',
    icon: 'droplet-fill',
    title: 'Гигиена',
    note: 'Всё в одну косметичку — так не теряется.',
    items: [
      { id: 'hyg-1', text: 'Зубная щётка + паста' },
      { id: 'hyg-2', text: 'Мыло, шампунь, гель для душа' },
      { id: 'hyg-3', text: 'Расчёска, резинки (для девочек)' },
      { id: 'hyg-4', text: 'Полотенце для душа + для лица', hint: '2 штуки минимум' },
      { id: 'hyg-5', text: 'Влажные салфетки (упаковка)' },
    ],
  },
  {
    id: 'cloth',
    icon: 'bag-fill',
    title: 'Одежда',
    note: 'На 10-дневную смену: 5–6 комплектов. Стирка в лагере есть.',
    items: [
      { id: 'cl-1', text: 'Футболки — 5–6 шт' },
      { id: 'cl-2', text: 'Шорты / лёгкие штаны — 3–4 шт' },
      { id: 'cl-3', text: 'Толстовка / худи — 2 шт' },
      { id: 'cl-warm', text: 'Одна тёплая вещь обязательно — свитшот или флиска', hint: 'Вы сами знаете, какая у нас летом бывает погода — переменчивая' },
      { id: 'cl-socks', text: 'Тёплые носки — 2 пары', hint: 'Чтобы вечером в номере было уютно' },
      { id: 'cl-4', text: 'Носки и нижнее бельё — на каждый день +2' },
      { id: 'cl-5', text: 'Кроссовки + сандалии + кроксы', hint: 'Кроксы желательно с закрытым носом — пальцы скажут вам спасибо' },
      { id: 'cl-rain', text: 'Дождевик', hint: 'На всякий случай — летом дожди приходят внезапно' },
      { id: 'cl-6', text: 'Лёгкая шапка / кепка от солнца' },
    ],
  },
  {
    id: 'swim',
    icon: 'water',
    title: 'Бассейн и спорт',
    note: 'У нас крытый бассейн — занятия каждый день.',
    items: [
      { id: 'sw-1', text: 'Купальник / плавки — 2 комплекта' },
      { id: 'sw-2', text: 'Резиновая шапочка для бассейна' },
      { id: 'sw-3', text: 'Полотенце для бассейна (отдельное)' },
      { id: 'sw-fun', text: 'Можно взять водное оружие или плавающие игрушки', hint: 'Не обязательно — но детям нравится' },
    ],
  },
  {
    id: 'cozy',
    icon: 'heart-fill',
    title: 'Любимое из дома',
    note: 'Особенно если ребёнок едет в лагерь впервые. Снимает тревогу первой ночи.',
    items: [
      { id: 'cz-1', text: 'Маленькая мягкая игрушка', hint: 'У половины отряда что-то такое есть — никто не засмеёт' },
      { id: 'cz-2', text: 'Семейная фотография' },
      { id: 'cz-3', text: 'Скетчбук / раскраска / книга — что любит' },
    ],
  },
  {
    id: 'med',
    icon: 'capsule',
    title: 'Личная аптечка',
    note: 'Передаётся вожатому при заселении — он выдаёт по графику.',
    items: [
      { id: 'md-1', text: 'Регулярные лекарства', hint: 'Запиши на упаковке: время приёма, дозировка' },
      { id: 'md-2', text: 'Крем от солнца SPF 30+' },
      { id: 'md-3', text: 'Спрей от комаров' },
    ],
  },
  {
    id: 'no',
    icon: 'x-octagon-fill',
    title: 'Что НЕ брать',
    note: 'Лагерь предоставляет, либо запрещено, либо просто не доедет.',
    items: [
      { id: 'no-laptop', text: 'Ноутбук — у нас своё оборудование' },
      { id: 'no-electro', text: 'Дорогую электронику и украшения' },
      { id: 'no-bed', text: 'Постельное бельё, подушки' },
      { id: 'no-cash', text: 'Большие суммы наличных' },
      { id: 'no-energy', text: 'Энергетики, газировку' },
      { id: 'no-perish', text: 'Скоропортящиеся продукты', hint: 'До конца смены не доедут' },
      { id: 'no-melt', text: 'Шоколад и всё, что плавится', hint: 'Превратится в лужу в чемодане' },
      { id: 'no-cookies', text: 'Печеньки и сладости «для своего»', hint: 'Если хотите — берите так, чтобы хватило на весь отряд. Иначе обидно' },
    ],
  },
  {
    id: 'before',
    icon: 'check2-circle',
    title: 'За день до выезда',
    note: 'Финальный чек, чтобы спать спокойно.',
    items: [
      { id: 'bf-mark', text: 'Промаркировать все вещи', hint: 'Перманентным маркером, термонаклейками или малярным скотчем с надписью — иначе вещи теряются' },
      { id: 'bf-doc', text: 'Положить документы сверху в файлике' },
      { id: 'bf-charge', text: 'Зарядить телефон + взять зарядку и пауэрбанк' },
    ],
  },
];

const totalItems = sections.reduce((s, sec) => s + sec.items.length, 0);
const t_safe = signLid(lidNum);
---

<!doctype html>
<html lang="ru">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
<meta name="theme-color" content="#0d1a2b" />
<meta name="robots" content="noindex,nofollow" />
<title>Памятка для смены — АйДаКемп</title>

<link rel="icon" href="/favicon.svg" type="image/svg+xml" />
<link rel="icon" href="/favicon.ico" sizes="32x32" />
<link rel="icon" href="/images/pamyatka/icon-192.png" type="image/png" sizes="192x192" />
<link rel="apple-touch-icon" href="/images/pamyatka/icon-180.png" />
<link rel="manifest" href="/manifest-pamyatka.webmanifest" />
<meta name="apple-mobile-web-app-capable" content="yes" />
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
<meta name="apple-mobile-web-app-title" content="Памятка лагеря" />
<meta name="mobile-web-app-capable" content="yes" />
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />

<!-- Service Worker для PWA «Памятка» — offline + быстрая загрузка -->
<script is:inline>
  if ('serviceWorker' in navigator) {
    window.addEventListener('load', function(){
      navigator.serviceWorker.register('/sw-pamyatka.js', { scope: '/p/' }).catch(function(){});
    });
  }
</script>

<!-- Yandex Metrika -->
<script is:inline>
(function(m,e,t,r,i,k,a){m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)};
m[i].l=1*new Date();
for (var j = 0; j < document.scripts.length; j++) {if (document.scripts[j].src === r) { return; }}
k=e.createElement(t),a=e.getElementsByTagName(t)[0],k.async=1,k.src=r,a.parentNode.insertBefore(k,a)})
(window, document, "script", "https://mc.yandex.ru/metrika/tag.js", "ym");
ym(96499295, "init", {clickmap:true, trackLinks:true, accurateTrackBounce:true, webvisor:true});
</script>

<style is:global>
  *,*::before,*::after{box-sizing:border-box}
  html,body{margin:0;padding:0;background:#f7f5f0;color:#0d1a2b;font-family:'Inter',-apple-system,'Helvetica Neue',sans-serif;-webkit-font-smoothing:antialiased}
  body{overflow-x:hidden;min-height:100vh;padding-bottom:90px}

  .pm-hero{
    position:relative;background:linear-gradient(160deg,#0d1a2b 0%,#13243d 50%,#0a1322 100%);
    color:#fff;padding:36px 22px 40px;overflow:hidden;
  }
  .pm-hero::before{
    content:"";position:absolute;inset:-60% -20% auto auto;width:380px;height:380px;border-radius:50%;
    background:radial-gradient(circle,rgba(236,155,68,.45) 0%,transparent 65%);filter:blur(30px);
  }
  .pm-hero::after{
    content:"";position:absolute;inset:auto auto -50% -20%;width:340px;height:340px;border-radius:50%;
    background:radial-gradient(circle,rgba(58,115,255,.35) 0%,transparent 70%);filter:blur(30px);
  }
  .pm-brand{display:flex;align-items:center;gap:10px;font-weight:800;font-size:18px;letter-spacing:.3px;position:relative;z-index:2}
  .pm-brand-dot{width:11px;height:11px;border-radius:50%;background:#ec9b44;box-shadow:0 0 12px #ec9b44}
  .pm-eyebrow{
    display:inline-flex;align-items:center;gap:8px;margin-top:24px;
    background:rgba(236,155,68,.16);color:#ec9b44;font-weight:800;font-size:13px;letter-spacing:1.5px;text-transform:uppercase;
    padding:8px 14px;border-radius:999px;border:1.5px solid rgba(236,155,68,.32);position:relative;z-index:2;
  }
  .pm-h1{font-size:34px;line-height:1.05;letter-spacing:-1px;font-weight:900;margin:18px 0 14px;position:relative;z-index:2}
  .pm-h1 em{font-style:normal;color:#ec9b44}
  .pm-lede{font-size:17px;line-height:1.5;color:rgba(255,255,255,.82);font-weight:500;max-width:560px;position:relative;z-index:2}

  /* Floating-плашка прогресса — фиксирована снизу по центру */
  .pm-progress-wrap{
    position:fixed;left:50%;bottom:18px;transform:translateX(-50%);
    z-index:80;background:rgba(13,26,43,.94);color:#fff;
    backdrop-filter:saturate(160%) blur(14px);
    padding:12px 20px 14px;border-radius:18px;
    box-shadow:0 12px 36px rgba(13,26,43,.35),0 2px 8px rgba(13,26,43,.2);
    display:flex;flex-direction:column;gap:8px;
    min-width:280px;max-width:calc(100vw - 28px);
    transition:transform .25s ease, opacity .25s ease;
  }
  .pm-progress-wrap.is-done{
    background:linear-gradient(135deg,#ec9b44 0%,#f7b96a 100%);
    color:#0d1a2b;box-shadow:0 12px 36px rgba(236,155,68,.5);
  }
  .pm-progress-row{display:flex;justify-content:space-between;align-items:baseline;font-size:12px;font-weight:700;letter-spacing:.4px;text-transform:uppercase;opacity:.85;gap:14px}
  .pm-progress-row b{font-weight:900;font-size:14px;font-variant-numeric:tabular-nums;letter-spacing:0;text-transform:none;opacity:1}
  .pm-progress-row .pm-progress-pct{margin-left:auto;font-weight:900;font-size:13px;font-variant-numeric:tabular-nums;letter-spacing:0;text-transform:none}
  .pm-progress-bar{height:8px;background:rgba(255,255,255,.16);border-radius:999px;overflow:hidden}
  .pm-progress-wrap.is-done .pm-progress-bar{background:rgba(13,26,43,.18)}
  .pm-progress-fill{height:100%;border-radius:999px;background:linear-gradient(90deg,#ec9b44 0%,#f7b96a 100%);width:0%;
    transition:width .45s cubic-bezier(.2,.7,.3,1);box-shadow:0 0 8px rgba(236,155,68,.6)}
  .pm-progress-wrap.is-done .pm-progress-fill{background:#0d1a2b;box-shadow:none}

  .pm-page{max-width:560px;margin:0 auto;padding:18px 16px 80px;position:relative;z-index:3}

  .pm-section{
    background:#fff;border-radius:22px;padding:22px 18px 8px;margin-bottom:16px;
    box-shadow:0 2px 8px rgba(15,23,42,.04),0 18px 36px rgba(15,23,42,.08);
    border:1px solid #ebe7d8;position:relative;overflow:hidden;
  }
  @media (prefers-reduced-motion:no-preference){
    .pm-section{opacity:0;transform:translateY(18px);transition:opacity .5s ease, transform .5s ease}
    .pm-section.is-in{opacity:1;transform:none}
  }
  .pm-section[data-tone] .pm-sec-icon{background:var(--tone)}
  .pm-section[data-tone] .pm-sec-bar{background:var(--tone)}
  .pm-sec-bar{position:absolute;top:0;left:0;width:6px;height:100%;background:#0d1a2b}
  .pm-sec-head{display:flex;gap:14px;align-items:flex-start;padding-left:6px}
  .pm-sec-icon{
    width:42px;height:42px;border-radius:12px;background:#0d1a2b;color:#fff;
    display:flex;align-items:center;justify-content:center;font-size:22px;flex-shrink:0;box-shadow:0 6px 14px rgba(13,26,43,.25);
  }
  .pm-sec-title{font-size:18px;font-weight:800;line-height:1.25;letter-spacing:-.3px;color:#0d1a2b;margin:2px 0 4px}
  .pm-sec-note{font-size:14px;color:#5e6878;line-height:1.4;font-weight:500}
  .pm-list{list-style:none;padding:14px 0 0;margin:0}
  .pm-item{
    display:flex;align-items:flex-start;gap:14px;padding:12px 6px;border-top:1px solid #f0ece2;
    cursor:pointer;-webkit-tap-highlight-color:transparent;user-select:none;transition:background .15s;
  }
  .pm-item:active{background:#fafaf3}
  .pm-item:first-child{border-top:none}
  .pm-check{
    width:26px;height:26px;border-radius:8px;border:2px solid #cfcab8;background:#fff;flex-shrink:0;
    display:flex;align-items:center;justify-content:center;color:#fff;transition:all .2s ease;
    font-size:14px;margin-top:1px;
  }
  .pm-check::before{content:"\F633";font-family:bootstrap-icons;opacity:0;transform:scale(.5);transition:all .2s}
  .pm-item.is-done .pm-check{background:#16a34a;border-color:#16a34a;box-shadow:0 4px 10px rgba(22,163,74,.35)}
  .pm-item.is-done .pm-check::before{opacity:1;transform:scale(1)}
  .pm-item-body{flex:1;min-width:0}
  .pm-item-text{font-size:16px;line-height:1.35;color:#0d1a2b;font-weight:600;transition:color .2s, text-decoration .2s}
  .pm-item.is-done .pm-item-text{color:#7a8497;text-decoration:line-through;text-decoration-color:rgba(122,132,151,.6)}
  .pm-item-hint{font-size:13px;color:#5e6878;font-weight:500;line-height:1.35;margin-top:3px}

  .pm-final{
    margin-top:24px;padding:24px 22px;border-radius:22px;background:linear-gradient(135deg,#0d1a2b 0%,#13243d 100%);
    color:#fff;box-shadow:0 18px 36px rgba(13,26,43,.2);position:relative;overflow:hidden;
  }
  .pm-final::after{content:"";position:absolute;inset:auto -30% -50% auto;width:200px;height:200px;border-radius:50%;
    background:radial-gradient(circle,rgba(236,155,68,.4) 0%,transparent 70%);filter:blur(20px)}
  .pm-final h3{font-size:22px;font-weight:900;letter-spacing:-.5px;margin:0 0 8px;position:relative;z-index:2}
  .pm-final h3 em{font-style:normal;color:#ec9b44}
  .pm-final p{font-size:15px;line-height:1.5;color:rgba(255,255,255,.82);margin:0 0 18px;position:relative;z-index:2}
  .pm-shift-tag{display:inline-flex;align-items:center;gap:8px;background:rgba(236,155,68,.18);border:1.5px solid rgba(236,155,68,.4);color:#ec9b44;font-weight:800;font-size:12px;letter-spacing:1.5px;text-transform:uppercase;padding:6px 12px;border-radius:999px;margin-bottom:14px;position:relative;z-index:2}
  .pm-cta-grid{display:grid;grid-template-columns:1fr;gap:10px;position:relative;z-index:2}
  .pm-cta{
    display:flex;align-items:center;justify-content:center;gap:10px;
    background:#ec9b44;color:#0d1a2b;font-weight:800;font-size:15px;letter-spacing:-.2px;
    padding:14px 18px;border-radius:14px;text-decoration:none;
    box-shadow:0 8px 20px rgba(236,155,68,.4);transition:transform .15s;
  }
  .pm-cta:active{transform:scale(.97)}
  .pm-cta-secondary{background:rgba(255,255,255,.1);color:#fff;border:1.5px solid rgba(255,255,255,.22);box-shadow:none}

  .pm-footer{text-align:center;color:#7a8497;font-size:13px;line-height:1.5;padding:28px 16px 12px;font-weight:500}
  .pm-footer a{color:#0d1a2b;font-weight:700;text-decoration:none}

  /* PWA-инструкция «Добавить на экран» */
  .pm-pwa-howto{
    margin-top:24px;border:1.5px dashed #cfcab8;border-radius:18px;padding:18px 20px;
    background:#fafaf3;
  }
  .pm-pwa-howto[hidden]{display:none}
  .pm-pwa-head{display:flex;align-items:flex-start;gap:12px}
  .pm-pwa-head-icon{
    width:36px;height:36px;border-radius:10px;background:#ec9b44;color:#fff;flex-shrink:0;
    display:flex;align-items:center;justify-content:center;font-size:18px;
  }
  .pm-pwa-title{font-size:15px;font-weight:800;line-height:1.3;color:#0d1a2b;margin:2px 0 2px}
  .pm-pwa-sub{font-size:13px;color:#5e6878;font-weight:500;line-height:1.4}
  .pm-pwa-toggle{
    margin-left:auto;flex-shrink:0;background:transparent;border:none;cursor:pointer;
    color:#5e6878;font-size:18px;padding:4px;transition:transform .2s;
  }
  .pm-pwa-toggle.is-open{transform:rotate(180deg)}
  .pm-pwa-body{margin-top:14px;display:none}
  .pm-pwa-body.is-open{display:block}
  .pm-pwa-platform{margin-bottom:14px}
  .pm-pwa-platform:last-child{margin-bottom:0}
  .pm-pwa-platform-name{font-size:12px;font-weight:800;letter-spacing:1.5px;text-transform:uppercase;color:#ec9b44;margin-bottom:6px;display:flex;align-items:center;gap:8px}
  .pm-pwa-platform-name i{font-size:14px}
  .pm-pwa-steps{list-style:none;padding:0;margin:0;font-size:13.5px;line-height:1.5;color:#0d1a2b;font-weight:500}
  .pm-pwa-steps li{padding:4px 0;display:flex;align-items:flex-start;gap:8px}
  .pm-pwa-steps li::before{content:counter(step);counter-increment:step;flex-shrink:0;width:20px;height:20px;border-radius:50%;background:#ec9b44;color:#fff;font-size:11px;font-weight:800;display:flex;align-items:center;justify-content:center;margin-top:1px}
  .pm-pwa-steps{counter-reset:step}
  .pm-pwa-steps i.bi{display:inline-flex;align-items:center;justify-content:center;width:22px;height:22px;border-radius:6px;background:#ebe7d8;font-size:13px;color:#0d1a2b;margin:0 2px;vertical-align:-4px}
  .pm-pwa-note{margin-top:12px;padding:10px 12px;background:rgba(236,155,68,.08);border-radius:10px;font-size:12.5px;color:#5e6878;font-weight:500;line-height:1.45}
  .pm-pwa-note b{color:#0d1a2b;font-weight:700}

  /* Confetti canvas */
  #pm-confetti{position:fixed;inset:0;pointer-events:none;z-index:90;display:none}

  /* Toast */
  #pm-toast{
    position:fixed;left:50%;bottom:24px;transform:translateX(-50%) translateY(40px);
    background:#0d1a2b;color:#fff;padding:14px 20px;border-radius:14px;font-weight:700;font-size:15px;
    box-shadow:0 12px 32px rgba(13,26,43,.4);opacity:0;transition:all .3s cubic-bezier(.2,.7,.3,1);
    z-index:100;display:flex;gap:10px;align-items:center;max-width:90vw;
  }
  #pm-toast.is-show{opacity:1;transform:translateX(-50%) translateY(0)}
  #pm-toast i{color:#ec9b44;font-size:18px}

  @media (min-width:640px){
    .pm-h1{font-size:42px}
    .pm-hero{padding:48px 32px 56px}
    .pm-cta-grid{grid-template-columns:1fr 1fr}
  }

  /* Десктоп: masonry в 2 колонки */
  @media (min-width:900px){
    .pm-page{max-width:1080px;padding:18px 24px 100px}
    .pm-grid{columns:2;column-gap:18px}
    .pm-grid > .pm-section{break-inside:avoid;margin-bottom:18px;display:block}
    .pm-grid > .pm-final{column-span:all;margin-top:6px}
    .pm-grid > .pm-docs-section{column-span:all}
  }

  /* Видео-карточка (клик → попап) */
  .pm-video{margin:14px auto 8px;max-width:540px;padding:0 16px;position:relative;z-index:4}
  .pm-video-card{
    display:flex;align-items:center;gap:14px;width:100%;
    background:linear-gradient(135deg,#0d1a2b 0%,#13243d 100%);
    color:#fff;border:none;border-radius:18px;padding:14px 18px 14px 14px;
    cursor:pointer;text-align:left;font-family:inherit;
    box-shadow:0 8px 22px rgba(15,23,42,.18);transition:.15s ease;
    -webkit-tap-highlight-color:transparent;
  }
  .pm-video-card:hover{transform:translateY(-2px);box-shadow:0 14px 32px rgba(15,23,42,.28)}
  .pm-video-card:active{transform:translateY(0)}
  .pm-video-thumb{
    position:relative;width:88px;height:64px;border-radius:12px;overflow:hidden;flex-shrink:0;
    background:linear-gradient(135deg,#ec9b44 0%,#f7b96a 100%);
    display:flex;align-items:center;justify-content:center;
  }
  .pm-video-thumb::after{
    content:"";position:absolute;inset:0;background:radial-gradient(ellipse at 30% 30%,rgba(255,255,255,.25) 0%,transparent 60%);
  }
  .pm-video-play{
    position:relative;z-index:2;width:30px;height:30px;border-radius:50%;background:#fff;
    display:flex;align-items:center;justify-content:center;color:#0d1a2b;
    box-shadow:0 3px 8px rgba(0,0,0,.25);
  }
  .pm-video-play::before{
    content:"";display:block;width:0;height:0;
    border-left:9px solid #0d1a2b;border-top:6px solid transparent;border-bottom:6px solid transparent;
    margin-left:2px;
  }
  .pm-video-text{flex:1;min-width:0}
  .pm-video-eyebrow{font-size:10.5px;font-weight:800;letter-spacing:1.5px;text-transform:uppercase;color:#ec9b44;margin-bottom:3px}
  .pm-video-title{font-size:15px;font-weight:700;line-height:1.25;color:#fff;margin:0}
  .pm-video-arrow{color:rgba(255,255,255,.5);font-size:18px;flex-shrink:0;transition:.15s}
  .pm-video-card:hover .pm-video-arrow{color:#ec9b44;transform:translateX(2px)}

  /* Видео-попап */
  #pm-video-modal{position:fixed;inset:0;z-index:300;display:none;align-items:center;justify-content:center;padding:16px}
  #pm-video-modal.is-open{display:flex}
  .pm-video-bg{position:absolute;inset:0;background:rgba(13,26,43,.88);backdrop-filter:blur(8px)}
  .pm-video-modal-inner{
    position:relative;width:100%;max-width:960px;aspect-ratio:16/9;
    border-radius:18px;overflow:hidden;background:#000;
    box-shadow:0 24px 60px rgba(0,0,0,.6);
  }
  .pm-video-modal-inner iframe{position:absolute;inset:0;width:100%;height:100%;border:0;display:block}
  .pm-video-close{
    position:absolute;top:-46px;right:0;width:38px;height:38px;border-radius:50%;
    border:none;background:rgba(255,255,255,.15);cursor:pointer;color:#fff;
    display:flex;align-items:center;justify-content:center;font-size:18px;
    transition:.15s;
  }
  .pm-video-close:hover{background:rgba(255,255,255,.28)}

  /* Бланки документов — без чекбоксов, прямые ссылки на download */
  .pm-docs-section .pm-sec-icon{background:#3a73ff!important}
  .pm-docs-section .pm-sec-bar{background:#3a73ff!important}
  .pm-docs-grid{display:grid;grid-template-columns:1fr;gap:10px;padding:14px 0 6px;list-style:none;margin:0}
  .pm-doc{
    display:flex;align-items:center;gap:14px;padding:14px 16px;
    background:#fafaf3;border:1.5px solid #ebe7d8;border-radius:14px;
    text-decoration:none;color:#0d1a2b;font-weight:600;font-size:15px;
    transition:.15s ease;
  }
  .pm-doc:hover{border-color:#3a73ff;background:#fff;transform:translateY(-1px);box-shadow:0 6px 14px rgba(58,115,255,.15)}
  .pm-doc-icon{
    width:42px;height:42px;border-radius:10px;background:rgba(58,115,255,.12);
    display:flex;align-items:center;justify-content:center;font-size:20px;color:#3a73ff;flex-shrink:0;
  }
  .pm-doc-text{flex:1;min-width:0}
  .pm-doc-text .pm-doc-title{font-size:15px;font-weight:700;color:#0d1a2b;line-height:1.25}
  .pm-doc-text .pm-doc-meta{font-size:12px;color:#7a8497;font-weight:500;margin-top:2px;text-transform:uppercase;letter-spacing:.5px}
  .pm-doc-arrow{color:#7a8497;font-size:18px;flex-shrink:0;transition:.15s}
  .pm-doc:hover .pm-doc-arrow{color:#3a73ff;transform:translateX(2px)}
  @media (min-width:640px){
    .pm-docs-grid{grid-template-columns:1fr 1fr}
  }

  /* Info-кнопка возле items */
  .pm-info-btn{
    border:none;background:transparent;color:#5e6878;cursor:pointer;
    padding:0;margin:6px 0 0;display:inline-flex;align-items:center;gap:5px;
    font:inherit;font-size:13px;font-weight:700;color:#0d1a2b;
  }
  .pm-info-btn i{color:#ec9b44;font-size:15px}
  .pm-info-btn:hover{text-decoration:underline}

  /* Модалка */
  #pm-modal{position:fixed;inset:0;z-index:200;display:none;align-items:center;justify-content:center;padding:20px}
  #pm-modal.is-open{display:flex}
  .pm-modal-bg{position:absolute;inset:0;background:rgba(13,26,43,.7);backdrop-filter:blur(6px)}
  .pm-modal-card{
    position:relative;background:#fff;border-radius:22px;max-width:560px;width:100%;
    max-height:90vh;overflow-y:auto;padding:26px 24px;box-shadow:0 24px 60px rgba(0,0,0,.4);
  }
  .pm-modal-close{
    position:absolute;top:12px;right:12px;width:36px;height:36px;border-radius:50%;
    border:none;background:#f0ece2;cursor:pointer;font-size:20px;color:#0d1a2b;font-weight:700;
    display:flex;align-items:center;justify-content:center;
  }
  .pm-modal-close:hover{background:#e6e2d5}
  .pm-modal-card h3{font-size:20px;font-weight:900;letter-spacing:-.3px;margin:0 0 12px;padding-right:32px}
  .pm-modal-card img{width:100%;border-radius:12px;margin:12px 0;display:block}
  .pm-modal-card p{font-size:15px;line-height:1.5;color:#404a5a;font-weight:500;margin:0 0 10px}
  .pm-modal-card a{color:#ec9b44;font-weight:700;text-decoration:none}
  .pm-modal-card a:hover{text-decoration:underline}
  .pm-modal-card a.pm-info-download{
    display:inline-flex;align-items:center;gap:8px;padding:10px 16px;
    background:#ec9b44;color:#fff;border-radius:10px;text-decoration:none;font-size:14px;
    transition:.15s;
  }
  .pm-modal-card a.pm-info-download:hover{background:#d88a3a;text-decoration:none;transform:translateY(-1px)}
  .pm-modal-card a.pm-info-download i{font-size:15px}
</style>
</head>
<body>

<header class="pm-hero">
  <div class="pm-brand"><span class="pm-brand-dot"></span> АйДаКемп</div>
  <span class="pm-eyebrow"><i class="bi bi-bag-check-fill"></i> Личная памятка</span>
  <h1 class="pm-h1">Собираем чемодан в&nbsp;<em>лагерь</em></h1>
  <p class="pm-lede">{sections.length}&nbsp;разделов, {totalItems} пунктов. Тапайте, чтобы отметить — прогресс сохранится в&nbsp;этом браузере.</p>
</header>

<div class="pm-progress-wrap" id="pm-progress">
  <div class="pm-progress-row">
    <span>Чемодан</span>
    <span><b id="pm-done">0</b> / {totalItems}</span>
    <span class="pm-progress-pct" id="pm-pct">0%</span>
  </div>
  <div class="pm-progress-bar"><div class="pm-progress-fill" id="pm-fill"></div></div>
</div>

<!-- Видео-приветствие — компактная карточка, открывает попап -->
<div class="pm-video">
  <button type="button" class="pm-video-card" data-pm-video-open aria-label="Смотреть видео-приветствие">
    <span class="pm-video-thumb"><span class="pm-video-play" aria-hidden="true"></span></span>
    <span class="pm-video-text">
      <span class="pm-video-eyebrow">Видео · 30 секунд</span>
      <span class="pm-video-title">Что нужно взять, а что не нужно</span>
    </span>
    <i class="bi bi-arrow-right pm-video-arrow" aria-hidden="true"></i>
  </button>
</div>

<div id="pm-video-modal" role="dialog" aria-modal="true" aria-label="Видео-приветствие">
  <div class="pm-video-bg" data-pm-video-close></div>
  <div class="pm-video-modal-inner">
    <button type="button" class="pm-video-close" data-pm-video-close aria-label="Закрыть">×</button>
    <iframe id="pm-video-iframe" src="" allow="autoplay; fullscreen; picture-in-picture; encrypted-media; gyroscope; accelerometer; clipboard-write" allowfullscreen></iframe>
  </div>
</div>

<main class="pm-page">
  <div class="pm-grid">

    <!-- Бланки документов — без чекбоксов, скачать -->
    <section class="pm-section pm-docs-section" data-tone style="--tone:#3a73ff">
      <span class="pm-sec-bar"></span>
      <div class="pm-sec-head">
        <div class="pm-sec-icon"><i class="bi bi-file-earmark-text"></i></div>
        <div>
          <h2 class="pm-sec-title">Бланки документов</h2>
          <div class="pm-sec-note">Скачайте, распечатайте, заполните и привезите в день заезда.</div>
        </div>
      </div>
      <div class="pm-docs-grid">
        <a class="pm-doc" href="/docs/pamyatka/pamyatka-2026.docx" download>
          <div class="pm-doc-icon"><i class="bi bi-clipboard-check"></i></div>
          <div class="pm-doc-text">
            <div class="pm-doc-title">Памятка лагерь 2026</div>
            <div class="pm-doc-meta">DOCX · 19 KB</div>
          </div>
          <i class="bi bi-download pm-doc-arrow"></i>
        </a>
        <a class="pm-doc" href="/docs/pamyatka/anketa.docx" download>
          <div class="pm-doc-icon"><i class="bi bi-clipboard-check"></i></div>
          <div class="pm-doc-text">
            <div class="pm-doc-title">Анкета на ребёнка</div>
            <div class="pm-doc-meta">DOCX · 16 KB</div>
          </div>
          <i class="bi bi-download pm-doc-arrow"></i>
        </a>
        <a class="pm-doc" href="/docs/pamyatka/soglasie-med.docx" download>
          <div class="pm-doc-icon"><i class="bi bi-clipboard-check"></i></div>
          <div class="pm-doc-text">
            <div class="pm-doc-title">Согласие на мед. вмешательство</div>
            <div class="pm-doc-meta">DOCX · 16 KB</div>
          </div>
          <i class="bi bi-download pm-doc-arrow"></i>
        </a>
        <a class="pm-doc" href="/docs/pamyatka/tehnika-bezopasnosti.docx" download>
          <div class="pm-doc-icon"><i class="bi bi-clipboard-check"></i></div>
          <div class="pm-doc-text">
            <div class="pm-doc-title">Техника безопасности</div>
            <div class="pm-doc-meta">DOCX · 13 KB</div>
          </div>
          <i class="bi bi-download pm-doc-arrow"></i>
        </a>
      </div>
    </section>

    {sections.map((sec) => (
      <section class="pm-section" data-tone style={`--tone:${sectionTone[sec.id] || '#0d1a2b'}`}>
        <span class="pm-sec-bar"></span>
        <div class="pm-sec-head">
          <div class="pm-sec-icon"><i class={`bi bi-${sec.icon}`}></i></div>
          <div>
            <h2 class="pm-sec-title">{sec.title}</h2>
            <div class="pm-sec-note">{sec.note}</div>
          </div>
        </div>
        <ul class="pm-list">
          {sec.items.map((it: any) => (
            <li class="pm-item" data-id={it.id}>
              <span class="pm-check" aria-hidden="true"></span>
              <div class="pm-item-body">
                <div class="pm-item-text">{it.text}</div>
                {it.hint && <div class="pm-item-hint">{it.hint}</div>}
                {it.info && (
                  <button
                    type="button"
                    class="pm-info-btn"
                    data-info-title={it.info.title}
                    data-info-image={it.info.image || ''}
                    data-info-body={it.info.body}
                  >
                    <i class="bi bi-info-circle-fill"></i> Как выглядит / подробнее
                  </button>
                )}
              </div>
            </li>
          ))}
        </ul>
      </section>
    ))}

    {shift && shift.tg && (
      <div class="pm-final">
        <span class="pm-shift-tag"><i class="bi bi-calendar-event"></i> {shift.name} · {shift.dates}</span>
        <h3>Канал родителей <em>смены</em></h3>
        <p>Здесь вожатые публикуют расписание дня, фото с занятий и важные объявления. Подписывайтесь обязательно — это основной канал коммуникации в смене.</p>
        <div class="pm-cta-grid">
          <a class="pm-cta" href={shift.tg} target="_blank" rel="noopener">
            <i class="bi bi-telegram"></i> Канал родителей
          </a>
          {shift.manager && (
            <a class="pm-cta pm-cta-secondary" href={`https://t.me/${shift.manager}`} target="_blank" rel="noopener">
              <i class="bi bi-chat-dots-fill"></i> @{shift.manager}
            </a>
          )}
          {shift.phone && (
            <a class="pm-cta pm-cta-secondary" href={`tel:${shift.phone.replace(/[^+0-9]/g,'')}`}>
              <i class="bi bi-telephone-fill"></i> {shift.phoneDisplay || shift.phone}
            </a>
          )}
        </div>
        <p style="font-size:13px;color:rgba(255,255,255,.6);margin-top:14px;margin-bottom:0;position:relative;z-index:2">
          Если будут возникать вопросы — пишите менеджеру в личные сообщения или звоните.
        </p>
      </div>
    )}
  </div>

  <!-- PWA-инструкция «Добавить на экран» -->
  <div class="pm-pwa-howto" id="pm-pwa-howto">
    <div class="pm-pwa-head">
      <div class="pm-pwa-head-icon"><i class="bi bi-phone"></i></div>
      <div style="flex:1;min-width:0">
        <h3 class="pm-pwa-title">Сохраните памятку на экран телефона</h3>
        <p class="pm-pwa-sub">Открывайте одним тапом, пока готовитесь к смене. После лагеря удалите.</p>
      </div>
      <button type="button" class="pm-pwa-toggle" id="pm-pwa-toggle" aria-label="Развернуть инструкцию">
        <i class="bi bi-chevron-down"></i>
      </button>
    </div>
    <div class="pm-pwa-body" id="pm-pwa-body">
      <div class="pm-pwa-platform" id="pm-pwa-ios">
        <div class="pm-pwa-platform-name"><i class="bi bi-apple"></i> iPhone (Safari)</div>
        <ol class="pm-pwa-steps">
          <li>Нажмите <i class="bi bi-box-arrow-up"></i> «Поделиться» внизу экрана</li>
          <li>Прокрутите и выберите <b>«На экран Дом»</b></li>
          <li>Подтвердите — иконка появится на главном экране</li>
        </ol>
      </div>
      <div class="pm-pwa-platform" id="pm-pwa-android">
        <div class="pm-pwa-platform-name"><i class="bi bi-android2"></i> Android (Chrome)</div>
        <ol class="pm-pwa-steps">
          <li>Нажмите <i class="bi bi-three-dots-vertical"></i> в правом верхнем углу</li>
          <li>Выберите <b>«Установить приложение»</b> или «На главный экран»</li>
          <li>Подтвердите — иконка появится на рабочем столе</li>
        </ol>
      </div>
      <p class="pm-pwa-note">
        <b>Перед лагерем:</b> чек-листы и бланки всегда под рукой, без поиска ссылки в чатах.<br/>
        <b>После лагеря:</b> удалите как обычное приложение — долгий тап → «Удалить».
      </p>
    </div>
  </div>

  <div class="pm-footer">
    {isPreview
      ? <>Лагерь АйДаКемп · <b style="color:#ec9b44">ТЕСТОВАЯ СТРАНИЦА</b><br/></>
      : <>Лагерь АйДаКемп · ваша заявка №<b>{lidNum}</b><br/></>
    }
    <a href="https://aidacamp.ru" target="_blank" rel="noopener">aidacamp.ru</a>
  </div>
</main>

<canvas id="pm-confetti"></canvas>
<div id="pm-toast"><i class="bi bi-check-circle-fill"></i><span id="pm-toast-text">Ок</span></div>

<div id="pm-modal" role="dialog" aria-modal="true" aria-labelledby="pm-modal-title">
  <div class="pm-modal-bg" data-pm-close></div>
  <div class="pm-modal-card">
    <button type="button" class="pm-modal-close" data-pm-close aria-label="Закрыть">×</button>
    <h3 id="pm-modal-title"></h3>
    <img id="pm-modal-image" alt="" hidden />
    <p id="pm-modal-body"></p>
  </div>
</div>

<script is:inline define:vars={{ lid: lidNum, t: t_safe, total: totalItems, isPreview }}>
(function(){
  const STORAGE_KEY = 'pm_done_' + lid;
  const items = Array.from(document.querySelectorAll('.pm-item'));
  const fill  = document.getElementById('pm-fill');
  const doneN = document.getElementById('pm-done');
  let done = new Set(JSON.parse(localStorage.getItem(STORAGE_KEY) || '[]'));

  const pctEl = document.getElementById('pm-pct');
  const wrapEl = document.getElementById('pm-progress');

  function save(){ localStorage.setItem(STORAGE_KEY, JSON.stringify([...done])); }
  function render(){
    items.forEach(el => el.classList.toggle('is-done', done.has(el.dataset.id)));
    const n = done.size;
    doneN.textContent = n;
    const pct = Math.round((n / total) * 100);
    fill.style.width = pct + '%';
    pctEl.textContent = pct + '%';
    wrapEl.classList.toggle('is-done', n === total);
    if (n === total) burst();
  }

  items.forEach(el => {
    el.addEventListener('click', () => {
      const id = el.dataset.id;
      if (done.has(id)) done.delete(id); else { done.add(id); haptic(); }
      save(); render();
      try { window.ym && ym(96499295,'reachGoal','pamyatka_check'); } catch(_){}
    });
  });

  function haptic(){ try{ navigator.vibrate && navigator.vibrate(8); }catch(_){} }

  // IntersectionObserver — секционная анимация появления
  const io = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting) { e.target.classList.add('is-in'); io.unobserve(e.target); }
    });
  }, { threshold: .08, rootMargin: '0px 0px -30px 0px' });
  const sects = document.querySelectorAll('.pm-section');
  sects.forEach(el => io.observe(el));
  // Fallback — раскрываем все через 800мс на случай если IO запоздал (full-page screenshots, low-power)
  setTimeout(() => sects.forEach(el => el.classList.add('is-in')), 800);

  // ====== confetti ======
  const cv = document.getElementById('pm-confetti');
  const ctx = cv.getContext('2d');
  function resize(){ cv.width = innerWidth; cv.height = innerHeight; }
  addEventListener('resize', resize); resize();
  let burstingUntil = 0;
  let parts = [];
  function burst(){
    if (Date.now() < burstingUntil) return;
    burstingUntil = Date.now() + 2500;
    cv.style.display = 'block';
    const colors = ['#ec9b44','#f7b96a','#16a34a','#3a73ff','#a855f7','#ffffff'];
    for (let i=0;i<140;i++){
      parts.push({
        x: innerWidth/2 + (Math.random()-.5)*60,
        y: innerHeight/2 - 40,
        vx: (Math.random()-.5)*9,
        vy: -Math.random()*11 - 4,
        c: colors[i%colors.length],
        s: 5 + Math.random()*5,
        r: Math.random()*Math.PI*2,
        vr:(Math.random()-.5)*.3,
        life:1
      });
    }
    toast('Чемодан собран!');
    requestAnimationFrame(loop);
  }
  function loop(){
    ctx.clearRect(0,0,cv.width,cv.height);
    parts.forEach(p=>{
      p.vy += .25; p.x += p.vx; p.y += p.vy; p.r += p.vr; p.life -= .008;
      ctx.save(); ctx.translate(p.x,p.y); ctx.rotate(p.r);
      ctx.fillStyle = p.c; ctx.globalAlpha = Math.max(0,p.life);
      ctx.fillRect(-p.s/2,-p.s/2,p.s,p.s*.6);
      ctx.restore();
    });
    parts = parts.filter(p => p.life > 0 && p.y < cv.height+40);
    if (parts.length) requestAnimationFrame(loop);
    else cv.style.display='none';
  }

  // ====== toast ======
  const toastEl = document.getElementById('pm-toast');
  const toastTx = document.getElementById('pm-toast-text');
  let toastT;
  function toast(msg){
    toastTx.textContent = msg;
    toastEl.classList.add('is-show');
    clearTimeout(toastT);
    toastT = setTimeout(()=>toastEl.classList.remove('is-show'), 2200);
  }

  // ====== bind ClientID к CRM ======
  function bindCid(cid){
    if (!cid) return;
    if (sessionStorage.getItem('pm_bound_'+lid) === cid) return;
    fetch('/api/bind-lead', {
      method:'POST',
      headers:{'Content-Type':'application/json'},
      body: JSON.stringify({ lid: lid, t: t, ym_client_id: String(cid) })
    }).then(r=>r.json()).then(j=>{
      if (j && j.ok) sessionStorage.setItem('pm_bound_'+lid, String(cid));
    }).catch(()=>{});
  }
  function tryBind(){
    try {
      if (window.ym) {
        ym(96499295, 'getClientID', function(cid){ bindCid(cid); });
      }
      // fallback из cookie _ym_uid
      const m = document.cookie.match(/(?:^|;\s*)_ym_uid=([^;]+)/);
      if (m && m[1]) setTimeout(()=>bindCid(m[1]), 1500);
    } catch(_) {}
  }
  // Метрика грузится async — подождём 1.5с. В preview-режиме не биндим.
  if (!isPreview) {
    setTimeout(tryBind, 1500);
    setTimeout(tryBind, 4500);
  }

  // ====== PWA-инструкция: toggle + платформа ======
  const pwaToggle = document.getElementById('pm-pwa-toggle');
  const pwaBody   = document.getElementById('pm-pwa-body');
  const pwaIos    = document.getElementById('pm-pwa-ios');
  const pwaAndroid= document.getElementById('pm-pwa-android');
  const pwaHowto  = document.getElementById('pm-pwa-howto');

  // Если уже установлено как PWA — скрываем блок целиком
  const isStandalone = (window.matchMedia && window.matchMedia('(display-mode: standalone)').matches)
    || window.navigator.standalone === true;
  if (isStandalone && pwaHowto) {
    pwaHowto.setAttribute('hidden', '');
  } else {
    // Показываем релевантную платформу первой (другую — оставляем для общего знания)
    const ua = navigator.userAgent;
    const isIOS = /iPhone|iPad|iPod/.test(ua);
    const isAndroid = /Android/.test(ua);
    if (isIOS && pwaAndroid) pwaAndroid.style.opacity = '0.6';
    if (isAndroid && pwaIos) pwaIos.style.opacity = '0.6';

    pwaToggle?.addEventListener('click', () => {
      const opened = pwaBody?.classList.toggle('is-open');
      pwaToggle.classList.toggle('is-open', !!opened);
      pwaToggle.setAttribute('aria-label', opened ? 'Свернуть инструкцию' : 'Развернуть инструкцию');
    });
  }

  // ====== видео-попап ======
  const VIDEO_SRC = 'https://kinescope.io/embed/wUhdcJ5RmsxpxKGu7DhtZH';
  const videoModal = document.getElementById('pm-video-modal');
  const videoIframe = document.getElementById('pm-video-iframe');
  function openVideo(){
    if (!videoModal || !videoIframe) return;
    videoIframe.src = VIDEO_SRC + '?autoplay=1';
    videoModal.classList.add('is-open');
    document.body.style.overflow = 'hidden';
    try { window.ym && ym(96499295,'reachGoal','pamyatka_video_open'); } catch(_){}
  }
  function closeVideo(){
    if (!videoModal || !videoIframe) return;
    videoModal.classList.remove('is-open');
    videoIframe.src = ''; // остановить воспроизведение
    document.body.style.overflow = '';
  }
  document.querySelectorAll('[data-pm-video-open]').forEach(b => b.addEventListener('click', openVideo));
  document.querySelectorAll('[data-pm-video-close]').forEach(b => b.addEventListener('click', closeVideo));
  document.addEventListener('keydown', (e) => { if (e.key === 'Escape' && videoModal?.classList.contains('is-open')) closeVideo(); });

  // ====== info-modal ======
  const modal = document.getElementById('pm-modal');
  const mTitle = document.getElementById('pm-modal-title');
  const mImage = document.getElementById('pm-modal-image');
  const mBody  = document.getElementById('pm-modal-body');
  function openInfo(btn){
    mTitle.textContent = btn.dataset.infoTitle || '';
    const img = btn.dataset.infoImage;
    if (img) { mImage.src = img; mImage.hidden = false; mImage.alt = btn.dataset.infoTitle || ''; }
    else { mImage.hidden = true; mImage.removeAttribute('src'); }
    mBody.innerHTML = btn.dataset.infoBody || '';
    modal.classList.add('is-open');
    document.body.style.overflow = 'hidden';
    try { window.ym && ym(96499295,'reachGoal','pamyatka_info_open'); } catch(_){}
  }
  function closeInfo(){
    modal.classList.remove('is-open');
    document.body.style.overflow = '';
  }
  document.querySelectorAll('.pm-info-btn').forEach(btn => {
    btn.addEventListener('click', (e) => { e.stopPropagation(); openInfo(btn); });
  });
  modal.querySelectorAll('[data-pm-close]').forEach(el => el.addEventListener('click', closeInfo));
  document.addEventListener('keydown', (e) => { if (e.key === 'Escape' && modal.classList.contains('is-open')) closeInfo(); });

  render();
})();
</script>

<noscript><div style="padding:20px;text-align:center;color:#5e6878">Включите JavaScript, чтобы пользоваться чек-листом.</div></noscript>

</body>
</html>
