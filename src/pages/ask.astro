---
export const prerender = true;
import iconsCSS from '../styles/icons.css?raw';
const title = 'Спросить про лагерь — АйДаКемп';
const description = 'AI-ассистент IT-лагеря АйДаКемп. Задайте любой вопрос про смены, программу, цены и безопасность — отвечу честно и без скриптов продаж.';
---
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>{title}</title>
  <meta name="description" content={description}>
  <!-- noindex: AI-чат не нужен в органическом поиске (решение владельца 24.07.2026).
       Уже исключён из sitemap.xml (astro.config.mjs) — теперь и сам не индексируется. -->
  <meta name="robots" content="noindex,follow">
  <link rel="canonical" href="https://aidacamp.ru/ask/">
  <meta property="og:title" content={title}>
  <meta property="og:description" content={description}>
  <meta property="og:type" content="website">
  <style set:html={iconsCSS}></style>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@300;400;500;600;700;800&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
  <style is:global>
    *{box-sizing:border-box;margin:0;padding:0}
    :root{
      /* Accent — same in both themes */
      --accent:#ec7c00;--accent-hover:#ad5b00;--accent-text:#fff;
      --accent-bg:rgba(236,124,0,.10);--accent-border:rgba(236,124,0,.20);
      /* Keep old aliases for backward compat */
      --or:#ec7c00;--ord:#ad5b00;--orl:rgba(236,124,0,.10);
      /* Light theme surfaces */
      --surface-page:#f2f5f9;--surface-1:#ffffff;--surface-2:#e8edf4;--surface-3:#dee5ef;
      /* Light theme text */
      --text-primary:#0d1a2b;--text-secondary:#4e6072;--text-muted:#7e9094;
      /* Light theme borders */
      --border:rgba(13,27,42,.09);--border-soft:rgba(13,27,42,.06);
      /* Light theme effects */
      --glow-opacity:0.06;--star-color:13,27,42;--star-max-a:0.12;
      --orbit-particle:#9a4f00;--orbit-ring:rgba(196,95,0,.35);
      /* Old aliases → new semantic (for backward compat) */
      --nv:var(--surface-page);--nv2:var(--surface-2);--nv3:var(--surface-3);
      --wh:var(--text-primary);--hi:var(--text-secondary);--lo:var(--text-muted);
      --bd:var(--border);--bd2:var(--border-soft);
      --sans:'Manrope',sans-serif;--mono:'JetBrains Mono',monospace;
    }
    [data-theme="dark"]{
      --surface-page:#060e1a;--surface-1:#0b1729;--surface-2:#0f2040;--surface-3:#1a2e4a;
      --text-primary:#f0f4f9;--text-secondary:#8aaccc;--text-muted:#4e6880;
      --border:rgba(255,255,255,.08);--border-soft:rgba(255,255,255,.05);
      --accent-hover:#ff9520;
      --accent-bg:rgba(236,124,0,.12);--accent-border:rgba(236,124,0,.30);
      --glow-opacity:0.18;--star-color:255,255,255;--star-max-a:0.65;
      --orbit-particle:#ec7c00;--orbit-ring:rgba(236,124,0,.30);
    }
    @media(prefers-color-scheme:dark){
      :root:not([data-theme="light"]){
        --surface-page:#060e1a;--surface-1:#0b1729;--surface-2:#0f2040;--surface-3:#1a2e4a;
        --text-primary:#f0f4f9;--text-secondary:#8aaccc;--text-muted:#4e6880;
        --border:rgba(255,255,255,.08);--border-soft:rgba(255,255,255,.05);
        --accent-hover:#ff9520;
        --accent-bg:rgba(236,124,0,.12);--accent-border:rgba(236,124,0,.30);
        --glow-opacity:0.18;--star-color:255,255,255;--star-max-a:0.65;
        --orbit-particle:#ec7c00;--orbit-ring:rgba(236,124,0,.30);
      }
    }
    html,body{height:100dvh;overflow:hidden;background:var(--surface-page)}
    body{font-family:var(--sans);color:var(--text-primary);-webkit-font-smoothing:antialiased;display:flex;flex-direction:column}

    #stars{position:fixed;inset:0;z-index:0;pointer-events:none}
    .glow{position:fixed;bottom:-100px;left:50%;transform:translateX(-50%);width:500px;height:220px;background:radial-gradient(ellipse,rgba(236,124,0,.06) 0%,transparent 70%);z-index:0;pointer-events:none;animation:glow 5s ease-in-out infinite}
    @keyframes glow{0%,100%{opacity:1}50%{opacity:.7}}

    /* Aurora blobs — full-page, light theme only */
    .aurora{position:fixed;inset:0;z-index:0;pointer-events:none;overflow:hidden}
    .aurora-b{position:absolute;border-radius:50%;filter:blur(120px);animation:auFloat 22s ease-in-out infinite alternate}
    .aurora-b1{width:700px;height:500px;background:radial-gradient(circle,rgba(251,191,108,.45) 0%,rgba(236,124,0,.22) 55%,transparent 100%);bottom:-100px;left:-180px;animation-duration:24s}
    .aurora-b2{width:600px;height:440px;background:radial-gradient(circle,rgba(167,139,250,.32) 0%,rgba(124,58,237,.15) 55%,transparent 100%);bottom:-80px;right:-160px;animation-duration:30s;animation-delay:-11s}
    .aurora-b3{width:500px;height:360px;background:radial-gradient(circle,rgba(103,232,249,.25) 0%,rgba(8,145,178,.12) 55%,transparent 100%);top:20%;left:25%;animation-duration:26s;animation-delay:-18s}
    @keyframes auFloat{0%{transform:translate(0,0) scale(1)}33%{transform:translate(50px,-30px) scale(1.07)}66%{transform:translate(-25px,25px) scale(.93)}100%{transform:translate(35px,12px) scale(1.04)}}
    /* Hide aurora in dark mode */
    [data-theme="dark"] .aurora{display:none}
    @media(prefers-color-scheme:dark){:root:not([data-theme="light"]) .aurora{display:none}}
    @media(prefers-reduced-motion:reduce){.aurora-b{animation:none}}

    .topbar{position:relative;z-index:20;display:flex;align-items:center;justify-content:space-between;padding:16px 24px;background:rgba(242,245,249,.97);border-bottom:1px solid rgba(13,27,42,.07);flex-shrink:0}
    .logo{display:flex;align-items:center;gap:10px;text-decoration:none}
    .logo-box{width:34px;height:34px;border-radius:50%;overflow:hidden;flex-shrink:0;box-shadow:0 0 16px rgba(236,124,0,.3)}
    #logoCanvas{display:block;border-radius:50%}
    .logo-name{font-size:15px;font-weight:700;color:var(--text-primary)}
    .logo-name span{color:var(--or)}
    .back{font-size:13px;color:var(--text-secondary);text-decoration:none;transition:.2s;display:flex;align-items:center;gap:5px}
    .back:hover{color:var(--text-primary)}
    .back .bi{font-size:14px}

    main.layout{position:relative;z-index:10;flex:1;display:flex;flex-direction:column;max-width:680px;margin:0 auto;width:100%;padding:0 16px;overflow:hidden}

    .intro{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center;padding:24px 0 16px;overflow-y:auto;transition:all .3s}
    .intro.gone{opacity:0;pointer-events:none;max-height:0;padding:0;overflow:hidden}
    .intro-icon{width:60px;height:60px;border-radius:50%;margin:0 auto 18px;box-shadow:0 0 30px rgba(236,124,0,.3)}
    .intro-h{font-size:clamp(22px,4vw,36px);font-weight:800;line-height:1.15;letter-spacing:-.02em;margin-bottom:10px;color:var(--text-primary)}
    .intro-s{font-size:15px;color:var(--text-secondary);margin-bottom:32px;line-height:1.6}
    .hint{display:inline-flex;align-items:center;gap:8px;background:var(--surface-1);border:1px solid var(--border);border-radius:30px;padding:9px 18px;font-size:14px;color:var(--text-secondary);margin-bottom:22px}
    .hint-dot{width:6px;height:6px;border-radius:50%;background:var(--or);box-shadow:0 0 6px var(--or);animation:blink 2s infinite}
    @keyframes blink{0%,100%{opacity:1}50%{opacity:.3}}
    .age-btns{display:flex;gap:10px;justify-content:center;margin-bottom:16px}
    .age-btn{padding:14px 24px;border:1px solid rgba(13,27,42,.1);border-radius:36px;background:#fff;font-family:var(--sans);font-size:15px;font-weight:600;color:#0d1a2b;cursor:pointer;transition:.2s;box-shadow:0 1px 4px rgba(13,27,42,.07)}
    .age-btn:hover{border-color:rgba(236,124,0,.5);color:var(--text-primary);background:rgba(236,124,0,.06);transform:translateY(-2px)}
    .age-btn .n{display:block;font-size:22px;font-weight:800;color:var(--text-primary);line-height:1}
    .age-btn .l{display:block;font-size:12px;color:#7e9094;margin-top:2px}
    .q-pills{display:flex;flex-wrap:wrap;gap:8px;justify-content:center;margin-bottom:20px;padding:0 4px}
    .q-pill{padding:10px 18px;border:1px solid rgba(13,27,42,.1);border-radius:30px;background:#fff;font-family:var(--sans);font-size:16px;font-weight:500;color:#4e6072;cursor:pointer;transition:.18s;box-shadow:0 1px 3px rgba(13,27,42,.05);white-space:nowrap}
    .q-pill:hover{border-color:rgba(236,124,0,.4);color:#0d1a2b;background:rgba(236,124,0,.05);transform:translateY(-1px)}
    .q-pill.q-bad{border-color:rgba(220,38,38,.2);color:#991b1b;background:rgba(220,38,38,.04)}
    .q-pill.q-bad:hover{border-color:rgba(220,38,38,.4);background:rgba(220,38,38,.08)}
    /* dark mode: q-pill */
    [data-theme="dark"] .q-pill{background:var(--surface-2);border-color:var(--border);color:var(--text-secondary)}
    [data-theme="dark"] .q-pill:hover{background:var(--surface-3);border-color:rgba(236,124,0,.4);color:var(--text-primary)}
    [data-theme="dark"] .q-pill.q-bad{background:rgba(220,38,38,.08);border-color:rgba(220,38,38,.25);color:#fca5a5}
    [data-theme="dark"] .q-pill.q-bad:hover{background:rgba(220,38,38,.15);border-color:rgba(220,38,38,.4);color:#fecaca}

    .messages{flex:1;overflow-y:auto;padding:8px 0 12px;display:none;flex-direction:column;gap:12px;scrollbar-width:thin;scrollbar-color:rgba(13,27,42,.1) transparent;min-height:0}
    .messages.show{display:flex}
    .messages::-webkit-scrollbar{width:3px}
    .messages::-webkit-scrollbar-thumb{background:rgba(13,27,42,.1);border-radius:3px}
    /* Spacer pushes messages to the bottom (messenger-style) */
    #msgs-spacer{flex:1;min-height:0}

    /* Client (parent) — LEFT, solid orange bubble */
    .msg-user{align-self:flex-start;max-width:80%;background:#ec7c00;border:none;border-radius:4px 18px 18px 18px;padding:12px 16px;font-size:16px;color:#fff;line-height:1.55;animation:up .25s cubic-bezier(.16,1,.3,1);box-shadow:0 3px 12px rgba(236,124,0,.35)}
    /* Bot (AI) — RIGHT, white bubble with clear border */
    .msg-text{align-self:flex-end;max-width:85%;background:#fff;border:1.5px solid rgba(13,27,42,.13);border-radius:18px 4px 18px 18px;padding:14px 18px;font-size:16px;color:#0d1a2b;line-height:1.7;animation:up .25s cubic-bezier(.16,1,.3,1);box-shadow:0 2px 16px rgba(13,27,42,.12)}
    .msg-text strong{color:var(--or)}
    .msg-text a{color:#ad5b00;text-decoration:none}
    /* Dark theme */
    [data-theme="dark"] .msg-text{background:#162444;border-color:rgba(255,255,255,.12);color:#f0f4f9;box-shadow:0 2px 16px rgba(0,0,0,.4)}
    [data-theme="dark"] .msg-user{background:#ad5b00;box-shadow:0 3px 12px rgba(236,124,0,.4)}
    /* Block cards and chips — align right (follow bot) */
    .vblock,.chips,.contact-card{align-self:flex-end}
    @keyframes up{from{opacity:0}to{opacity:1}}
    /* All blocks: smooth fade-in, no jumping from bottom */
    @keyframes blockIn{
      0%{opacity:0;transform:scale(.98)}
      100%{opacity:1;transform:scale(1)}
    }
    @keyframes blockSlide{
      0%{opacity:0;transform:scale(.98)}
      100%{opacity:1;transform:scale(1)}
    }
    @keyframes blockPop{
      0%{opacity:0;transform:scale(.96)}
      60%{opacity:1;transform:scale(1.01)}
      100%{opacity:1;transform:scale(1)}
    }
    @keyframes blockFade{
      0%{opacity:0}
      100%{opacity:1}
    }
    @keyframes blockGlow{
      0%{box-shadow:0 2px 8px rgba(13,27,42,.05)}
      40%{box-shadow:0 4px 28px rgba(236,124,0,.16)}
      100%{box-shadow:0 2px 12px rgba(13,27,42,.06)}
    }

    .typing{align-self:flex-end;background:var(--surface-1);border:1px solid var(--border);border-radius:18px 4px 18px 18px;padding:14px 18px;display:none;gap:5px;align-items:center;box-shadow:0 1px 6px rgba(13,27,42,.05);margin-top:8px;margin-bottom:6px}
    .typing.show{display:flex}
    .td{width:6px;height:6px;border-radius:50%;background:var(--text-muted);animation:td .9s infinite}
    .td:nth-child(2){animation-delay:.18s}.td:nth-child(3){animation-delay:.36s}
    @keyframes td{0%,60%,100%{transform:translateY(0);opacity:.3}30%{transform:translateY(-5px);opacity:1}}

    .chips{align-self:flex-start;display:flex;gap:7px;flex-wrap:wrap;animation:up .3s .1s cubic-bezier(.16,1,.3,1) both}
    .chip{padding:10px 18px;border:1px solid var(--border);border-radius:24px;background:var(--surface-1);font-family:var(--sans);font-size:16px;font-weight:500;color:var(--text-primary);cursor:pointer;transition:.18s;box-shadow:0 1px 4px rgba(13,27,42,.07)}
    .chip:hover{border-color:rgba(236,124,0,.5);color:var(--text-primary);background:rgba(236,124,0,.06);transform:translateY(-1px);box-shadow:0 4px 12px rgba(236,124,0,.1)}
    .chip:active{transform:translateY(0)}
    .chip.cta{background:linear-gradient(135deg,#ad5b00,#a34e00);border-color:transparent;color:#fff;font-weight:700;box-shadow:0 4px 16px rgba(236,124,0,.4);letter-spacing:.01em}
    .chip.cta:hover{background:linear-gradient(135deg,#d96a00,#b85500);box-shadow:0 6px 20px rgba(236,124,0,.5);transform:translateY(-1px)}
    /* cta-soft (зелёные) — светлая тема: тёмно-зелёный текст, тёмная тема: светло-зелёный */
    .chip.cta-soft{background:rgba(34,197,94,.08);border-color:rgba(34,197,94,.25);color:#15803d;font-weight:600}
    .chip.cta-soft:hover{background:rgba(34,197,94,.14);transform:translateY(-1px)}
    .chip-soft{padding:10px 18px;border:1px solid rgba(34,197,94,.25);border-radius:24px;background:rgba(34,197,94,.08);font-family:var(--sans);font-size:16px;font-weight:600;color:#15803d;cursor:pointer;transition:.18s}
    .chip-soft:hover{background:rgba(34,197,94,.14);transform:translateY(-1px)}
    /* dark mode: chip */
    [data-theme="dark"] .chip{background:var(--surface-2);border-color:var(--border);color:var(--text-primary)}
    [data-theme="dark"] .chip:hover{background:var(--surface-3);border-color:rgba(236,124,0,.4);color:var(--text-primary)}
    [data-theme="dark"] .chip.cta-soft{color:#4ade80;border-color:rgba(74,222,128,.3);background:rgba(74,222,128,.08)}
    [data-theme="dark"] .chip.cta-soft:hover{background:rgba(74,222,128,.14)}
    [data-theme="dark"] .chip-soft{color:#4ade80;border-color:rgba(74,222,128,.3);background:rgba(74,222,128,.08)}
    [data-theme="dark"] .chip-soft:hover{background:rgba(74,222,128,.14)}

    /* ── CSAT — оценка ответа бота (полезно / не полезно) ── */
    .csat-row{align-self:flex-end;display:flex;gap:6px;margin-top:-4px;animation:blockFade .35s ease-out both}
    .csat-btn{width:30px;height:30px;padding:0;border:1px solid var(--border);border-radius:50%;background:transparent;cursor:pointer;display:flex;align-items:center;justify-content:center;color:var(--text-muted);transition:.18s}
    .csat-btn .bi{font-size:14px}
    .csat-btn:hover{border-color:var(--accent-border);color:var(--accent);background:var(--accent-bg)}
    .csat-btn.on{border-color:var(--accent-border);color:var(--accent);background:var(--accent-bg)}

    .contact-card{align-self:flex-start;width:100%;max-width:380px;background:#f0fdf4;border:1px solid rgba(34,197,94,.2);border-radius:16px;padding:18px 20px;animation:up .3s cubic-bezier(.16,1,.3,1)}
    .contact-card-title{font-size:14px;font-weight:700;color:#0d1a2b;margin-bottom:4px}
    .contact-card-sub{font-size:12px;color:#4e6072;margin-bottom:14px;line-height:1.5}
    .contact-btns{display:flex;gap:8px}
    .contact-btn{flex:1;padding:11px 8px;border-radius:10px;border:none;font-family:var(--sans);font-size:13px;font-weight:700;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:6px;text-decoration:none;transition:.15s}
    .contact-btn-wa{background:#25d366;color:#fff}
    .contact-btn-wa:hover{background:#1eb356}
    .contact-btn-tg{background:#229ed9;color:#fff}
    .contact-btn-tg:hover{background:#1a8abf}

    /* VISUAL BLOCKS — full width, no bubble constraint */
    .vblock{align-self:stretch;width:100%;animation:blockIn .45s cubic-bezier(.16,1,.3,1) both,blockGlow .8s .2s ease-out both}
    .block{background:#fff;border:1px solid rgba(13,27,42,.09);border-radius:20px;overflow-x:hidden;overflow-y:auto;max-height:min(calc(100dvh - 310px),360px);scrollbar-width:thin;scrollbar-color:rgba(13,27,42,.1) transparent;box-shadow:0 2px 16px rgba(13,27,42,.07)}
    .block::-webkit-scrollbar{width:3px}
    .block::-webkit-scrollbar-thumb{background:rgba(13,27,42,.12);border-radius:3px}
    .block-head{padding:13px 18px;border-bottom:1px solid rgba(13,27,42,.07);display:flex;align-items:center;justify-content:space-between}
    .block-title{font-size:13px;font-weight:600;color:rgba(13,27,42,.55);display:flex;align-items:center;gap:8px}
    .block-title .bi{opacity:.6;font-size:14px;color:rgba(13,27,42,.55)}
    .block-badge{font-size:10px;font-weight:600;padding:3px 9px;border-radius:20px}
    .badge-green{background:rgba(34,197,94,.15);color:#15803d}
    .badge-red{background:rgba(239,68,68,.15);color:#dc2626}
    .badge-or{background:rgba(236,124,0,.12);color:#ad5b00}

    /* ── SMENY CARDS ── */
    .smeny-cards{display:flex;flex-direction:column;gap:8px;padding:10px 12px 4px}
    .smena-card{border-radius:14px;border:1px solid rgba(13,27,42,.09);background:#f8fafb;overflow:hidden;transition:.18s;cursor:pointer}
    .smena-card:hover{border-color:rgba(236,124,0,.35);background:rgba(236,124,0,.04);transform:translateY(-1px);box-shadow:0 6px 20px rgba(236,124,0,.1)}
    .smena-card.sold{opacity:.45;cursor:default;pointer-events:none}
    .smena-card.popular{border-color:rgba(236,124,0,.3);background:rgba(236,124,0,.04)}
    @keyframes smena-in{from{opacity:0;transform:scale(.98)}to{opacity:1;transform:scale(1)}}
    .smena-card{animation:smena-in .3s cubic-bezier(.16,1,.3,1) both}
    .smena-card:nth-child(1){animation-delay:.04s}
    .smena-card:nth-child(2){animation-delay:.09s}
    .smena-card:nth-child(3){animation-delay:.14s}
    .smena-card:nth-child(4){animation-delay:.19s}
    .smena-card:nth-child(5){animation-delay:.24s}
    .smena-card-top{padding:12px 14px 8px;display:grid;grid-template-columns:1fr auto;gap:6px;align-items:start}
    .smena-pop{display:inline-flex;align-items:center;gap:4px;background:#ec7c00;color:#fff;font-size:10px;font-weight:700;padding:2px 9px;border-radius:20px;margin-bottom:5px;width:fit-content}
    .smena-pop .bi{font-size:10px}
    .smena-card-name{font-size:15px;font-weight:800;color:#0d1a2b;line-height:1.2}
    .smena-card-dates{font-size:13px;color:#4e6072;margin-top:2px}
    .smena-card-days{font-size:12px;color:#7e9094;margin-top:1px}
    .smena-card-price{text-align:right}
    .smena-price-num{font-size:19px;font-weight:800;color:var(--or);white-space:nowrap;letter-spacing:-.02em}
    .smena-price-sub{font-size:11px;color:rgba(255,255,255,.4);margin-top:1px}
    .smena-sold-badge{font-size:11px;color:#dc2626;font-weight:600;background:rgba(220,38,38,.08);border:1px solid rgba(220,38,38,.2);padding:3px 10px;border-radius:20px}
    /* occupancy bar */
    .smena-occ{padding:0 14px 12px}
    .smena-bar-wrap{height:6px;border-radius:3px;background:rgba(13,27,42,.08);overflow:hidden;margin-bottom:5px}
    .smena-bar-fill{height:100%;border-radius:3px;background:rgba(13,27,42,.15);width:0;transition:width 1s cubic-bezier(.16,1,.3,1)}
    .smena-bar-free{height:100%;background:linear-gradient(90deg,#22c55e,#16a34a);border-radius:3px;transition:width 1s cubic-bezier(.16,1,.3,1)}
    .smena-occ-row{display:flex;justify-content:space-between;font-size:11px}
    .smena-occ-taken{color:#7e9094}
    .smena-occ-free{color:#15803d;font-weight:600}
    /* cta */
    .block-cta{margin:4px 12px 12px;padding:12px;background:linear-gradient(135deg,#ad5b00,#a34e00);border:none;border-radius:10px;font-family:var(--sans);font-size:14px;font-weight:700;color:#fff;cursor:pointer;width:calc(100% - 24px);transition:.15s;display:block;box-shadow:0 4px 14px rgba(236,124,0,.3);letter-spacing:.01em}
    .block-cta:hover{background:linear-gradient(135deg,#d96a00,#b85500);box-shadow:0 6px 20px rgba(236,124,0,.4)}
    .block-foot{padding:4px 14px 12px;font-size:11px;color:rgba(13,27,42,.35);text-align:center}
    /* typewriter cursor */
    .msg-text.typing-cursor::after{content:'▋';animation:cur .7s infinite;color:var(--or);font-size:12px;margin-left:1px}
    @keyframes cur{0%,100%{opacity:1}50%{opacity:0}}

    /* ── COURSES GRID ── */
    .courses-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;padding:12px}
    .courses-grid.single{grid-template-columns:1fr}
    .course-tile{position:relative;border-radius:14px;padding:16px 14px 14px;cursor:pointer;transition:.2s;overflow:hidden;border:1px solid rgba(13,27,42,.08);background:#f8fafb;display:flex;flex-direction:column;gap:10px}
    .course-tile::before{content:'';position:absolute;inset:0;opacity:0;transition:.2s;background:var(--tile-glow);border-radius:14px}
    .course-tile:hover::before{opacity:1}
    .course-tile:hover{border-color:var(--tile-border);transform:translateY(-2px);box-shadow:0 8px 24px var(--tile-shadow)}
    .course-tile-icon{width:44px;height:44px;border-radius:12px;display:flex;align-items:center;justify-content:center;background:var(--tile-icon-bg);border:1px solid var(--tile-border);position:relative;flex-shrink:0}
    .course-tile-icon .bi{font-size:22px;color:var(--tile-color)}
    .course-tile-icon::after{content:'';position:absolute;inset:-4px;border-radius:16px;background:var(--tile-icon-bg);filter:blur(8px);opacity:.4;z-index:-1}
    .course-tile-name{font-size:14px;font-weight:800;color:#0d1a2b;line-height:1.2;margin-top:2px}
    .course-tile-age{font-size:10px;font-weight:600;color:var(--tile-color);background:var(--tile-icon-bg);padding:2px 8px;border-radius:20px;display:inline-block;margin-top:2px;width:fit-content}
    .course-tile-hook{font-size:11.5px;color:#4e6072;line-height:1.5;flex:1}
    .course-tile-result{display:flex;align-items:center;gap:5px;font-size:10px;font-weight:600;color:var(--tile-color);background:var(--tile-icon-bg);border:1px solid var(--tile-border);padding:4px 10px;border-radius:20px;width:fit-content;margin-top:2px}
    .course-tile-result .bi{font-size:10px}
    .course-tile-arrow{position:absolute;top:12px;right:12px;opacity:0;transition:.15s;color:var(--tile-color);font-size:12px}
    .course-tile:hover .course-tile-arrow{opacity:.7}
    @keyframes tile-in{from{opacity:0;transform:scale(.97)}to{opacity:1;transform:scale(1)}}
    .course-tile{animation:tile-in .3s cubic-bezier(.16,1,.3,1) both}
    .course-tile:nth-child(1){animation-delay:.04s}
    .course-tile:nth-child(2){animation-delay:.08s}
    .course-tile:nth-child(3){animation-delay:.12s}
    .course-tile:nth-child(4){animation-delay:.16s}
    .course-tile:nth-child(5){animation-delay:.20s}
    .course-tile:nth-child(6){animation-delay:.24s}
    .courses-more{padding:10px 16px 12px;font-size:12px;color:#7e9094;text-align:center;cursor:pointer;transition:.15s;border-top:1px solid rgba(13,27,42,.06);display:flex;align-items:center;justify-content:center;gap:5px}
    .courses-more:hover{color:var(--or)}
    .courses-more .bi{font-size:11px}

    .day-list{padding:8px 0}
    .day-row{padding:9px 18px;display:flex;gap:14px;align-items:flex-start;border-bottom:1px solid rgba(13,27,42,.06)}
    .day-row:last-child{border-bottom:none}
    .day-time{font-family:var(--mono);font-size:12px;color:var(--or);min-width:46px;margin-top:2px;flex-shrink:0}
    .day-text{font-size:13px;color:#4e6072;line-height:1.5}
    .day-text strong{color:#0d1a2b}

    .cond-grid{display:grid;grid-template-columns:1fr 1fr;gap:1px;background:rgba(13,27,42,.07)}
    .cond-cell{background:#f8fafb;padding:14px 16px;display:flex;gap:10px;align-items:flex-start}
    .cond-icon{font-size:20px;flex-shrink:0;color:var(--or)}
    .cond-title{font-size:13px;font-weight:600;color:#0d1a2b;margin-bottom:3px}
    .cond-desc{font-size:11px;color:#4e6072;line-height:1.45}

    .loc-body{padding:16px 18px;display:flex;flex-direction:column;gap:10px}
    .loc-row{display:flex;gap:12px;align-items:flex-start}
    .loc-icon{font-size:16px;flex-shrink:0;margin-top:1px;color:var(--or)}
    .loc-label{font-size:12px;color:#7e9094}
    .loc-val{font-size:14px;font-weight:600;color:#0d1a2b}
    .loc-map{background:rgba(13,27,42,.04);border-radius:10px;height:100px;display:flex;align-items:center;justify-content:center;font-size:12px;color:#7e9094;cursor:pointer;transition:.15s;border:1px solid rgba(13,27,42,.08);margin:0 18px 14px}
    .loc-map:hover{background:rgba(13,27,42,.07);color:#4e6072}

    .price-tabs{display:flex;gap:1px;background:rgba(13,27,42,.07)}
    .price-tab{flex:1;padding:10px;text-align:center;font-size:12px;font-weight:600;color:#7e9094;cursor:pointer;transition:.15s;background:#f8fafb}
    .price-tab.on{background:rgba(236,124,0,.08);color:#ad5b00}
    .price-body{padding:14px 18px;display:none}
    .price-body.show{display:block}
    .price-num{font-size:28px;font-weight:800;color:#0d1a2b;margin-bottom:4px}
    .price-per{font-size:13px;color:#4e6072}
    .price-items{margin-top:12px;display:flex;flex-direction:column;gap:6px}
    .price-item{font-size:12px;color:#4e6072;display:flex;gap:8px}
    .price-item::before{content:"✓";color:var(--or);flex-shrink:0}
    .price-cashback{background:rgba(34,197,94,.08);border:1px solid rgba(34,197,94,.2);border-radius:8px;padding:9px 12px;font-size:12px;color:#15803d;margin-top:12px;line-height:1.5}

    .input-area{padding:8px 0 max(20px,env(safe-area-inset-bottom,0px));flex-shrink:0;position:relative;z-index:10}
    .input-box{display:flex;align-items:flex-end;background:var(--surface-1);border:1px solid var(--border);border-radius:14px;transition:.2s;box-shadow:0 1px 6px rgba(13,27,42,.06);padding:5px 5px 5px 0;gap:2px}
    .input-box:focus-within{border-color:rgba(236,124,0,.4);background:var(--surface-1);box-shadow:0 0 0 3px rgba(236,124,0,.07)}
    .inp{flex:1;padding:10px 14px 10px 18px;background:transparent;border:none;outline:none;font-family:var(--sans);font-size:16px;color:var(--text-primary);resize:none;line-height:1.5;min-height:44px;max-height:140px;overflow-y:auto;display:block}
    .inp::placeholder{color:var(--text-muted)}
    .send{width:44px;height:44px;background:var(--or);border:none;border-radius:10px;cursor:pointer;display:flex;align-items:center;justify-content:center;transition:.15s;flex-shrink:0}
    .send:hover{background:var(--ord)}
    .send:disabled{background:var(--surface-2);cursor:default}
    .send .bi{font-size:16px;color:#fff}
    .mic{width:40px;height:44px;background:transparent;border:none;cursor:pointer;display:flex;align-items:center;justify-content:center;transition:.15s;flex-shrink:0;color:var(--text-muted)}
    .mic:hover{color:var(--text-primary)}
    .mic.recording{color:#f87171;animation:pulse-mic .8s infinite}
    .mic .bi{font-size:20px}
    @keyframes pulse-mic{0%,100%{opacity:1}50%{opacity:.4}}
    /* Booking quick button — left of input */
    .book-quick{width:40px;height:44px;background:transparent;border:none;cursor:pointer;display:flex;align-items:center;justify-content:center;transition:.15s;flex-shrink:0;color:var(--accent);padding-left:8px}
    .book-quick:hover{color:var(--accent-hover);transform:scale(1.08)}
    .book-quick .bi{font-size:20px}

    .overlay{display:none;position:fixed;inset:0;background:rgba(13,27,42,.45);backdrop-filter:blur(6px);z-index:100;align-items:center;justify-content:center;padding:20px}
    .overlay.open{display:flex}
    .popup{background:#fff;border:1px solid rgba(13,27,42,.1);border-radius:22px;width:100%;max-width:390px;overflow:hidden;animation:pop .22s cubic-bezier(.16,1,.3,1)}
    @keyframes pop{from{opacity:0;transform:scale(.94) translateY(10px)}to{opacity:1;transform:none}}
    .ph{padding:20px 24px;border-bottom:1px solid rgba(13,27,42,.08);display:flex;justify-content:space-between;align-items:center}
    .ph-title{font-size:17px;font-weight:800;color:#0d1a2b}
    .px{width:32px;height:32px;background:rgba(13,27,42,.06);border:none;border-radius:8px;cursor:pointer;color:rgba(13,27,42,.4);font-size:18px;display:flex;align-items:center;justify-content:center;transition:.15s}
    .px:hover{background:rgba(13,27,42,.1);color:#0d1a2b}
    .px .bi{font-size:16px}
    .pb{padding:22px 24px}
    .pfree{display:inline-block;background:rgba(22,163,74,.1);color:#16a34a;font-size:11px;font-weight:600;padding:3px 10px;border-radius:20px;border:1px solid rgba(22,163,74,.2);margin-bottom:18px}
    .plbl{font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:.07em;color:rgba(13,27,42,.35);margin-bottom:8px;display:block}
    .par{display:flex;gap:6px;margin-bottom:14px}
    .pab{flex:1;padding:10px 4px;border:1px solid rgba(13,27,42,.12);border-radius:9px;background:transparent;font-family:var(--sans);font-size:12px;font-weight:600;color:rgba(13,27,42,.4);cursor:pointer;transition:.15s;text-align:center}
    .pab:hover,.pab.on{border-color:var(--or);background:rgba(236,124,0,.1);color:var(--ord)}
    .psel{width:100%;padding:12px 14px;border:1px solid rgba(13,27,42,.12);border-radius:9px;background:#f8fafc;font-family:var(--sans);font-size:14px;color:#0d1a2b;outline:none;appearance:none;cursor:pointer;margin-bottom:12px}
    .psel option{background:#fff}
    .pin{width:100%;padding:13px 16px;border:1px solid rgba(13,27,42,.12);border-radius:9px;background:#f8fafc;font-family:var(--sans);font-size:15px;color:#0d1a2b;outline:none;margin-bottom:13px;transition:.2s}
    .pin:focus{border-color:rgba(236,124,0,.5);background:#fff}
    .pin::placeholder{color:rgba(13,27,42,.3)}
    .pcons{display:flex;align-items:flex-start;gap:8px;font-size:12px;color:rgba(13,27,42,.5);line-height:1.4;margin-bottom:8px;cursor:pointer}
    .pcons input{margin-top:2px;flex-shrink:0;accent-color:var(--or)}
    .pcons a{color:rgba(13,27,42,.5);text-decoration:underline}
    .psub{width:100%;padding:14px;background:var(--or);color:#fff;border:none;border-radius:9px;font-family:var(--sans);font-size:15px;font-weight:700;cursor:pointer;transition:.15s;margin-bottom:9px;box-shadow:0 4px 18px rgba(236,124,0,.25)}
    .psub:hover{background:var(--ord)}
    .ptr{font-size:11px;color:rgba(13,27,42,.35);text-align:center;line-height:1.6}
    .pback{font-size:12px;color:rgba(13,27,42,.3);text-align:center;padding:8px 24px 18px}
    .pback a{color:rgba(13,27,42,.35);text-decoration:none}
    .pback a:hover{color:#0d1a2b}

    @media(max-width:600px){
      .topbar{padding:12px 14px}
      .back span{display:none}
      .layout{padding:0 10px}
      .age-btns{gap:7px}
      .age-btn{padding:12px 18px}
      .age-btn .n{font-size:18px}
      .msg-user,.msg-text{max-width:92%;font-size:17px}
      .vblock{max-width:100%}
      .cond-grid{grid-template-columns:1fr}
      .courses-grid-inner{grid-template-columns:1fr!important}
      .inp{padding:14px 14px;font-size:17px;max-height:100px}
      .popup{margin:12px;border-radius:18px}
      .chips{gap:6px}
      .chip{font-size:14px;padding:9px 14px}
      .mic .bi{font-size:28px}
      .mic{width:46px}
      .book-quick{width:46px}
      .book-quick .bi{font-size:24px}
      /* Share row — на мобилке скрываем текст, оставляем только иконку */
      .share-btn span{display:none}
      .share-row{padding:0 4px 4px;gap:4px}
      .share-btn{padding:5px 9px}
    }

    /* ── COMPACT MODE: short screens (MacBook, small laptop) ── */
    @media(max-height:750px){
      .intro{padding:14px 0 10px;justify-content:flex-start;padding-top:clamp(10px,3vh,20px)}
      .intro-icon{width:46px;height:46px;margin-bottom:10px}
      .intro-h{font-size:clamp(20px,3.5vw,28px);margin-bottom:6px}
      .intro-s{font-size:14px;margin-bottom:18px}
      .q-pills{margin-bottom:14px;gap:6px}
      .q-pill{padding:8px 14px;font-size:14px}
      .hint{padding:7px 14px;font-size:13px;margin-bottom:14px}
      .age-btns{margin-bottom:10px;gap:8px}
      .age-btn{padding:11px 18px}
      .age-btn .n{font-size:18px}
    }
    @media(max-height:650px){
      .intro-icon{width:36px;height:36px;margin-bottom:8px}
      .intro-h{font-size:clamp(18px,3vw,24px);margin-bottom:4px}
      .intro-s{font-size:13px;margin-bottom:12px}
      .q-pills{margin-bottom:10px;gap:5px}
      .q-pill{padding:6px 12px;font-size:13px}
      .hint{padding:5px 12px;font-size:12px;margin-bottom:10px}
      .age-btns{margin-bottom:6px}
      .age-btn{padding:8px 14px}
    }

    /* ── DARK THEME OVERRIDES ── */
    [data-theme="dark"] .topbar{background:rgba(6,14,26,.95);border-color:var(--border)}
    [data-theme="dark"] .glow{background:radial-gradient(ellipse,rgba(236,124,0,.18) 0%,transparent 70%)}
    [data-theme="dark"] .popup{background:var(--surface-1)}
    [data-theme="dark"] .ph{border-color:var(--border)}
    [data-theme="dark"] .ph-title{color:var(--text-primary)}
    [data-theme="dark"] .px{background:var(--surface-2);color:var(--text-secondary)}
    [data-theme="dark"] .px:hover{background:var(--surface-3);color:var(--text-primary)}
    [data-theme="dark"] .plbl{color:var(--text-muted)}
    [data-theme="dark"] .pab{border-color:var(--border);color:var(--text-muted)}
    [data-theme="dark"] .psel{background:var(--surface-2);border-color:var(--border);color:var(--text-primary)}
    [data-theme="dark"] .pin{background:var(--surface-2);border-color:var(--border);color:var(--text-primary)}
    [data-theme="dark"] .ptr{color:var(--text-muted)}
    [data-theme="dark"] .pback a{color:var(--text-muted)}
    [data-theme="dark"] .age-btn{background:var(--surface-2);border-color:var(--border);color:var(--text-secondary)}
    [data-theme="dark"] .age-btn .n{color:var(--text-primary)}
    [data-theme="dark"] .contact-card{background:var(--surface-2);border-color:rgba(34,197,94,.25)}
    [data-theme="dark"] .input-box:focus-within{border-color:rgba(236,124,0,.5);background:var(--surface-1);box-shadow:0 0 0 3px rgba(236,124,0,.1)}
    [data-theme="dark"] .mic{color:var(--text-muted)}
    [data-theme="dark"] .mic:hover{color:var(--text-secondary)}
    [data-theme="dark"] .messages::-webkit-scrollbar-thumb{background:var(--border)}

    /* ── DIALOG PROGRESS ── */
    .dialog-progress{position:fixed;top:0;left:0;right:0;height:3px;z-index:500;background:transparent;pointer-events:none}
    .dialog-progress-bar{height:100%;width:0%;background:linear-gradient(90deg,var(--accent),#ff9520);transition:width .7s cubic-bezier(.4,0,.2,1);border-radius:0 2px 2px 0}

    /* ── АЧИВКА "ХОРОШАЯ МАМА" ── */
    .achivka{position:fixed;top:68px;left:50%;transform:translateX(-50%) translateY(-140px);z-index:400;background:var(--surface-1);border:1px solid rgba(236,124,0,.4);border-radius:40px;padding:10px 10px 10px 14px;display:flex;align-items:center;gap:10px;box-shadow:0 6px 28px rgba(236,124,0,.18);transition:transform .5s cubic-bezier(.34,1.56,.64,1);pointer-events:none;white-space:nowrap}
    .achivka.show{transform:translateX(-50%) translateY(0);pointer-events:auto}
    .achivka.dad{border-color:rgba(96,165,250,.5);box-shadow:0 6px 28px rgba(96,165,250,.18)}
    .achivka.dad .achivka-icon{color:#60a5fa}
    .achivka-icon{font-size:20px;color:var(--accent)}
    .achivka-text{font-family:var(--sans);font-size:13px;font-weight:700;color:var(--text-primary);line-height:1.2}
    .achivka-sub{font-size:11px;color:var(--text-muted);font-weight:400}
    .achivka-close{width:24px;height:24px;border-radius:50%;background:var(--surface-2);border:none;cursor:pointer;color:var(--text-muted);font-size:14px;display:flex;align-items:center;justify-content:center;flex-shrink:0;transition:.15s;margin-left:4px}
    .achivka-close:hover{background:var(--surface-3);color:var(--text-primary)}

    /* ── SHARE ROW ── */
    .share-row{display:flex;gap:6px;padding:0 16px 6px;justify-content:flex-end;opacity:0;max-height:0;overflow:hidden;transition:opacity .3s,max-height .3s}
    .share-row.show{opacity:1;max-height:40px}
    .share-btn{display:inline-flex;align-items:center;gap:5px;padding:5px 12px;border-radius:20px;border:1px solid var(--border);background:transparent;font-family:var(--sans);font-size:12px;color:var(--text-muted);cursor:pointer;transition:.18s}
    .share-btn:hover{border-color:var(--accent-border);color:var(--text-primary);background:var(--accent-bg)}
    .share-btn .bi{font-size:13px}

    /* ── SHARE CHOOSER MODAL ── */
    /* ── LIGHTBOX ── */
    .lb{display:none;position:fixed;inset:0;background:rgba(0,0,0,.92);z-index:900;align-items:center;justify-content:center;cursor:zoom-out;animation:lbIn .18s ease-out}
    .lb.open{display:flex}
    @keyframes lbIn{from{opacity:0;transform:scale(.96)}to{opacity:1;transform:scale(1)}}
    .lb-img{max-width:min(94vw,780px);max-height:88dvh;object-fit:contain;border-radius:6px;box-shadow:0 0 60px rgba(0,0,0,.5);cursor:default;user-select:none}
    .lb-close{position:absolute;top:16px;right:16px;width:36px;height:36px;background:rgba(255,255,255,.12);border:none;border-radius:50%;cursor:pointer;display:flex;align-items:center;justify-content:center;color:#fff;font-size:18px;transition:.15s;backdrop-filter:blur(4px)}
    .lb-close:hover{background:rgba(255,255,255,.22)}
    .lb-caption{position:absolute;bottom:20px;left:50%;transform:translateX(-50%);color:rgba(255,255,255,.75);font-size:13px;white-space:nowrap;pointer-events:none}

    .share-chooser{display:none;position:fixed;inset:0;background:rgba(0,0,0,.4);z-index:700;align-items:center;justify-content:center}
    .share-chooser.open{display:flex}
    .share-chooser-box{background:var(--surface-1);border-radius:20px;padding:24px;text-align:center;max-width:260px;width:90%;box-shadow:0 10px 40px rgba(0,0,0,.15)}
    .share-chooser-title{font-family:var(--sans);font-size:15px;font-weight:700;color:var(--text-primary);margin-bottom:16px}
    .share-chooser-btns{display:flex;flex-direction:column;gap:10px}
    .share-choice{display:flex;align-items:center;gap:10px;padding:12px 16px;border-radius:12px;border:1px solid var(--border);background:transparent;font-family:var(--sans);font-size:13px;color:var(--text-primary);cursor:pointer;transition:.18s}
    .share-choice:hover{border-color:var(--accent-border);background:var(--accent-bg)}
    .share-choice .bi{font-size:16px;color:var(--accent)}

    /* ── QR MODAL ── */
    .qr-modal{display:none;position:fixed;inset:0;background:rgba(0,0,0,.5);z-index:600;align-items:center;justify-content:center}
    .qr-modal.open{display:flex}
    .qr-box{background:var(--surface-1);border-radius:20px;padding:28px 24px;text-align:center;max-width:280px;width:90%}
    .qr-box img{width:180px;height:180px;border-radius:8px;margin:14px 0;display:block;margin-left:auto;margin-right:auto}
    .qr-title{font-family:var(--sans);font-size:15px;font-weight:700;color:var(--text-primary)}
    .qr-sub{font-size:12px;color:var(--text-muted);margin-top:4px}
    .qr-close{margin-top:14px;padding:8px 22px;border:1px solid var(--border);border-radius:20px;background:transparent;font-family:var(--sans);font-size:13px;cursor:pointer;color:var(--text-secondary)}
  </style>
</head>
<body>
<!-- Прогресс диалога — тонкая полоска вверху -->
<div class="dialog-progress" id="dialogProgress"><div class="dialog-progress-bar" id="dialogBar"></div></div>
<!-- Ачивка "Хорошая мама" — всплывает после 5 вопросов -->
<div class="achivka" id="achivka" aria-live="polite">
  <i class="bi bi-patch-check-fill achivka-icon" aria-hidden="true"></i>
  <div><div class="achivka-text" id="achivkaText">Хорошая мама</div><div class="achivka-sub" id="achivkaSub">Вы задали все правильные вопросы</div></div>
  <button class="achivka-close" id="achivkaClose" onclick="closeAchivka()" aria-label="Закрыть">×</button>
</div>

<canvas id="stars"></canvas>
<div class="glow"></div>
<div class="aurora" aria-hidden="true">
  <div class="aurora-b aurora-b1"></div>
  <div class="aurora-b aurora-b2"></div>
  <div class="aurora-b aurora-b3"></div>
</div>

<div class="topbar">
  <a href="/" class="logo">
    <div class="logo-box"><canvas id="logoCanvas" width="34" height="34"></canvas></div>
    <span class="logo-name">#АйДа<span>Кемп</span></span>
  </a>
  <div style="display:flex;align-items:center;gap:10px">
    <button id="themeBtn" onclick="toggleTheme()" aria-label="Переключить тему" style="width:32px;height:32px;border-radius:8px;background:var(--surface-2);border:1px solid var(--border);cursor:pointer;display:flex;align-items:center;justify-content:center;transition:.2s">
      <i id="themeIcon" class="bi bi-moon-stars-fill" aria-hidden="true" style="color:var(--text-secondary);font-size:14px"></i>
    </button>
    <a href="/" class="back" aria-label="На сайт лагеря">
      <i class="bi bi-arrow-right" aria-hidden="true" style="transform:rotate(180deg);display:inline-block"></i>
      <span>На сайт лагеря</span>
    </a>
  </div>
</div>

<main class="layout">
  <div class="intro" id="intro">
    <div class="intro-icon"><canvas id="heroCanvas" width="60" height="60"></canvas></div>
    <h1 class="intro-h">Спросите всё про<br><em id="rotWord" style="color:var(--or);font-style:normal;text-shadow:0 0 32px rgba(236,124,0,.40);transition:opacity .25s;display:inline-block">лагерь</em></h1>
    <p class="intro-s">Отвечу честно. Без скриптов и впаривания.</p>
    <div class="q-pills">
      <button class="q-pill q-bad" onclick="go('А что у вас плохо?')">А что у вас плохо?</button>
      <button class="q-pill" onclick="go('Сколько стоит и что входит в цену?')">Сколько стоит?</button>
      <button class="q-pill" onclick="go('Как кормят в лагере?')">Как кормят?</button>
      <button class="q-pill" onclick="go('Телефон у ребёнка заберут?')">Телефон заберут?</button>
      <button class="q-pill" onclick="go('Что такое хакатон?')">Что за хакатон?</button>
      <button class="q-pill" onclick="go('Можно без опыта программирования?')">Без опыта можно?</button>
      <button class="q-pill" onclick="go('Как добраться до лагеря?')">Как добраться?</button>
      <button class="q-pill" onclick="go('Можно вернуть деньги если не понравится?')">Возврат денег?</button>
      <button class="q-pill" onclick="go('Расскажи историю из лагеря')">Расскажи историю</button>
    </div>
    <div class="hint"><span class="hint-dot"></span>Выберите возраст — дальше всё расскажу</div>
    <div class="age-btns">
      <button class="age-btn" onclick="if(typeof ym!=='undefined')ym(96499295,'reachGoal','ask_age_select',{age:'7-9'});go('Нам 7–9 лет')">
        <span class="n">7–9</span>
      </button>
      <button class="age-btn" onclick="if(typeof ym!=='undefined')ym(96499295,'reachGoal','ask_age_select',{age:'10-12'});go('Нам 10–12 лет')">
        <span class="n">10–12</span>
      </button>
      <button class="age-btn" onclick="if(typeof ym!=='undefined')ym(96499295,'reachGoal','ask_age_select',{age:'13-16'});go('Нам 13–16 лет')">
        <span class="n">13–16</span>
      </button>
    </div>
  </div>

  <div class="messages" id="msgs"><div id="msgs-spacer"></div></div>
  <div class="typing" id="typing"><span class="td"></span><span class="td"></span><span class="td"></span></div>

  <!-- Кнопки шаринга — появляются после 3 обменов -->
  <div class="share-row" id="shareRow">
    <button class="share-btn" onclick="showShareChooser()">
      <i class="bi bi-share" aria-hidden="true"></i><span> Отправить подруге</span>
    </button>
    <button class="share-btn" id="qrBtn" style="display:none" onclick="showQR()">
      <i class="bi bi-qr-code" aria-hidden="true"></i><span> Открыть на телефоне</span>
    </button>
  </div>

  <!-- Share channel chooser modal -->
  <div class="share-chooser" id="shareChooser" onclick="if(event.target===this)closeShareChooser()">
    <div class="share-chooser-box">
      <div class="share-chooser-title">Отправить подруге</div>
      <div class="share-chooser-btns">
        <button class="share-choice" onclick="shareToWhatsApp()">
          <i class="bi bi-whatsapp" aria-hidden="true"></i> WhatsApp
        </button>
        <button class="share-choice" onclick="shareToTelegram()">
          <i class="bi bi-telegram" aria-hidden="true"></i> Telegram
        </button>
      </div>
    </div>
  </div>

  <div class="input-area">
    <div class="input-box">
      <button class="book-quick" onclick="openPopup()" aria-label="Забронировать смену" title="Забронировать смену">
        <i class="bi bi-calendar-check" aria-hidden="true"></i>
      </button>
      <textarea class="inp" id="inp" placeholder="Спросите про лагерь..." rows="1" onkeydown="if(event.key==='Enter'&&!event.shiftKey){event.preventDefault();send()}" oninput="autoGrow(this)"></textarea>
      <button class="mic" id="micBtn" onclick="toggleMic()" aria-label="Голосовой ввод" title="Голосовой ввод">
        <i class="bi bi-mic" id="micIcon" aria-hidden="true"></i>
      </button>
      <button class="send" id="sendBtn" onclick="send()" aria-label="Отправить">
        <i class="bi bi-send" aria-hidden="true"></i>
      </button>
    </div>
  </div>
</main>

<!-- Lightbox для просмотра фото -->
<div class="lb" id="lb" onclick="if(event.target===this||event.target.classList.contains('lb'))closeLb()">
  <button class="lb-close" onclick="closeLb()" aria-label="Закрыть"><i class="bi bi-x-lg" aria-hidden="true"></i></button>
  <img class="lb-img" id="lbImg" src="" alt="">
  <span class="lb-caption" id="lbCaption"></span>
</div>

<!-- QR-модал "Открыть на телефоне" -->
<div class="qr-modal" id="qrModal" onclick="if(event.target===this)closeQR()">
  <div class="qr-box">
    <div class="qr-title">Открыть на телефоне</div>
    <div class="qr-sub">Наведите камеру на код</div>
    <img id="qrImg" src="" alt="QR-код для открытия чата на телефоне">
    <button class="qr-close" onclick="closeQR()">Закрыть</button>
  </div>
</div>

<div class="overlay" id="overlay" onclick="if(event.target===this)closePopup()">
  <div class="popup">
    <div class="ph">
      <div class="ph-title">Забронировать смену</div>
      <button class="px" onclick="closePopup()" aria-label="Закрыть">
        <i class="bi bi-x-lg" aria-hidden="true"></i>
      </button>
    </div>
    <div class="pb">
      <span class="pfree">Место фиксируется на 48 часов</span>
      <label class="plbl">Возраст</label>
      <div class="par">
        <button class="pab" onclick="pa(this)">7–9 лет</button>
        <button class="pab on" onclick="pa(this)">10–12 лет</button>
        <button class="pab" onclick="pa(this)">13–16 лет</button>
      </div>
      <label class="plbl">Смена</label>
      <select class="psel" id="popShift"></select>
      <label class="plbl">Телефон</label>
      <input class="pin" type="tel" id="popPhone" placeholder="+7 (___) ___-__-__">
      <p id="popHintPhone" style="display:none;color:#ef4444;font-size:13px;margin-top:6px;">Введите номер полностью — 11 цифр (+7 …)</p>
      <label class="pcons"><input type="checkbox" id="popConsentPolicy"> Ознакомлен(а) с <a href="/privacy-policy/" target="_blank">Политикой обработки ПД</a></label>
      <label class="pcons"><input type="checkbox" id="popConsent"> Согласен(а) на <a href="/soglasie-na-obrabotku-personalnyh-dannyh/" target="_blank">обработку персональных данных</a></label>
      <p id="popHintConsent" style="display:none;color:#ef4444;font-size:13px;margin-top:6px;">Поставьте обе галочки — без них не можем принять заявку</p>
      <button class="psub" id="popSubmit" onclick="submitBooking()">Забронировать →</button>
      <div class="ptr">Пн–Вс 11:00–20:00 МСК · Лицензия Минобра<br>Налоговый вычет 13% через ФНС</div>
    </div>
    <div class="pback"><a href="#" onclick="closePopup();return false">← Вернуться к чату</a></div>
  </div>
</div>

<script>import '../scripts/pages/ask'</script>

<!-- Yandex.Metrika -->
<script is:inline>
(function(m,e,t,r,i,k,a){m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)};
m[i].l=1*new Date();
for(var j=0;j<document.scripts.length;j++){if(document.scripts[j].src===r){return}}
k=e.createElement(t),a=e.getElementsByTagName(t)[0],k.async=1,k.src=r,a.parentNode.insertBefore(k,a)})
(window, document, "script", "https://mc.yandex.ru/metrika/tag.js", "ym");
ym(96499295, "init", {
  clickmap:true,
  trackLinks:true,
  accurateTrackBounce:true,
  webvisor:true
});
</script>

</body>
</html>
