# Handoff — шаблон

_Для будущего Клода в новом чате. Заполняется в конце сессии._

---

## Сессия: [дата] [краткое название]

### Контекст за 30 секунд

[Одна-две фразы: что было сделано, что ещё нет, на чём остановились]

### Что сделано

- [факт 1]
- [факт 2]

### Что НЕ сделано / осталось висеть

- [задача] → [следующий шаг, ветка, PR-ссылка]

### Критические состояния инфраструктуры

- Сервер: `ssh root@159.194.223.55`
- Dev: `dev.aidacamp.ru` (процесс node порт 4181, PID из `pgrep -fa node`)
- Prod: static nginx на `aidacamp.ru`
- Dev-deploy: `rsync` из `/var/www/aidacamp-dev/repo/dist/` → не автоматически, только после `npm run build`
- AlfaCRM env: загружаются через `source /var/www/aidacamp-dev/.env` — systemd-сервис сломан

### Правила (не забыть)

- Никогда не писать напрямую в `dev` — только через `agent/*` ветку + PR
- Один агент = одна зона файлов (см. `.claude/AGENTS.md`)
- После `gh pr create` — сообщить оркестратору ссылку на PR
